# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

한국 주식 투자 추천 시스템(Spring Boot 3.x). 외부 OpenAPI(DART, 한국투자증권) 연동 + JPA/PostgreSQL + Redis 캐싱. 추천 API + **공시 기반 신호 알림 파이프라인** end-to-end 검증 완료.

### 신호 알림 파이프라인 (최종 목표 기능, Phase 1 완료)
흐름: **DART 공시 폴링(1분)** → 워치리스트 종목 신규 공시 적재 → **신호 평가(1분)**: [일봉 게이트] 거래량 급증(시간보정 20일평균×2) + 상승추이(당일 +1.5%) → [분봉 필터] 신선도/활성도 → **추천 의견 ≥ 중립(score≥40)** → **Discord 알림** (1회 후 `notified`로 중복 방지).
- **분봉 신선도/활성도 필터**(`MarketSignalService.isFreshAndActive`): "알림 왔는데 이미 올라 끝났고 거래도 식음" 문제 대응. 일봉 게이트 통과 후보에 한해 KIS 당일 분봉(`fetchMinuteCandles`, FHKST03010200) 조회 → ① 최근 N분(기본 5) 모멘텀 ≥ 0.3%(지금도 상승중) ② 최근 분당거래량 ≥ 당일 평균분당 ×1.5(지금도 활발) 둘 다 충족해야 알림. 분봉 데이터 없으면 일봉만으로 degrade. (실측: +15.94% 급등주 006490이 활성도 0.27배로 정상 차단됨.)
- 조기 포착 위해 `min-change-rate` 기본 3%→**1.5%**, 평가 주기 5분→**1분**으로 완화하되, 위 분봉 필터가 "식은 신호"를 걸러 노이즈 억제.
- **동시호가/장외 제외**: `MarketSignalService.evaluate`가 연속매매(`session-start`~`session-end`, 기본 09:00~15:20) 밖이면 즉시 empty(KIS 호출도 생략). 당일 거래일(`businessDate==today`) 아니면(휴장/거래정지) 제외. → 동시호가 때 B/C 대량 오발동 차단.
- **부실/저가주 제외**(`StrategyEvaluator`): 매수가 `min-price`(기본 1000원) 미만 저가주 제외 + KIS 현재가의 `mang_issu_cls_code`(관리종목)·`sltr_yn`(정리매매)·`mrkt_warn_cls_code`(투자경고)로 부실 종목 제외(`KisQuoteResponse.Output.isHealthy()`). C는 하락폭 `meanReversionMinDrop`~`meanReversionMaxDrop`(기본 -3%~-12%)만 — 그 이상 폭락은 제외.
- ⚠️ **거래량 시간대 보정**(`MarketSignalService.sessionElapsedFraction`): 장중 '오늘 누적거래량'을 20일 '하루' 평균과 그대로 비교하면 장 마감 전엔 2배 도달이 구조적으로 불가능 → 분모(평균)에 경과 거래시간 비율(KRX 09:00~15:30=390분)을 곱해 보정. 과거 거래일/마감 후 데이터는 1.0. (이 보정 없이는 +15% 급등주도 미포착됐음 — 실측으로 확인.)
- 진입점: `SignalScheduler`(cron, 장중 평일) — 공시 폴링(1분)/신호 평가(1분)/후속 추적(1분)/B·C 스캔(15분)/워치리스트 동기화(16:00)/**일별 리포트(16:30)** 스케줄. 수동 트리거 `POST /api/v1/admin/{poll-disclosures,scan-signals,track-followups,sync-watchlist,scan-market,daily-report}`, `GET /api/v1/admin/{signal/{code},strategy-report,exit-timing,outcome-analysis}`.
- **일별 실매매 리포트**(`DailyReportService`, 평일 16:30 Discord, 2026-07-10 실매매 기준으로 전환): **[시장] 코스피/코스닥 등락률**(`KisApiClient.fetchIndexChangeRate`, 지수 FHPUP02100000, 코스피 0001/코스닥 1001) + **오늘 실매매 건별**(LIVE 매수: 종목명·전략라벨·체결가 → ✅익절/🛑손절 실현손익·⏳미청산 경고) + **전략별 누적 실현손익(원·승률)** + **일별 실현손익 그래프**(최근 10거래일) + 전략 진단 감시 플래그(대조군 기반). ⚠️ 섀도우(가상매수) 성과·권장 청산시점은 리포트에서 **제외**(사용자 요청 — 상세는 `/admin/strategy-report`·`exit-timing` 조회). 잔고보정(reconcile) 청산은 손익 미상이라 집계 제외. `daily-report-cron` 설정. 단위테스트 `DailyReportServiceTest` 2.

### 다전략 섀도우 백테스트 (`strategy/` + `StrategyReportService`)
- 5개 전략 `TradingStrategy` 빈, 각자 **`scope()`**: `MOMENTUM_A`=**DISCLOSURE**(공시가 촉매), `VOLUME_LEADING_B`·`MEAN_REVERSION_C`·`INDEX_RELATIVE_D`·`BREAKOUT_E`=**MARKET_SCAN**(공시 무관, 전 종목 스캔). 공통 평가는 `StrategyEvaluator.evaluateStock`(종목당 REQUIRES_NEW): 거래량급증 게이트 → 매도 제외 → scope 필터 전략 평가 → (전략·종목·일자) 1회 기록. 전략 빈은 Spring이 자동 주입하므로 새 전략은 `@Component` + `scope()`만으로 파이프라인(control-group·perf-gate·리스크·사이징)에 자동 편입.
  - **A**: `DisclosurePollingService`→`SignalAlertService`→공시 종목에 DISCLOSURE 전략 평가.
  - **B·C**: `WatchlistScanService.scanAll`이 **15분마다 워치리스트 1500종목 전체**에 MARKET_SCAN 전략 평가(공시와 독립). `TradeOutcome.disclosureId`=null. ⚠️ 전 종목 순회라 무거움(스캔당 ~1500 KIS 일봉콜) → 15분 주기 + scheduler pool 분리.
  - **B 방향 필터**(라이브 관찰 반영): B는 `volumeSpike` + 가격 횡보 + 점수≥40인데, 횡보 밴드가 **비대칭** — `volume-leading-min-change`(기본 0.0)~`volume-leading-max-change`(기본 1.0). 즉 **당일 등락률 < 0(하락 중)은 제외**(거래량 급증+하락=분산일 수 있어). 이전엔 `Math.abs(changeRate)≤max`로 하방도 통과해 알림이 폭주했음 → 비대칭으로 노이즈↓ + 상방 가설 정합. `SIGNAL_B_MIN_CHANGE`로 조정(예: -0.3=약간 노이즈 허용). 당일 등락률만 써 추가 KIS 호출 0. (더 강한 분봉 모멘텀 필터는 B 후보당 분봉콜 부담이라 미적용.) **B 볼륨 하한 실험(2026-07-30)**: 3주 진단 — B 일반주 진입분(−1.76%/23%)이 대조군 탈락분(−1.3~−1.5%)보다 나쁨 + 볼륨 배수 단조: 2~6배(표본 87%) −1.8~−2.0% vs **6~15배 −0.59%/35%** → "거래량 선행은 확실한 급증에서만 작동". **B 전용 하한 `volume-leading-min-ratio`(코드 기본 0=비활성)** — 미달은 `WEAK_VOLUME` 대조군 기록. ⚠️ n=20·위기국면 데이터 기반 실험 — **대조군(WEAK_VOLUME)이 진입분보다 좋으면 원복**. ✅ **2026-08-14 원복 완료(prod 3.0→0)**: 사전 합의된 원복 조건이 충족됨 — 대조군 `WEAK_VOLUME` **+0.33%(n=1,758)** vs 진입 **−0.50%(n=589)**(close, 단일일 클러스터 아님). 도입 근거(n=20)보다 표본이 훨씬 크고 부호가 반대라, "거래량 선행은 확실한 급증에서만 작동" 가설이 현 데이터에선 기각. 인버스 경로 미적용. env `SIGNAL_B_MIN_VOLUME_RATIO`. 승/패 feature상 B는 고강도·고ret5d 승자(반등계열과 반대)라 RET5D/과열 가드 비대상 재확인. **⚠️ B LIVE 화이트리스트 제외(2026-08-21, 사용자 결정)**: `feature-mining` 진입-대조군 비교에서 B는 **edge −0.61%p**(진입 net −1.44% vs 대조군 −0.83%, n=321, 24거래일, close, 커버리지 100%) — **B가 고른 종목이 같은 풀에서 B가 안 고른 종목보다 나빴다**(선택이 가치를 파괴). 여기에 ① 집행 드래그 `execution-quality` gap **−1.21%p**(real −0.57% vs shadow +0.64%, n=34, 진입슬립 +0.21%로는 설명 안 됨) ② **딥바잉 특성 소멸** — B의 정체성이던 '흐름↓ 우위'가 lag30에선 남아 있으나(−0.21 vs −0.48) **lag60에선 사실상 동률**(−0.54 vs −0.49)이 겹친다. → prod `TRADING_LIVE_STRATEGIES`에서 제거(게이트도 차단 중이라 즉각적 실동작 변화는 없고 **이중 차단**). 섀도우·대조군 기록은 유지 → 재편입 판단 근거는 계속 축적. ⚠️ `WEAK_VOLUME` 대조군 +0.33%(n=1,758)는 8/14 원복으로 **동결된 표본**이니 볼륨 하한 재도입 근거로 쓰지 말 것(원복 판단 자체가 그 표본으로 내려졌다). **B 인버스 지수 위임(2026-07-29)**: B 인버스 경로는 ETF 볼륨만 보고 지수를 안 물어 혼조일 휩쏘 반복(실현 4전4패 평균 −0.31% 실측) → **인버스 후보는 `InverseIndexStrategy.rejectReason` 통과 필수**(당일 밴드·mom 동시하락·낙폭상한·반등일 fade — 지수 판정 단일 소스, 거부 시 `IDX_` 접두 사유). env `SIGNAL_B_INVERSE_REQUIRE_INDEX`(compose 패스스루). 테스트 2.
  - **전역(전략 무관) 진입 필터 3종(2026-08-18)**: 지금까지 필터는 전부 "전략별 조건부"였는데, `feature-mining`(27거래일, `clustered=false`, close horizon)에서 **전략이 아니라 조건 자체가 지는 구간**이 나왔다 — 셋 다 **진입-대조군 edge가 음수**(그 조건에선 진입하는 것 자체가 미진입보다 나빴다). → 전 전략 공통 게이트: ① **`max-entry-change-pct`(10%)** — 당일 등락률 ≥10% 진입 **−1.89%(n=306)** vs 같은 구간 대조군 **+0.25%** → **edge −2.14%p**(셋 중 근거 최강). "이미 10% 오른 걸 사는 것"은 전략 종류와 무관하게 졌다. ② **`max-entry-volume-ratio`(15배)** — ≥15배 진입 −2.56%(n=152, edge −0.71%p), 8~15배도 −1.52%(n=169) = **거래량배수와 수익이 단조 역방향**(B 볼륨하한 원복과 같은 방향의 증거). ③ **`min-entry-market-cap-eok`(1000억)** — 시총 <1000억 진입 −1.89%(n=163, edge −0.36%p), 유동성 필터(거래대금·슬리피지)로는 안 걸러지던 구간. 사유 `CHANGE_OVERHEAT`/`VOLUME_EXTREME`/`MICRO_CAP`, 전부 0=비활성. **인버스는 제외**(등락률·시총의 의미가 다름). ⚠️ **판정 위치가 설계의 핵심** — 모든 전략별 가드 **뒤**에 둔다: 어차피 탈락할 후보가 아니라 **진입했을 후보**만 걸러야 "차단이 옳았나"를 대조군으로 측정할 수 있고 기존 reject 사유 통계도 오염되지 않는다. 차단분은 `tracksControl` 무관 **강제 대조군 기록**(F/H/K 등 control-off 전략도 검증 가능하게). ⚠️ **근거는 전략 풀링 표본**이라 효과가 전략별로 갈릴 가능성이 남아 있다(topStrategy가 ①③은 F, ②는 E) — 강제 대조군이 쌓이면 전략별로 분해해 재검할 것. env `SIGNAL_MAX_ENTRY_CHANGE_PCT`/`SIGNAL_MAX_ENTRY_VOLUME_RATIO`/`SIGNAL_MIN_ENTRY_MARKET_CAP_EOK`(compose 패스스루). **+ ④ 뉴스 과다 `max-entry-news-cnt-1h`(2026-08-24, 사용자 결정, prod=3)**: 8/22 소급으로 대조군 커버리지 100%가 확보된 뒤 `뉴스1h건수` 축이 **셋 다 단조 악화**임이 확인됐다 — 진입 net `<1` −0.64 / `1~3` −1.00 / `≥3` −1.40, **대조군도 같은 방향**(−0.78/−0.89/−0.96 = 뉴스 나는 종목 자체가 나쁘다), 그 위에 **edge가 추가로 −0.44까지** 벌어진다(그 구간에선 우리 선택이 더 나쁘다). base rate 열위와 선택 열위가 **더해지는** 구간. ⚠️ **8/22 원안(≥1건 차단, 진입의 38% 제외)이 아니라 ≥3건만 차단한다** — 40일 창 재측정에서 `≥1` 구간 edge가 **−0.01로 사실상 0**이 됐고(8/22엔 −0.11) 기대 개선이 0.1%p 미만이라 38%를 버리는 비용을 정당화 못 했다. `≥3`은 진입의 **~9%(n=351)**뿐이고 edge −0.20으로 유지된다. ⚠️ **효과 크기가 창에 따라 흔들리는 축**이라는 게 이 축의 성질이다(방향은 시간분할 4/4 셀 통과, 크기는 −0.12↔−0.67로 불안정) — 크기를 믿지 말 것. ⚠️ **추가 KIS 호출 ≈ 0** — 진입 시 태깅으로 어차피 하던 종목당 1콜을 앞당길 뿐이고, 진입했을 후보에만 걸리는 위치라 대조군 전체로 번지지 않는다. ⚠️ 기존 `FRESH_BAD_NEWS`(C/D/J·악재 키워드)와 **다른 층위**(전 전략·뉴스 유무·내용 무관). 사유 `NEWS_HEAVY`, 차단분 강제 대조군 기록, 0=비활성, 인버스 제외. env `SIGNAL_MAX_ENTRY_NEWS_CNT_1H`(compose 패스스루). ⚠️ **구표본 처리는 since 리셋이 아니라 `GateRefilter` 전역 규칙**(아래) — 조이는 필터라 재필터가 정확히 들어맞고, 전 전략 fail-closed 공백을 피한다.
  - **다일 과열 필터(2026-07-29)**: 반등 계열(H/G/J) 진입 시 **최근 5일 수익률 > `ret5d-overheat-max-pct`(10%)면 보류(`RET5D_OVERHEAT`)** — 흐름↑ 진입 분석(n1,419)에서 ret5d 단조 관계 실측: 5일 −5%↓ 진입 +0.17%/48% vs +10%↑ 진입 −1.21%. 반등계열만 깨끗하게 갈림(H +0.17 vs −1.23 / G +0.46 vs −0.87), K/B는 방향 반대·E는 정체성이라 미적용. `SignalResult.ret5dPct` 재사용(KIS 0). 차단분 대조군 강제 기록. 체결강도(당일)과 합쳐 "추격 금지"가 시간축 양쪽 완성. env `SIGNAL_RET5D_OVERHEAT_MAX_PCT`(0=비활성, compose 패스스루).
  - **과열 추격 필터(2026-07-24, HLB 계열 3종 −75,214원 계기)**: 반등 계열(`OVERHEAT_FAMILY`=H/G/J) 진입 직전 **체결강도 ≥ `exec-overheat-min-strength`(200)면 보류(`EXEC_OVERHEAT`)** — 근거: 7/16~20 3거래일 일관 "반등계열 승자 강도 < 패자"(H 133vs165, G 133vs146, J 132vs196) + 강도 200+ 구간 전체 net 음수 + 7/24 H가 +22% 급등 중 HLB 계열을 추격 매수해 서킷 청산. 체결강도 1콜은 진입 태깅과 공유(lazy). 차단분은 tracksControl 무관 대조군 기록(REBOUND_DAY와 동일 예외 — forward 검증). 추세 계열(F/K/E)은 승자가 고강도라 미적용(전략 조건부 원칙). env `SIGNAL_EXEC_OVERHEAT_MIN_STRENGTH`(0=비활성, compose 패스스루). **⚠️ 가드 대상 전략 csv 분리 — H 제외(2026-08-25, 사용자 결정)**: 표본이 20배 쌓이자 **가드의 옳고 그름이 전략별로 갈렸다** — `control-analysis`(close, 25거래일)에서 **H는 차단분 `EXEC_OVERHEAT` −0.07%(n=103) > 진입분 −0.74%(n=758)**로 **가드가 더 나은 후보를 버리고 있다(+0.67%p)**. 반면 J는 차단분 −1.57% < 진입분 −0.28%로 가드가 옳고(8/24 확인), G도 차단분이 낫지 않다. → `exec-overheat-strategies`(csv, **코드 기본 H,G,J = 종전 동작**, prod만 `RSI_REVERSAL_G,VALUE_REVERSAL_J`). ⚠️ **`RET5D_OVERHEAT`은 분리하지 않았다** — 종전대로 `OVERHEAT_FAMILY`(H/G/J) 전체에 걸린다(H의 RET5D 차단분은 진입분보다 낫지 않다). ⚠️ 도입 근거(2026-07-24)가 **3거래일 표본**이었다는 점이 요점이다 — 얇은 표본으로 넣은 가드는 두꺼워졌을 때 반드시 재검할 것. ⚠️ H는 LIVE 제외 상태라 **실동작은 섀도우뿐**이고, 제외로 그 후보들이 진입군에 들어와 forward 검증된다. env `SIGNAL_EXEC_OVERHEAT_STRATEGIES`(compose 패스스루).
  - **반등일 순추세 보류 가드(2026-07-22)**: 폭락 후 V자 초입 급반등일 — **당일 지수 ≥ `rebound-day-min-surge-pct`(2%) 급등 AND 기저(MA3, intraday 보정 前) 라벨 비강세** — 엔 순추세 계열(`TREND_FAMILY`=H/F/E/K) 신규진입 보류(`REBOUND_DAY`). 2사이클 실측 근거: 7/15 H 12건 1승 −34,639원(흐름버킷 통과 진입) + 7/22 H 8건 0승 −81,672원(중립버킷 통과 진입) — **게이트를 정상 통과한 진입이 갭 되돌림에 반복 전멸**(버킷 통계는 평상시를 검증한 것). 안정 강세장(기저 BULL)은 미발동 → H 정상 매매. 차단분은 REBOUND_DAY 대조군으로 forward 검증(차단이 수익이면 완화) — **단 H/F 등 control-off 전략은 기록이 안 남아 7/23 예외 추가: REBOUND_DAY 차단만은 `tracksControl`/집중 설정 무관 대조군 기록**(반등일×TREND_FAMILY 한정 부담). 판정 `MarketRegimeService.isReboundDay`(정적, 테스트 3 — 60s 지수캐시 재사용, 추가 KIS 0). 역추세(C/D)·B·인버스 무관. env `SIGNAL_REBOUND_DAY_GUARD_ENABLED`/`MIN_SURGE_PCT`(compose 패스스루). ⚠️ **K 제외(2026-08-18)**: 강제 대조군 기록의 forward 검증에서 **K에는 가드가 역효과**임이 드러남 — `REBOUND_DAY` 차단분 **+3.04%(n=89)** vs K 진입분 **−0.61%(n=381)**(close). 같은 전략에 걸린 `INDEX_GAP_DAY`가 차단분 −2.93%(n=26)로 **정반대 성적**인 것과 대비된다(가드가 다 옳은 게 아니라 전략별로 갈린다). K의 정체성이 "어제까지 국면 + 오늘 시초가 갭"이라 V자 반등일 아침의 갭업은 회피 대상이 아니라 표적에 가깝다(가드의 계기는 H/F 갭 되돌림 전멸이었다). → 대상을 하드코딩 `TREND_FAMILY`에서 **csv env `SIGNAL_REBOUND_DAY_STRATEGIES`(기본 H,F,E)**로 전환(원복하려면 `OPENING_GAP_K` 추가). ⚠️ K 진입 조건이 바뀌므로 **`TRADING_PERF_GATE_STRATEGY_SINCE`의 K 날짜를 배포일로 갱신**해야 구표본이 섞이지 않는다.
  - **신선 악재뉴스 가드(2026-07-15, SK디앤디 −54,485원 실손실 계기)**: 반등 계열(C/D/J)이 **하락 중 진입하려 할 때, 최근 `fresh-news-window-minutes`(60분) 내 뉴스 제목에 `bad-news-keywords`(유상증자·감자·구조조정·관리종목·상장폐지·거래정지·소송 등, csv env) 매치 시 진입 보류(`FRESH_BAD_NEWS`)** — C의 전제는 "이유 없는 낙폭"인데 신선한 악재 뉴스가 있는 하락은 "이유 있는 하락"(실측: 유상증자 뉴스 13분 후 C 진입 → −9.8%). **호재/중립 뉴스는 키워드 미매치로 통과**(하락 중 호재 반등 시나리오 보존 — 사용자 지적 반영). 뉴스 조회는 진입 직전 후보만 1콜(진입 태깅과 공유), 차단분은 대조군 추적으로 필터 자체 검증. 판정 `KisNewsResponse.freshBadNews`(순수, 테스트 3). env `SIGNAL_BAD_NEWS_KEYWORDS`/`SIGNAL_FRESH_NEWS_WINDOW_MINUTES`. v2 후보: LLM 제목 분류.
  - **C 선별 강화**(승자/패자 분석 반영, `outcome-analysis` p10/p30 n≈140 기준): ① **분봉 반등확인**(`mean-reversion-require-rebound`, 기본 on) — C는 당일 하락이라 `freshActive`(상승 전제)를 못 써서, 일중 방향 무관한 **`reboundActive`**(최근 분봉 모멘텀↑·거래활발, `MarketSignalService`가 A후보(상승)·C후보(하락폭≥minDrop)에만 1회 계산해 분봉콜 절감)를 새로 만들어 "떨어지는 칼날" 회피. ② **C 점수 게이트**(`mean-reversion-min-score`, **기본 40** — 공통 `min-opinion-score`와 동일로 완화. 당초 55→50→40, 분봉 반등확인이 1차 선별을 충분히 해줘 점수는 완화하고 C 표본 확보 우선. ⚠️ 승자/패자 분석상 승자 점수(56.7)가 패자(53.7)보다 높아 한때 50으로 뒀던 근거가 있으니, 표본 쌓이면 재상향 검토). `SIGNAL_C_REQUIRE_REBOUND`/`SIGNAL_C_MIN_SCORE`로 조정. **⚠️ C LIVE 화이트리스트 제외(2026-08-25, 사용자 결정)**: B(8/21)·F·H(8/21)·E(8/24)와 **동일 기준** — `control-analysis`(close, 25거래일)에서 **C가 거른 종목이 C가 산 종목보다 낫다**: 진입 **−1.15%(n=197, 승률 27%)** vs `NO_REBOUND` **−0.39%(n=1,082)** · `DROP_RANGE` **−0.63%(n=4,606)** → 자기 필터 대비 각각 **+0.76%p·+0.52%p 열위**. 즉 "이유 없는 낙폭을 산다"는 C의 전제가 현 데이터에서 지지되지 않는다(반등확인·낙폭밴드 둘 다 거른 쪽이 나음). prod `TRADING_LIVE_STRATEGIES` 7개 → **6개**. ⚠️ 게이트도 이미 차단 중이라(KOSPI 1/30·KOSDAQ 3/30) **즉각적 실동작 변화는 없고 이중 차단**. ⚠️ C는 유일한 스윙(D+1) 전략이라 제외로 **오버나잇 경로가 사실상 비었다**(swing-strategies 설정은 유지). 섀도우·대조군 기록은 유지 → 재편입 근거는 계속 축적. ⚠️ 멀티데이 쪽 C 흑자 기록(D+1 +5.77%)은 **완주 150건 중 89%가 20260626 하루**라 클러스터 가드가 이미 권장에서 제외한 상태다 — 재편입 근거로 쓰지 말 것. (추가 후보: 초대형주 패배 편향 → 시총 상한, 단기 KOSDAQ 약세 — 미적용.)
  - **D 지수상대 역추세**(`IndexRelativeStrategy`, 롱온리 "짝꿍(페어) 매매" 변형 — 공매도 불가 환경 대응): 숏 다리 대신 **해당 시장 지수를 무포지션 기준선**으로, **잔차=종목등락률−지수등락률 ≤ `-index-relative-min-gap`(기본 2.0%p)**면 롱(지수 대비 과소→상대 회귀 노림). C(절대 -3~-12%)와 달리 **상대 부진**을 봐서 시장 동반하락(회귀 약함)을 걸러내고 혼자 뒤처진 종목(회귀 강함)만 → 방향성 의존↓(준헤지), 약세·횡보장 보완. 지수 등락률은 `StrategyEvaluator`가 이미 계산하던 `marketChange`를 `StrategyContext`에 실어 사용(추가 KIS 호출 0). 떨어지는칼날 회피: 절대하락(≤`-mean-reversion-min-drop`) 후보면 분봉 `reboundActive` 요구. 절대 폭락(≤`-index-relative-max-drop` 12%)·점수(`index-relative-min-score` 40) 게이트. `rejectReason`: `NO_INDEX`/`NO_VOLUME`/`TOO_DEEP`/`GAP`/`NO_REBOUND`/`SCORE`. env `SIGNAL_D_MIN_GAP`/`SIGNAL_D_MAX_DROP`/`SIGNAL_D_MIN_SCORE`/`SIGNAL_D_REQUIRE_REBOUND`. **`alerts()=false`(조용한 섀도우 — 가상매수/성과기록은 되나 Discord 미발송)** — ⚠️ **화이트리스트 미포함은 코드 기본값 기준이며 현행 prod는 D가 화이트리스트에 편입돼 LIVE 실매매 중**(2026-07-31 실측). perf-gate로 net 검증되면 `TRADING_LIVE_STRATEGIES`에 추가해 실전 편입(→ prod는 이미 완료). 단위테스트 `IndexRelativeStrategyTest` 12. **⚠️ D BEAR 국면 제외(2026-08-24, 사용자 결정)**: K(8/21)와 **같은 처방** — 제외가 아니라 **국면 제한**이 답인 전략이다. `feature-mining` 국면 분해(40일·close·대조군 커버리지 100%)에서 진입-대조군 edge가 **BULL +0.62%p(대조군 n=1,142) · NEUTRAL +1.51%p(n=869) ↔ BEAR −0.48%p(n=1,712)**로 갈리고, BEAR 진입 net은 **−1.83%(n=135)**로 절대치도 최악이다. 대조군 표본이 **시스템 내 최강급**이라 근거 강도가 높다. → csv `index-relative-allowed-regimes`(코드 기본 `BULL,NEUTRAL,BEAR` = 제약 없음 = 종전 동작) 신설, **prod만 `BULL,NEUTRAL`**. 사유는 `REGIME_` + 국면. ⚠️ **판정 위치는 다른 D 조건을 다 통과한 뒤** — 그래야 `REGIME_BEAR` 대조군이 "D 조건 다 만족했으나 BEAR"가 돼 ENTERED와 직접 비교되고, 기존 `GAP`/`NO_REBOUND` 사유 통계도 안 오염된다. ⚠️ **국면 미상(null)은 통과**(degrade open). ⚠️ D는 K와 달리 **전일 확정 라벨이 아니라 현재(intraday 보정) 라벨**을 쓴다 — `trading.prior-day-regime-strategies`에 D는 없고, D는 개장 창 전용이 아니라 하루 종일 도는 전략이라 지금의 국면이 판단 대상이다. ⚠️ **`TRADING_PERF_GATE_STRATEGY_SINCE`는 갱신하지 않았다** — K 때와 달리 여기선 리셋이 불필요하다: 게이트가 이미 (시장,국면) 버킷으로 채점하므로 BEAR 표본은 BULL/NEUTRAL 판정에 안 섞이고, 전국면 fallback 풀에 남는 BEAR 표본(net −1.83%)은 문턱을 **더 높이는** = fail-closed 방향이다. 리셋하면 수 주간 실주문만 막고 격리 이득은 없다. ⚠️ **이 판단은 일반화하지 말 것** — K는 라벨 *소스*가 바뀐 변경(8/13 전일국면 전환)이 겹쳐 있어 리셋이 필요했다. env `SIGNAL_D_ALLOWED_REGIMES`(compose 패스스루). 테스트 5. ⚠️ 현재 국면(KOSPI 중립/KOSDAQ 약세)이 준헤지 성격과 잘 맞아 표본 축적 관찰 가치 높음.
  - **E 신고가 돌파 모멘텀**(`BreakoutStrategy`, 공격적 — "강세를 산다"): 직전 `breakout-lookback`(기본 20)거래일 최고가(오늘 제외)를 거래량 급증과 함께 **돌파**(현재가 ≥ 직전최고가 ×(1+`breakout-buffer-pct`/100))하면 롱. A(공시상승) 외 B/C/D의 횡보·역추세·상대부진 편향과 **반대 방향** → 전략 다양성(비상관)↑, 포트폴리오 다중검정 위험 완화. 직전 최고가는 `MarketSignalService`가 이미 조회한 일봉에서 계산해 **`SignalResult.priorHigh`(고가 우선, 없으면 종가)**에 실음(추가 KIS 호출 0). `rejectReason`: `NO_VOLUME`/`NO_HIGH`(데이터부족)/`NOT_BREAKOUT`/`SCORE`. env `SIGNAL_BREAKOUT_LOOKBACK`/`SIGNAL_BREAKOUT_MIN_SCORE`/`SIGNAL_BREAKOUT_BUFFER_PCT`. **`alerts()=false`(조용한 섀도우)** — ⚠️ **화이트리스트 미포함은 코드 기본값 기준이며 현행 prod는 E도 화이트리스트 편입**(2026-07-31 실측, 단 perf-gate가 net 검증 전엔 실주문 차단하므로 사실상 게이트 통과 시에만 LIVE). ⚠️ 공격적(추세 초입 추격 → 변동성·손실폭 큼)이라 catastrophic stop 타이트하게(예 -5%)·perf-gate 문턱 상향 권장. 단위테스트 `BreakoutStrategyTest` 6. **⚠️ E LIVE 화이트리스트 제외(2026-08-24, 사용자 결정)**: B(8/21 오전)·F·H(8/21)와 **동일 기준**으로 prod `TRADING_LIVE_STRATEGIES`에서 제거(8개 → **7개**). 근거는 `feature-mining` 진입-대조군 edge를 국면별로 분해한 것 — **세 국면 전부 음수**(BULL −0.42 / NEUTRAL −0.93 / BEAR −1.12)이고 **대조군 표본이 F·H보다 크다**(n=1,809 / 1,072 / 1,910). 즉 '국면이 안 맞았다'가 아니라 **선택 자체가 가치를 파괴**하며 임계 튜닝으로 회수될 성격이 아니다. `control-analysis`에서도 같은 방향 — E가 거른 `NOT_BREAKOUT`(n=5,995) **−0.78%** > E 진입분 **−1.33%**. 게이트도 차단 중이라(KOSPI −0.33% / KOSDAQ −0.32% fallback 미달) **즉각적 실동작 변화는 없고 이중 차단**. 섀도우·대조군 기록은 유지 → 재편입 판단 근거는 계속 축적.
  - **F 분봉신선도 필터 원복(2026-08-14)**: F 보완필터 ①(`ma-trend-require-fresh`, 코드 기본 false)이 prod에서 on이었으나 **forward 검증이 반대 결론에 도달** — 차단분 `NOT_FRESH` **+0.47%(n=382)** vs 진입분 **−1.35%(n=835)**(close, **거래일 9일 중 7일 양수**로 단일일 클러스터 아님). 신선도 요구가 더 나은 쪽을 버리고 있어 **prod env를 false로 원복**. `NOT_FRESH`가 `tracksControl` 무관 강제 대조군 기록 대상이라 이 판정이 가능했던 사례(강제 기록의 효용 실증). env `SIGNAL_MA_TREND_REQUIRE_FRESH`.
  - **⚠️ F·H LIVE 화이트리스트 제외(2026-08-21, 사용자 결정)**: B 제외(같은 날 오전)와 **동일 기준·더 강한 근거**로 두 전략을 prod `TRADING_LIVE_STRATEGIES`에서 제거 (10개 → **8개**). 근거는 `feature-mining` 진입-대조군 edge(40일, close, `clustered=false`, 대조군 커버리지 100%)를 **국면별로 분해**한 것 — **F edge −1.31%p**(진입 net −1.41% vs 대조군 −0.10%, n=719/26거래일, 대조군 n≈652) · **H edge −0.86%p**(−1.05% vs −0.19%, n=591/22거래일, 대조군 n≈508). 결정적인 건 **세 국면 전부 음수**라는 점이다(F: BULL −1.45 / NEUTRAL −1.03 / BEAR −0.68, H: −1.26 / −0.71 / −0.36) — 즉 "국면이 안 맞았다"가 아니라 **선택 자체가 가치를 파괴**하며, 임계 튜닝으로 회수될 성격이 아니다. F는 제외 시점 B(−0.56%p)보다 나쁘고 8월 LIVE 실적도 **8건 승률 0%·−37,750원**이었다. 게이트도 차단 중이라 **즉각적 실동작 변화는 없고 이중 차단**(F·H 둘 다 국면표본부족+fallback미달로 닫혀 있었다). 섀도우·대조군 기록은 유지 → 재편입 판단 근거는 계속 축적. ⚠️ **같은 분해에서 K는 정반대 처방이 나온다** — K 풀링 edge는 −1.47%p지만 **BULL +1.76(n=243) / NEUTRAL −4.73(n=128)**로 갈리는 Simpson 역설이라, K는 제외가 아니라 **국면 제한**이 답이다(미적용, 판단 대기). 즉 **풀링 edge만 보고 제외를 결정하지 말고 반드시 국면 분해를 볼 것**. ⚠️ 반대로 J(+1.40)·G(+0.70)의 양수 edge는 **대조군 n이 20·32뿐**이라 근거가 약하다(D는 대조군 n≈3,987로 시스템 내 최강). edge를 인용할 땐 진입 n이 아니라 **대조군 n**을 함께 볼 것.
  - **F·H 흐름↓ 스킵(2026-08-25, 사용자 결정)**: A(8/24)·G·J(8/21)에 적용한 것과 **같은 기준**(lag30·lag60 양쪽 일관 + 표본 충분)을 추세 계열 둘도 통과했다 — F 흐름↓ **−0.95%(n=387)/−0.95%(n=388)** vs 흐름↑ −0.47/−0.46(n=572), H 흐름↓ **−0.78%(n=314)/−0.77%(n=315)** vs 흐름↑ −0.38/−0.35(n=428/427). 지수가 내리는 중의 MA20 돌파·NR7 수축 돌파는 대체로 **가짜 돌파**로 끝난다. `SIGNAL_F_REQUIRE_RISING_FLOW`/`SIGNAL_H_REQUIRE_RISING_FLOW`(코드 기본 false, prod만 true, compose 패스스루). ⚠️ **판정 위치는 마지막 게이트** — 전략 고유 사유(NO_CROSS/WEAK_BREAKOUT/NOT_FRESH, NO_SQUEEZE/NOT_CONFIRMED) 뒤에 둬야 `FLOW_DOWN` 대조군이 ENTERED와 직접 비교된다(G·J·D와 동일 설계). 차단분은 `FLOW_DOWN`이 이미 강제 대조군 기록 대상이라 자동으로 남는다. ⚠️ **둘 다 LIVE 제외 상태라 즉시 손익 효과는 0** — 재편입 판단용 섀도우 품질 개선이 목적이다. ⚠️ 기대효과는 흑자 전환이 아니라 **출혈 감소**(흐름↑만 남겨도 F −0.47%·H −0.38%로 음수). ⚠️ 진입 조건이 바뀌므로 `TRADING_PERF_GATE_STRATEGY_SINCE`에 **F를 20260817→20260826으로 갱신, H를 20260826으로 신규 추가**. **`GateRefilter`를 쓰지 않은 이유**: `mom30`은 persisted라 재필터가 가능해 보이지만 `Cond.test`가 **null을 보수적으로 제외**하는 반면 라이브 필터는 **null을 통과**시킨다(degrade open) → 개장 ~30분 진입분이 게이트 풀에서만 빠져 **라이브와 채점 모집단이 어긋난다**. F·H는 LIVE 제외라 게이트 정지 비용이 0이므로 since가 더 정확하다. 테스트 4.
  - 수동: `POST /api/v1/admin/scan-market?limit=N`.
  - **티어드 스캔**(`HotWatchService`+`SignalScheduler.scanHot`, `stockadvisor.tiered-scan`, 2026-07-09): 전수 스캔(12분)은 급변을 최대 12분 늦게 포착 → 위에 **핫셋(곧 진입근접 소집합)을 2분마다**(hot-scan-cron) 별도 스캔. **핫셋 = 직전 전수스캔 volumeRatio 상위 N(50) ∪ 볼륨무관 이벤트플래그(maCrossUp/rsiCrossUp/squeezeBreakout=F/G/H/J) ∪ 인버스**. 전수 스캔이 `record(code,volumeRatio,eventTriggered)`(볼륨게이트 전, breadth와 같은 훅, KIS콜 0)로 수집→`publish()`로 핫셋 확정, `scanCodes()`가 핫셋만 같은 evaluateStock 평가(중복방지 그대로). breadth/hotWatch는 전수 스캔만 begin/publish(핫 스캔은 record no-op). `pool.size` 3→4. env `TIERED_SCAN_ENABLED`/`HOT_SCAN_CRON`/`TIERED_SCAN_HOT_SET_SIZE`/`SCHEDULER_POOL_SIZE`(compose 패스스루). ⚠️ 핫셋 staleness ≤12분(신규 급등은 다음 전수스캔 전엔 못 듦). 테스트 `HotWatchServiceTest`.
  - **K 개장갭(순수 시초가, `OpeningGapStrategy`, 섀도우, 2026-07-10)**: "어제까지 국면 + 오늘 시초가 갭"으로 **개장 창(09:00~`opening-gap-window-end` 09:30)**에 판단(ORB와 달리 레인지 대기 없음). 갭=`SignalResult.gapPct`((오늘시가−전일종가)/전일종가, `MarketSignalService`가 일봉 신규필드 `openPrice`=`stck_oprc`로 계산). `rejectReason`: `OUT_OF_WINDOW`/`NO_GAP`(<min-gap 2%)/`GAP_TOO_BIG`(>max 10%)/`REGIME_BEAR`(약세장 갭업 제외)/`FADING`(changeRate<gap=현재가<시가)/`SCORE`. 어제국면은 `StrategyContext.entryTrend`(신규, StrategyEvaluator가 regime 태깅해 실음, compat 생성자로 기존 전략테스트 무churn). `requiresVolumeSpike=false`·`preScreen`=개장창+갭업후보만. ⚠️ **폴링 구조상 "시초가 정확 체결"은 아님**(첫 스캔 ~09:00~07 현재가=시가 근접). ⚠️ 갭업 모멘텀이라 약세장 실패 잦음 → REGIME_BEAR 필터+섀도우. env `SIGNAL_OPENING_GAP_*`. 단위테스트 `OpeningGapStrategyTest`(시간주입형 `reject(ctx,now)`). **⚠️ 어제국면 = 전일 확정 라벨(2026-08-13 변경)**: K의 유효 창(09:00~09:30)이 하필 **장중 라벨이 가장 불안정한 구간**과 겹쳐, 장중 라벨로 버킷을 잡으면 표본 풀이 계속 바뀐다(실측: 09:10 KOSDAQ 중립 n=10 차단 ↔ 마감 강세 n=77 통과). → `trading.prior-day-regime-strategies`(csv, 기본 `OPENING_GAP_K`)에 지정된 전략은 **`MarketRegimeService.priorDayTrendOf`(오늘 부분봉 제외)로 판단·태깅·게이트 버킷팅**. K의 정체성("어제까지 국면 + 오늘 시초가 갭")과도 정합. ⚠️ **`StrategyEvaluator`(태깅)와 `StrategyPerformanceGate`(버킷)가 같은 설정을 읽어야 함** — 한쪽만 바꾸면 게이트가 엉뚱한 표본 풀과 비교. ⚠️ **태깅 의미가 바뀌므로 구표본(장중 라벨 태깅)과 신표본이 섞임** — 필요 시 `TRADING_PERF_GATE_STRATEGY_SINCE=OPENING_GAP_K:<배포일>`로 재검증 시작일 지정 검토. env `TRADING_PRIOR_DAY_REGIME_STRATEGIES`(compose 패스스루). 테스트 `전일국면_전략은_장중라벨이_아니라_전일확정라벨로_버킷팅된다`. **⚠️ K 중립국면 제외 — BULL 한정(2026-08-21, 사용자 결정)**: K는 8월 LIVE 최대 손실원이었다(**−137,129원, 17건, 승률 11.8%** — D −58,294·F −37,750보다 큼). 원인을 `feature-mining` 국면 분해로 특정: K의 풀링 edge는 −1.47%p지만 이건 **Simpson 역설**이고, 실제로는 **BULL +1.76%p(진입 −0.24% vs 대조군 −2.00%, n=243, 14거래일)** ↔ **NEUTRAL −4.73%p(진입 −1.35% vs 대조군 **+3.38%**, n=128, 12거래일)**로 정반대다. 즉 중립국면의 K는 **고른 종목이 거른 종목보다 4.7%p 나쁜** 구간 = 약세장과 같은 이유로 제외 대상. → v1의 하드코딩 `REGIME_BEAR`를 **csv `opening-gap-allowed-regimes`(코드 기본 `BULL,NEUTRAL` = 종전 동작)**로 전환하고 **prod만 `BULL`**로 좁혔다(원복하려면 env에 `BULL,NEUTRAL`). 사유는 `REGIME_` + 국면(`REGIME_BEAR`는 기존 통계 유지, 신규는 `REGIME_NEUTRAL`)이라 둘을 분해해 볼 수 있다. ⚠️ **국면 미상(null)은 통과**(degrade open — 국면 산출 실패로 매매를 막지 않는다. 실측 미상 표본 n=13/+0.62%로 열위 근거도 없음). **`REGIME_NEUTRAL` 차단분은 `tracksControl` 무관 강제 대조군 기록**(K는 control-off라 흔적이 없으면 "중립을 막은 게 옳았나"를 forward 검증할 표본이 안 쌓인다 — INDEX_GAP_DAY와 동일 취지). `REGIME_BEAR`은 종전대로 미기록. ⚠️ 진입 조건이 바뀌므로 **`TRADING_PERF_GATE_STRATEGY_SINCE`의 K를 `20260824`(다음 거래일)로 갱신**했다(20260819 → 20260824). ⚠️ **이 결정의 일반 교훈: 풀링 edge만 보고 전략을 제외하지 말 것** — 같은 날 F·H는 세 국면 전부 음수라 제외가 답이었지만 K는 국면 분해에서 부호가 갈려 **제외가 아니라 국면 제한**이 답이었다. env `SIGNAL_OPENING_GAP_ALLOWED_REGIMES`(compose 패스스루). 테스트 2(허용국면 밖 국면별 사유/미상 degrade open·빈 목록 무제약). **⚠️ 지수 통째 갭업일 제외(2026-08-14, K 7건 −120,050원 계기)**: 지수 자체가 갭업한 날의 종목 갭업은 **종목 고유 촉매가 아니라 시장 갭의 반영**이라, 갭이 되돌려지면 종목선택과 무관하게 전부 동반 하락한다(실측 8/14: KOSPI +2.6%·KOSDAQ +1.6~2.0% 갭업 개장 → K 7건이 09:01~09:06에 진입 → 코스닥이 +1.6%→−1.3%로 3%p 반전하며 **4건이 손절선(−5.3%) 직행**). → 해당 시장 지수 갭 ≥ `opening-gap-max-index-gap`(**기본 1.5%**)면 `INDEX_GAP_DAY`로 진입 보류. 지수 갭은 `MarketRegimeService.indexGapPct(market)` — 프록시 일봉의 (오늘시가−전일종가)/전일종가를 **시장별 당일 1회 캐시**(1콜/시장/일. ⚠️ 국면 캐시 60분 TTL을 재사용하면 08:40 배치 시점 갱신분엔 '오늘 행'이 없어 K 창에서 미상이 되므로 별도 캐시). `StrategyContext.indexGapPct`로 전달(compat 생성자 유지). 판정 `indexGapDay`(순수 정적), 미상이면 degrade open. **차단분은 `tracksControl` 무관 강제 대조군 기록**(K는 control-off + 지수갭업일 자체가 드물어, 강제 기록 없이는 "차단이 옳았나"를 검증할 표본이 안 쌓임 — REBOUND_DAY와 동일 취지). env `SIGNAL_OPENING_GAP_MAX_INDEX_GAP`(0=비활성, compose 패스스루). 테스트 3 + `MarketRegimeServiceTest.지수_갭_계산`. **+ 갭 feature 태깅(같은 날)**: K는 정체성이 갭 전략인데 갭 크기가 저장되지 않아 "갭 3%와 갭 9% 중 뭐가 이기나"(=갭 상한 튜닝의 전제)를 사후에 물을 수 없었음 → `TradeOutcome.entry_gap_pct`(종목 갭%)·`entry_index_gap_pct`(지수 갭%) 신규 태깅(`SignalResult.gapPct`·위 캐시 재사용이라 **추가 KIS 0**, nullable→ddl-auto 안전, forward-only). `outcome-analysis` numericFeatures("종목갭%"/"지수갭%")·`feature-mining` 축에 노출.
  - **L 눌림 반전**(`ReversalStrategy`, 섀도우, 2026-08-21): 기존 10개 전략이 사는 것의 **거울상** — **20일선 아래 눌림(이격 −3~−10%) + 거래량 미급증(volumeRatio < 1.0) + 최근 5일 약세(ret5d ≤ −5%)**를 산다. `requiresVolumeSpike=false`가 **정체성**(볼륨 게이트를 통과하면 전제가 깨진다). `rejectReason`: `VOLUME_UP`/`NOT_PULLBACK`/`BROKEN_TREND`/`NOT_WEAK`/`CHASING`(당일 >2%)/`TOO_DEEP`(당일 <−5%)/`SCORE`. **왜 만들었나 — 네 갈래 독립 증거가 이 시장의 단기 반전 우세를 가리킨다**: ① 유니버스 횡단면(전 종목 분모) 급증 lift **−0.20%p**(4거래일 전부 음수, 지수 +6.17% 오른 날에도 −0.01로 이득 없음) + MA20 아래·저거래량·ret5d 낮음이 양의 lift ② 거래량배수와 수익의 단조 역방향(2~4 −0.78 · 8~15 −1.42 · ≥15 −2.65%) ③ B의 `WEAK_VOLUME` 대조군 +0.33% > 진입 −0.50% ④ 전역 필터 3종(등락률 ≥10%·거래량 ≥15배·초소형주)이 전부 edge 음수. ⚠️ **ATR(저변동성) 조건은 일부러 뺐다** — 유니버스에서 저ATR lift가 폭락일에 최대(+0.61)라 **저베타 아티팩트 혐의**가 짙고, 근거 ②③④는 40거래일 표본이라 그쪽에 무게를 뒀다(4거래일 lift에 과적합 회피). ⚠️ **`tracksControl()=false`인데 대조군이 필요 없다** — `UniverseSnapshot`이 이미 전 종목 분모라 `/universe-analysis`로 같은 조건 구간의 base rate와 직접 비교된다(발굴 세션 교훈: 반사실은 lift가 아니라 분모/대조군으로만 잰다). ⚠️ **결과가 어느 쪽이든 정보가 된다** — 이기면 파이프라인의 스크리닝 방향 자체를 재검토해야 하고, 지면 위 네 증거가 '무조건 매수 평균'의 착시였다는 뜻이라 그것대로 결론이 선다. **⚠️ L LIVE 편입 + 부트스트랩(2026-08-25, 사용자 결정) — 위 '실주문 0' 서술은 더 이상 prod에 해당 안 됨**: prod `TRADING_LIVE_STRATEGIES`에 `REVERSAL_L` 추가(6개 → **7개**) + `TRADING_PERF_GATE_BOOTSTRAP_STRATEGIES`에 추가(`VALUE_REVERSAL_J,RSI_REVERSAL_G,REVERSAL_L`) → **축소사이징 ×0.5(종목당 ≈2.5%)로 실주문 시작**. 배포 직후 게이트가 KOSPI 13/30·KOSDAQ 7/30 부트스트랩으로 열림 확인. 계기는 2026-08-25 실측 — 그날 LIVE 대상 7개는 진입 **9건(그중 7건 인버스)·net −3.07%**인데 LIVE 제외군(B/E/F/H/L)이 **170건·net +2.04%**였고, 그 대부분이 L(84건 net +3.43%)과 F(66건 +0.67%)였다. ⚠️ **근거는 거래일 2일치뿐이다**(8/24 +0.03% · 8/25 +3.43%). 둘 다 눌림 매수에 유리한 날이었고 **BULL일 표본이 없다** — J·G 부트스트랩과 같은 성격의 "기대손실을 내며 실표본을 사는" 결정이며, 근거는 그 둘보다도 얇다. ⚠️ **전략별 포지션 상한이 이 결정의 전제**다(아래 항목) — L은 하루 84~89건을 내는데 슬롯이 선착순이라 상한 없이 켜면 D·G·J·K를 전부 밀어내고 계좌를 독점한다. `TRADING_MAX_POSITIONS_PER_STRATEGY=REVERSAL_L:5`로 제한. ⚠️ 상한은 **동시 보유** 기준이라 90분 청산 후 슬롯이 비면 다시 채워진다(연속매매 6h20m ÷ 90분 ≈ 하루 최대 4회전 ≈ 20건). 멱등키가 `전략:종목:일자`라 같은 종목 당일 재진입은 없어 회전마다 새 종목으로 간다. ⚠️ L은 승자 MFE가 4.18%뿐이라 **왕복비용 0.44%가 기대이익의 ~11%**를 먹는다(진입 건수가 많을수록 비용 누적이 커진다). 판정 조건: **거래일 10일 + 상승일/하락일 양쪽 표본**이 쌓일 때까지는 부트스트랩 유지, 표본 30 도달 시 엄격 경로로 자동 졸업. `alerts()=false` + 코드 기본값은 여전히 화이트리스트 미포함(실주문 0). env `SIGNAL_REVERSAL_*`(compose 패스스루). 단위테스트 `ReversalStrategyTest` 7. **⚠️ 대조군 정책 전환(2026-08-26) — 위 "대조군이 필요 없다" 서술은 LIVE 편입으로 무효**: 도입 근거는 "`UniverseSnapshot`이 전 종목 분모니 `/universe-analysis`로 base rate와 비교하면 된다"였는데, **LIVE 편입으로 물어야 할 질문이 바뀌었다** — 유니버스 lift는 *"MA20 아래가 좋은가"*를 재지만 지금 필요한 건 **"L이 고른 MA20 아래가 L이 거른 MA20 아래보다 나은가"**이고, 후자는 **대조군으로만** 잰다(발굴 세션 교훈: lift는 반사실이 아니다). 대조군이 없으면 **B·C·E·F·H를 제외할 때 쓴 것과 같은 기준으로 L을 판정할 수단이 없다**. → `tracksControl()`은 **false로 유지**하고 `StrategyEvaluator.reversalControl`(순수 정적)이 **L의 판별 사유만** 강제 기록: `NOT_WEAK`·`CHASING`·`TOO_DEEP`·`BROKEN_TREND`·`SCORE`. ⚠️ **정체성 사유 `VOLUME_UP`은 일부러 뺐다** — L은 `requiresVolumeSpike=false`라 **급증 종목 전부**에 대해서도 평가돼 거기서 `VOLUME_UP`으로 탈락하는데, 그 행까지 남기면 대조군이 **하루 ~700행** 늘면서 정작 비교로는 무의미하다("거래량이 급증해서 안 샀다"는 L의 후보 풀 자체가 아니고, 그 질문엔 `universe-analysis` 급증 lift와 B의 `WEAK_VOLUME`이 이미 답했다). `NOT_PULLBACK`도 preScreen 경계라 급증 종목에서만 발생해 같은 이유로 제외. 남긴 사유는 **L의 후보 풀(눌림+저거래량) 안에서 L이 실제로 선별에 쓴 축**뿐이라 ENTERED와 직접 비교된다(C의 `DROP_RANGE`/`NO_REBOUND`가 C 제외 판단 근거가 된 것과 같은 구조). 예상 부하 하루 ~150~250행. ⚠️ **전략명을 함께 보는 게 핵심** — `SCORE`·`TOO_DEEP`은 여러 전략이 쓰는 공용 사유라 사유 문자열만 매칭하면 **전 전략의 대조군 정책이 조용히 바뀐다**. 테스트 `ReversalControlTest` 4. **⚠️ 2026-08-26 실측 — L의 흑자 기록은 8/25 하루가 전부**: `control-analysis` ENTERED **+1.44%(n=194, 승률 65.5%)** → 최대기여일 제외 **−0.08%**, 거래일 **3일**(8/24 +0.25 n=89 · **8/25 +3.89 n=84** · 8/26 +0.85 n=21, close gross), `clustered=true`. 8/25를 빼면 +0.36% gross ≈ **+0.14% net = 왕복비용 언저리**. 즉 8/25 편입 근거(2거래일)가 하루 더 쌓이자 이미 흔들린다 — **판정 조건(거래일 10일 + 상승일/하락일 양쪽 표본)이 충족될 때까지 현행 유지**가 맞고, 3일로 뒤집을 근거도 없다.
  - **인버스 ETF(하락장 수익, 방식A — 공매도 없이 하락 베팅)**: `stockadvisor.inverse-codes`(기본 114800 KODEX인버스·251340 코스닥150선물인버스)를 워치리스트에 두면, **하락장에 인버스가 급등할 때 모멘텀 전략(B·E)이 롱으로 자동 포착**. `StrategyEvaluator`가 인버스 코드는 ① **SELL/점수게이트 면제**(ETF는 PER/PBR 무의미 → recScore 100·BUY 강제) ② **독립 국면버킷 `entry_market="INVERSE"`**(일반주와 안 섞여 수익 희석 방지, 국면조건부 미적용=전체 인버스 표본 풀링) ③ 지수상대(D) 대상 아님(marketChange=null). `WatchlistSyncService` reconcile이 인버스 코드를 **항상 유지**(시총선정 무관 삭제 방지). ⚠️ **v1 서술은 섀도우 검증만(코드 기본값 기준)** — ⚠️ **현행 prod는 I가 화이트리스트에 편입돼 있고 INVERSE 부트스트랩(×0.3 축소사이징)으로 실주문 경로 활성**(2026-07-31 실측, perf-gate INVERSE 버킷이 실현손익으로 판정. `strategy-gate`에 INVERSE 버킷 노출). **인버스 재진입(2026-07-14)**: dedup 하루 1회 때문에 오전 진입→청산 후 오후 재붕괴를 놓치는 문제(실측 7/14: 10:14 청산 직후 코스닥 −6% 재붕괴 미포착) → 인버스는 **마지막 진입 후 `inverse-reentry-minutes`(20분) 경과 + 활성 실포지션 0**이면 같은 날 재평가 허용. 재진입은 승격이 아니라 **새 표본 행**(기존 사이클 보존), 주문 멱등키는 사이클 접미사(`전략:종목:일자#2`)로 첫 사이클(FILLED)과 충돌 없음(부분 유니크 정합). 판정은 `StrategyEvaluator.inverseReentryEligible`(정적, 테스트 7). 0=비활성(기존 하루 1회). env `SIGNAL_INVERSE_REENTRY_MINUTES`. **+ 하루 사이클 상한(2026-08-18)**: 왕복마다 비용 0.22%를 내므로 사이클이 늘면 방향이 맞아도 진다(8/18 251340이 **10차**까지 재진입). → `inverse-max-reentries-per-day`(기본 3, 0=무제한)로 당일 진입 사이클을 제한(대조군 행은 카운트 제외 — control→entry 승격 경로를 막지 않기 위해). ⚠️ 주된 처방은 위 ③ 청산 수정이고 이건 **안전망** — 정상 동작 시 상한에 걸릴 일은 드물다. env `SIGNAL_INVERSE_MAX_REENTRIES_PER_DAY`(compose 패스스루, 기본 3). ⚠️ **prod는 도입 후에도 `.env`에 0(무제한)이 명시돼 있어 상한이 비활성이었다** — 8/20 251340이 **11차**까지 재진입해 7사이클 −3,711원(전패, 보유 0~2분)을 냈다. 2026-08-20 prod `.env`를 **3으로 변경**(코드 기본값과 일치). ⚠️ 교훈: compose 패스스루에 기본값을 넣어도 **`.env`에 키가 있으면 그 값이 이긴다** — 새 knob 도입 시 prod `.env`의 기존 줄을 반드시 확인할 것. ⚠️ 섀도우-only 상황에선 조건 지속 시 쿨다운마다 새 표본이 쌓임(폭락일 한정이라 수용). **I 반등일 fade 확대(2026-07-22, 사용자 결정)**: REBOUND_DAY 성립일(당일 **고점** ≥ surge% AND 기저 비강세)엔 진입 상한을 −1% → **+`inverse-rebound-fade-max-pct`(2%)**로 확대 — "갭업이 무너지는 날"의 fade 구간(+2%~−1%)을 포착(7/22 아침 +2.4%→종가 붕괴 실측 계기). 평상일 상한 불변(강세장 눌림 휩쏘 회피 — 전면 확대·고점반전 전략화는 사용자가 기각). **반등일 판정은 "당일 장중 고점" 기준으로 개선**(`MarketRegimeService.dayHighChangeOf` 시장별 고점 추적, 날짜 리셋) — 순간 등락률 기준이면 fade로 +2% 아래에 내려온 순간 판정이 풀리는 모순. 이 개선으로 **순추세 REBOUND_DAY 가드도 하루 종일 유지**(오전 +2.4% 찍고 오후 +1%여도 보류). env `SIGNAL_INVERSE_REBOUND_FADE_MAX_PCT`(0=비활성, compose 패스스루). **⚠️ fade 확인 요건(2026-08-20, 11차 재진입 −3,711원 계기)**: 확대창을 열어두기만 하면 **진입 허용구간과 청산 트리거가 통째로 겹친다** — 진입 상한 **+2.0%** vs 인버스 청산 `지수 > −recovery-pct(−0.5%)` → 구간 `(−0.5%, +2.0%]`은 **사는 즉시 파는** 왕복비용 루프다. 8/20 실측: 코스닥이 하루 종일 +1.66~2.33%였는데 251340이 **11차**까지 재진입, 청산 사유 전부 `지수회복(+1.66~+2.33% > −0.5%)`, 보유 0~2분, 7전패. (8/19 배포한 `rebound-from-low`는 *모멘텀 반등* 분기용이라 여기서 발동한 *레벨 회복* 분기엔 무효였다.) → 확대창을 **fade가 실제 진행 중일 때만** 연다: **당일 지수 고점 − 현재 ≥ `inverse-fade-confirm-pct`(1.0%p)**, 미달이면 `NOT_FADING` 보류. 8/20 케이스는 고점 +2.4% vs 진입 +1.84% = 0.56%p차라 차단됐을 것. ⚠️ **판정 위치가 핵심** — 확대창에 기대야만 통과할 후보(`chg > −minDrop`)에만 적용해, 진짜 약세(`chg ≤ −minDrop`)는 fade 미확인이어도 그대로 진입한다(회귀 방지). 고점은 판정에 쓰는 `chg`(`fetchIndexChangeRate`, 60s 캐시)를 지수코드별로 누적해 전략이 직접 추적한다(추가 KIS 0). `MarketRegimeService.dayHighChangeOf`도 **같은 소스**지만 private + 시장 키라 재사용하지 않았을 뿐, 두 추적기가 보는 값은 같다. 차단분은 `tracksControl` 무관 **강제 대조군 기록**(반등일×인버스 한정이라 부담 작음). env `SIGNAL_INVERSE_FADE_CONFIRM_PCT`(0=비활성=종전 동작, compose 패스스루). 테스트 5(fade 허용/고점근처 NOT_FADING/진짜약세 통과/비활성 종전/평상일 불변). **⚠️ fade 확인만으론 부족했다 — 진입-청산 겹침 직접 차단(2026-08-24, 251340 3사이클 전패 계기)**: 8/24 실측에서 251340이 **3사이클 모두 진입 1~2분 만에 `지수회복(+1.05~1.45% > −0.5%)`으로 청산**돼 전패(−913원)했다. fade 확인이 이걸 못 막은 이유는 **fade 확인은 '고점 대비 거리'를, 청산은 '절대 레벨'을 보기 때문에 둘이 동시에 참일 수 있어서**다(그날 KOSDAQ 고점 +2.37% / 진입 +1.05~1.45% → fade 0.9~1.3%p로 확인 통과). 즉 8/20 노트가 지적한 겹침 구간 `(−0.5%, +2.0%]`이 여전히 열려 있었다. → **진입 시점에 레벨-청산이 이미 참이면 진입 거부(`EXIT_ACTIVE`)** — 판정 `InverseIndexStrategy.exitAlreadyActive`(순수), 임계는 **청산과 같은 프로퍼티**(`trading.inverse-exit.recovery-pct`)를 읽어 두 규칙이 갈라지지 않게 한다. ⚠️ **판정 위치는 마지막 게이트** — 전략 고유 조건을 다 통과한 후보에만 걸어야 `EXIT_ACTIVE` 대조군이 ENTERED와 직접 비교된다(FLOW_DOWN과 동일 설계). 차단분은 `tracksControl` 무관 **강제 대조군 기록**(NOT_FADING과 동일 취지). ⚠️ **평상일엔 사실상 no-op**(평상 상한 −minDrop(−1%)이 이미 −recovery(−0.5%)보다 낮아 통과분이 자동 충족) — 실질 효과는 **7/22 반등일 fade 확대창을 `[−maxDrop, −recovery]`로 좁히는 것**이라 그 확대의 유효 폭이 크게 준다. 다만 그 확대창은 애초에 청산 규칙과 **논리적으로 양립 불가**였다(열어둬도 다음 틱에 팔린다). ⚠️ 8/24 피해가 −913원에 그친 건 `SIGNAL_INVERSE_MAX_REENTRIES_PER_DAY=3`이 실제로 물렸기 때문 — **안전망은 작동했고 원인은 그때 안 고쳐져 있었다**. env `SIGNAL_INVERSE_BLOCK_WHEN_EXIT_ACTIVE`(false=종전 동작, compose 패스스루). 테스트 4(EXIT_ACTIVE 차단/청산선 아래 fade 진입 허용/비활성 종전/경계 부등호). **I 낙폭 상한(2026-07-20)**: 지수 당일 ≤ −`inverse-index-max-drop`(기본 4%)이면 `INDEX_TOO_DEEP` 진입 보류 — 7/20 실측 "지수 −4% 진행 후 진입" 4건 전패(−1.0~−1.3%, 인버스가 이미 +5% 오른 고점매수라 되돌림 한 번에 청산). **폭락 초입 진입만 허용**(C의 max-drop과 같은 사상). 기보유분 청산엔 무영향. env `SIGNAL_INVERSE_INDEX_MAX_DROP`(0=비활성, compose 패스스루). 테스트 `지수_낙폭_과대면_INDEX_TOO_DEEP`. **I 지수 흐름필터(2026-07-14, 7/16 강화)**: 당일 지수 −1%여도 회복 중이면 `INDEX_RECOVERING`으로 진입 보류 — 인버스 고점매수(반등하는 칼날의 거울상) 회피. **7/16 강화: mom10·mom30 둘 다 ≤0(순간도, 지속도 하락/보합)이어야 진입** — 기존 mom10 우선 판정은 "30분 추세 회복(+) 중 순간 10분 음전"을 통과시켜 휩쏘 재진입(고점매수→반등 손절, 7/16 오후 3건 −4,350원) 반복. 청산(mom30 기준)과 대칭. `MarketRegimeService.intradayFlow` 재사용(추가 KIS 0), 흐름 미가용 시 생략(degrade open). env `SIGNAL_INVERSE_INDEX_REQUIRE_FALLING`. **PolicyGate 시간규칙 면제(2026-07-20)**: 적응형 보유시간(I 90분, OutcomeSample 기반)이 "시간청산 장마감 초과 → 진입금지" 규칙에 적용돼 **인버스 마감 전 진입(15:12 재진입 실측)이 오차단** — 인버스는 시간청산 미적용(지수조건+sessionEnd 15:20 백스톱=당일청산 보장)이라 전제 불성립 → `PolicyGate`가 `inverse-codes` 종목은 이 규칙 면제(스윙 면제와 동일 패턴). 마감 전 하락 가속(인버스 최적 구간) 진입 보존. 테스트 `인버스는_장마감_넘겨도_진입허용`. **서킷/노출 예외 처리 완료**: `OrderService.submitEntry`가 market="INVERSE"면 **리스크가드(서킷·노출상한) 건너뜀**(하락 급락이 인버스엔 기회 — per-order 사이징·max-positions는 유지). **일일손실한도도 인버스 면제(2026-07-24)**: 아침 H −81,078이 한도(5만)를 소진해 코스피 −5.9% 폭락일의 인버스 재진입 4회가 전부 거부된 실측 — 헤지 다리는 하락일에 "버는" 방향이라 무관 전략 손실로 커버리지를 끄는 건 취지 역행(`PolicyGate` 6번 규칙에서 inverse-codes 제외, 테스트 `인버스는_일일손실한도_면제` — 일반주 차단은 그대로 대조 검증). 노출은 부트스트랩 ×0.3 × 2코드 ≈ 3%로 자체 제한, `PositionExitService`는 리스크오프 강제청산에서 **인버스(stockCode∈inverse-codes) 면제**(승자 보유). **인버스 전용 청산(2026-07-14, `trading.inverse-exit`)**: 진입(지수 약세)과 대칭 — 시간청산·트레일링·적응형 방식(개별종목 표본 학습값) 미적용(실측 7/13: I 60분 시간청산이 +1.3%에 나가고 종가 +7.4%를 놓침), 대신 **지수 하락 지속 동안 시간 무관 보유 → 약세 소멸 시 청산**: ① 지수 당일 등락률 > −`recovery-pct`(0.5%, 진입 −1%보다 완화=히스테리시스) ② 지수 모멘텀(mom10/30) ≥ +`rebound-mom-pct`(0.3%, `intradayFlow` 재사용) ③ 장마감 15:20(다일 감쇠 — 스윙 미적용, swing 분기보다 앞) ④ `max-hold-minutes`(300) 백스톱. **손절도 지수 스케일 −`stop-pct`(2.0%)로 교체**(적응형 −5~7%는 지수 ETF에 도달 불가=무방비). **반등 판정 둔감화(2026-07-16, 과민 청산 실측 대응)**: −6.7% 폭락일에 mom10 +0.3% 데드캣 미세반등마다 청산→반등 종료 후 더 높은 가격 재매수 반복(5왕복 +495원 vs 단순보유 ~+5,300원) → ① 판정을 **mom30만**(mom10 단독 발사 제거, mom30 미산출(개장 30분 내)이면 판정 생략 — 레벨회복·손절·백스톱이 방어) ② 임계 **낙폭 비례 상향 thr=max(`rebound-mom-pct`, |당일등락|×`rebound-day-scale`(0.15))** — 폭락일(−6.7%)엔 ~1.0% 요구·약보합일(−1.5%)엔 0.3% 유지. env `TRADING_INVERSE_EXIT_REBOUND_DAY_SCALE`(0=비활성, compose 패스스루). 테스트 3(mom30 기준 청산/폭락일 임계상향 보유/mom10 스파이크 무시). **③ 저점 대비 레벨 회복 요건(2026-08-18, 7왕복 −4,521원 계기)**: ②의 낙폭 비례 상향이 **'지금 등락률'을 기준으로 삼는 탓에 정작 필요한 순간에 안 걸렸다** — 8/18 KOSDAQ 마감 −3.52%(장중저점 −4.23%)인 날, 청산 6건의 사유가 전부 `mom30 +0.31~+0.48%`였는데 그 시점 지수는 −2%대라 thr이 기저 0.30%에 머물렀다. 251340이 2,335→2,400(+2.8%)으로 오르는 내내 **7왕복 −4,521원**(방향은 맞고 왕복비용만 지불), 섀도우 인버스는 같은 날 +1.63%(11/13승). → 반등 청산에 **`inverse-exit.rebound-from-low-pct`(기본 1.0%p): 지수가 장중 저점 대비 그만큼 실제로 올라왔을 것**을 함께 요구한다(모멘텀 반전 AND 레벨 회복). `MarketRiskGuard`의 서킷 재개 판정(저점 대비 rebound-pct%p)과 같은 사상이고, `MarketRegimeService.dayLowChangeOf`(고점 추적의 대칭, 추가 KIS 0)를 쓴다. 판정 `PositionExitService.inverseReboundExit`(순수 정적) — 지수·저점 미상이거나 `dayLow > chg`(데이터 부정합)면 모멘텀만으로 degrade open. env `TRADING_INVERSE_EXIT_REBOUND_FROM_LOW_PCT`(0=비활성, compose 패스스루). 테스트 `InverseReboundExitTest` 5 + `PositionExitServiceTest`(저점대비 미회복이면 보유). ⚠️ 이 로직 세대 교체이므로 **`TRADING_PERF_GATE_INVERSE_REALIZED_SINCE`를 배포일로 갱신**해야 구로직 실현표본(현재 n=10·net −0.58%로 게이트를 닫아둔 바로 그 표본)이 신로직 채점을 막지 않는다. 상한가익절은 인버스 제외(±1x 무관, 등락률 조회 생략). env `TRADING_INVERSE_EXIT_*`(compose 패스스루). 단위테스트 4(약세지속 보유/레벨회복 청산/모멘텀반등 청산/전용 손절선). 단위테스트 `OrderServiceTest`(인버스 리스크가드 면제)·`PositionExitServiceTest`(인버스 리스크오프 면제). ⚠️ 파생ETF 계좌 신청 필요(선택확인서 — 사용자 신청 완료). ⚠️ 인버스·레버리지 ETF는 다일 감쇠 → 단타(당일청산)로 한정. env `INVERSE_CODES`.
- 진입 전략마다 `TradeOutcome(strategy, disclosureId, buyPrice)` 기록(중복방지 unique). **A·B·C 모두 실시간 Discord 발송**(`alerts()=true`), 메시지에 `🧭 알고리즘: {label}`로 어느 전략이 추천했는지 표시(`TradingStrategy.label()`). **+ 실주문 접수 시 신호 알림 강제(2026-07-24)**: 섀도우 전략(alerts=false)이 게이트 승격으로 LIVE 주문을 내면 "접수 알림만 오고 신호 알림이 없는" 불일치(H 실측) → `alerts() OR 주문 SUBMITTED/PENDING_APPROVAL`이면 신호 알림 발송. 섀도우·DRY_RUN은 종전대로 조용.
- **매도(SELL) 의견이면 전 전략 진입/알림 제외**(`DisclosureAlertProcessor`의 글로벌 가드, minOpinionScore와 무관하게 하드 차단).
- **종목당 1회 제한**(`TradingStrategy.oncePerStockPerDay()`, A=true): 한 종목이 여러 공시를 동시에 내도 A는 하루 1회만 알림(`existsByStrategyAndStockCodeAndAlertDate`).
- 호출 절감: 세 전략 공통 전제가 `volumeSpike`라, 미급증 공시는 추천/분봉 호출 없이 조기 종료. 추천은 `computeRecommendation`(무저장)으로 평가 → 이력테이블 비대화 방지.
- horizon 수집: +5/+10/+30분(분봉 현재가) + 당일종가·**D+1/D+2/D+3 거래일 종가**(일봉 날짜매칭, 사후 확정). **D+N은 KIS 일봉의 실제 거래일(`businessDate`)만 카운트해 휴장(주말·공휴일)은 자동 제외**(`TradeFollowUpService.findClose(rows, alertDate, businessDayOffset)` — 0=당일, N=진입일 이후 N번째 거래일). 추적 만료 `MAX_TRACK`=8일(D+3가 연휴 끼면 ~7캘린더일 → 여유), 종료조건=D+3 수집 or 만료. C(역추세)는 당일 반등이 며칠에 걸쳐 진행될 수 있어 스윙 보유 가치 비교용. 리포트: `GET /api/v1/admin/strategy-report` → 전략별 horizon×표본수·평균수익률·승률.
- **뉴스 feature 태깅(2026-07-14, 측정 먼저)**: KIS 종목뉴스(`fetchNewsTitles`, 종합 시황/공시 제목 `FHKST01011800`, `/news-title` — 실호출 검증 완료)로 **진입 시점 뉴스 feature 태깅** — `TradeOutcome.entry_news_cnt_1h`(최근 1시간 건수)·`entry_news_age_min`(최신 경과분). `StrategyEvaluator`가 **진입 시에만 종목당 1콜 lazy**(대조군 미태깅 — 부하), 실패는 태깅 생략(진입 안 깨뜨림). feature 계산은 `KisNewsResponse.features`(순수함수, 테스트 3). `outcome-analysis` numericFeatures에 "뉴스1h건수/뉴스경과분" 노출 → **표본 누적 후 "뉴스 동반 진입이 이기나" 검증, 이기면 필터/신규 촉매 전략 승격 판단**. **+ `feature-mining` 뉴스 축 추가(2026-08-21)**: 승자/패자 분석에서 **10개 전략 중 8개가 같은 방향**으로 갈렸다 — 승자의 최신 뉴스가 더 최근(E 1,153 vs 2,204 / G 2,005 vs 3,697 / A 1,168 vs 3,027 / F 1,934 vs 2,439 / J 3,053 vs 4,823분). **전략별 조건이 아니라 전 전략 공통으로 나온 첫 신호**라 bin별 net을 보려고 `뉴스경과분` 축을 mining에 추가(경계 60/240/720/2880분 — 첫 경계 60분은 `fresh-news-window-minutes`와 같은 기준으로 맞춰 해석 정합). `뉴스1h건수`는 이미 있었음. 저장된 컬럼 계산만이라 **비용 0**. **+ `개장후경과분` 축(같은 날)**: `alertTime`에서 파생돼 **소급 계산**된다(신규 태깅 불필요 = forward-only 제약 없음). 경계 15/60/150/270분, 연속매매(09:00~15:30) 밖은 null로 제외(장전 공시 경로가 '<15' bin을 오염시키는 것 방지). 결과는 위 개장 창 노출 상한 항목 참조. ⚠️ **이 축은 진입군 랭킹 전용** — **대조군은 뉴스 태깅이 0%**(실측 30,979행 전부 null; 진입 시에만 종목당 1콜 lazy하는 설계)라 `edgeVsControlPct`가 **구조적으로 null**이다(커버리지 0%). 즉 "뉴스 있는 종목이 이긴다"를 **반사실로 검증하려면 대조군 뉴스 태깅이 선행**돼야 하는데, 대조군은 진입군의 6배(30,979 vs 4,902)라 종목당 1콜은 비현실적 → **후보: 진입 직전 후보(대조군 중 점수·게이트 근접분)만 부분 태깅**하거나, 뉴스 조회를 종목별 캐시로 묶어 스캔당 1회로 상각. ⚠️ 진입군 커버리지도 77.4%(3,792/4,902)라 나머지는 null bin으로 제외된다. **🔴 결과: 가설 반증(2026-08-21, 축 배포 직후 실측)** — "신선한 뉴스가 이긴다"는 **틀렸다. 방향이 반대다.** bin별 net(40일, close): **`<60분` −0.79%(n=1,397)** · 60~240 −0.70 · 240~720 −0.55 · **`720~2880` −0.31%(n=1,092, 승률 45.6%)** · ≥2880 −0.59 → **신선할수록 나쁘다**. `뉴스1h건수`도 단조 동일: `<1` −0.49 · `1~3` −0.69 · `≥3` −1.09%(n=335) → **뉴스가 많을수록 나쁘다**. ⚠️ **왜 반대로 봤나 — 왜곡된 분포에서 평균을 봤다**: 리포트가 인용한 건 전략별 승자/패자 **평균**(E 1,153 vs 2,204 등)인데, 뉴스경과분은 중앙 233~895분 vs p90 ~7,000분으로 **30배 꼬리**를 가진 분포라 평균이 꼬리 구성에 지배된다. 풀링해서 다시 보면 **평균조차 반대**(승자 2,420 vs 패자 2,248)이고 **중앙값은 승자 895 vs 패자 233으로 승자가 3.8배 오래됐다**(n=1,599/2,193). → **교훈: 왜도 큰 feature는 승자/패자 평균 비교로 판단하지 말 것**(중앙값·bin별 net으로 볼 것). `outcome-analysis`의 numericFeatures가 전부 평균 비교라 같은 함정이 다른 축에도 있을 수 있다. ⚠️ 여전히 `edgeVsControlPct`는 null(대조군 태깅 0%)이라 "뉴스가 나쁜 것"인지 "뉴스 나는 종목이 나쁜 것"인지는 미판정 — 필터로 승격하려면 대조군 태깅 선행 필요. 검증용 `GET /api/v1/admin/news?stockCode=`. **✅ 소급 가능함이 확인됨(2026-08-22)** — 위 "대조군 태깅이 선행돼야 한다"는 숙제의 해법이 **부분 태깅이 아니라 소급**으로 판명. 실측: ① `FID_INPUT_DATE_1`에 과거 날짜를 주면 **그날 뉴스가 온다**(20260721 지정 → 그날 40건) ② `FID_INPUT_HOUR_1`을 주면 **그 시각 이전 것만 온다**(093000 → 06:00~09:30) → **진입 시각 기준 뷰를 look-ahead 없이 정확히 재구성**할 수 있다(수급의 '직전 거래일' 규칙에 해당하는 안전장치가 여기선 이 시각 파라미터다). ⚠️ **1콜 40건 상한이 설계를 좌우한다** — 활발한 종목은 40건이 하루치도 안 된다(005930·000660은 40건 전부 8/21 하루, 005930은 09:35~11:47 2시간에 40건). 따라서 (종목,일자)당 1콜로 끝나지 않을 수 있어 **가장 늦은 진입시각으로 1콜 → 반환된 최古 항목이 그 쌍의 최早 진입시각보다 오래됐으면 그 1콜로 전 행 커버, 아니면 추가 콜**하는 적응형이 맞다. 규모: 고유 (종목,일자) 쌍 **7,271**(행 33,689 대비 4.6:1) → 낙관 시 ~15분, 행별 1콜 최악 시 ~70분. ⚠️ 소급하면 **진입군 커버리지 77.4%(3,792/4,902)도 함께 해소**된다 — 진입군과 대조군의 커버리지가 비대칭이면 비교 자체가 편향되므로 이건 부수효과가 아니라 요건이다. 검증 `GET /admin/news-probe?stockCode=&date=&hour=&srno=`(raw). **✅ 실행 완료(2026-08-22, `NewsBacktagService` + `POST /admin/backfill-news?lookbackDays=&force=`)** — 실측 **7,977쌍 / 7,982콜**(풀 재사용이 거의 완벽 = 쌍당 1콜), **34,268행 태깅, 실패 0, 17분**. **커버리지 진입 4,086/4,086 · 대조군 27,738/27,738 = 양쪽 100%**. ⚠️ 설계 요점 셋: ① (종목,일자) 쌍의 행을 **진입시각 내림차순**으로 처리하며 항목 풀을 재사용, 풀이 관심구간(진입−60분)을 못 덮을 때만 추가 호출(상한 6) ② **진입시각 이후 항목은 반드시 버린다** — `features()`가 미래 항목을 age 0(방금 나온 뉴스)으로 치므로 안 거르면 소급이 조용히 look-ahead가 된다 ③ `force=true` 기본으로 **기존 라이브 태깅분도 덮는다** — 진입군 77.4%/대조군 0%인 **비대칭 커버리지는 비교 자체를 편향**시키므로 양쪽을 같은 방법으로 맞추는 게 작업의 절반이다. `entry_news_basis_date` 신설(뉴스는 "없음"도 정상값 null이라 값만으로는 미태깅과 구분 불가). **🔴 결과 — 8/21 미판정("뉴스가 나쁜 것인가, 뉴스 나는 종목이 나쁜 것인가")의 답은 "둘 다이고 겹친다"**: **`뉴스1h건수`가 경과분보다 깨끗한 단조 축**이다 — 진입 net `<1` −0.64 / `1~3` −1.00 / `≥3` −1.40, **대조군도 같은 방향 단조**(−0.78/−0.89/−0.96 = **뉴스 나는 종목 자체가 나쁘다**), 그리고 **edge가 그 위에 추가로 단조 악화**(+0.14/−0.11/**−0.44** = **그 구간에서 우리 선택이 더 나쁘다**). 즉 base rate 열위와 선택 열위가 **더해진다**. 시간분할 통과: **뉴스 있는 두 구간(1~3·≥3)의 edge가 전·후반 4/4 셀 전부 음수**(−0.18/−0.12, −0.07/−0.67)이고 `<1`은 양쪽에서 최고(+0.19/0.00). ⚠️ **효과 크기는 불안정**하다(≥3 edge −0.12 ↔ −0.67) — 방향은 믿되 크기는 믿지 말 것. ⚠️ **절대 net은 전 구간 음수**라 이건 "이기는 조건"이 아니라 "덜 지는 조건"이다. → 필터 후보: "진입 직전 1시간 내 뉴스 ≥1건이면 보류"(진입의 ~38% 제외, 기대 개선 0.1~0.2%p) — **미적용, 판단 대기**. ⚠️ 기존 `FRESH_BAD_NEWS`(C/D/J·악재 키워드)와 다른 층위다(이건 전 전략·뉴스 유무). ⚠️ 이 결과 역시 **"이미 관심을 받은 것을 사면 진다"의 여섯 번째 독립 증거**(뉴스는 관심의 가장 직접적인 지표)이며 수급·급증 lift·거래량배수·`WEAK_VOLUME`·전역필터 3종과 같은 방향이다. **+ 체결강도 태깅(같은 날 추가)**: 주식현재가 체결(`fetchCcnl`, FHKST01010300)의 당일 체결강도(`tday_rltv`, 매수체결/매도체결×100)를 `entry_exec_strength`에 동일 lazy 패턴으로 태깅(진입만, 실패 격리) — "체결강도 높은 급등 vs 낮은 급등"이 갈리는지 측정, 이기면 웹소켓 실시간화 근거. outcome-analysis "체결강도%" 노출, 검증 `GET /admin/ccnl?stockCode=`(실호출 검증 완료). **⚠️ 대조군 커버리지 0% → 수집 지점 이동(2026-08-24)**: 소급 가능한 축들(뉴스·수급)이 8/22에 해결된 뒤 **체결강도만 대조군 0%로 남았다**(실측 진입 71.2% / 대조군 **0.0%**) — 진입군만 보면 `<100` −0.26 → `100~150` −0.85로 단조지만 **반사실이 없어 "체결강도가 나쁜 것인지, 체결강도 높은 종목이 나쁜 것인지"를 가를 수 없다**(뉴스 축이 8/21에 있던 그 상태). ⚠️ 그런데 `fetchCcnl`은 **당일치만** 줘서 **소급이 불가능한 유일한 축**이다 — 지금 안 모으면 그 기간은 영영 반사실을 못 만든다. → 두 단계로 처리: ① **무료 부분 해결** — `setEntryExecStrength`를 진입 분기 **앞**으로 이동해, 과열 가드가 이미 1콜을 한 후보(H/G/J)는 대조군이 돼도 태깅된다(추가 호출 0). 이것만으로도 **`EXEC_OVERHEAT` 가드가 실제로 판정하는 모집단**(다른 조건 다 통과·강도로만 갈린 후보)에 대조군이 생겨 "차단이 옳았나"를 직접 검증할 수 있다. ② **전 후보 수집** — `signal.collect-exec-strength-all`(코드 기본 false, **prod true**)로 수집을 유동성 필터·SELL 제외 **직후**(= 행이 될 수 있는 후보 전원)로 옮긴다. ⚠️ 이 위치가 요점 — 호가 조회 자리보다 뒤라 탈락분에 콜을 낭비하지 않으면서 대조군을 빠짐없이 덮고, `strengthFetched` 공유로 과열 가드·진입 태깅과 **중복 호출이 없다**. ⚠️ 대가는 **후보당 KIS 1콜 증가**다 — 스캔 부하가 문제되면 false로 되돌리면 종전의 진입-시-lazy 경로가 살아 있어 진입군 태깅은 유지된다(대조군만 다시 빈다). env `SIGNAL_COLLECT_EXEC_STRENGTH_ALL`(compose 패스스루). ⚠️ 종목 등록 뉴스에 시황성 잡음(순매수 상위 등) 포함 — 건수 feature에 노이즈 있음(측정 단계 수용, 승격 시 제목 필터 검토).
- **진입 feature 기록 + 승자/패자 분석**: `TradeOutcome`에 진입 시점 feature 저장(등락률·거래량배수·추천점수·PER·PBR·시장·시총·업종 — `StrategyEvaluator.recordEntryFeatures`). `GET /api/v1/admin/outcome-analysis?horizon=close|nextClose|d2|d3|p10|p30`(`OutcomeAnalysisService`) → 수익(>0)/손실 그룹으로 나눠 feature 평균 비교 + 시장·업종별 승률 + **국면별(`byMarketRegime`: 상승장/하락장) 승률·평균수익**(진입시점 해당시장 지수 등락률 `entry_market_change` 기준 — 전략이 상승장/하락장에서 통하는지 진단). "어떤 조건의 종목이 수익냈는지" 사후 진단용. 실데이터 며칠 누적 후 유효. (대조군은 제외 — `isControl()` 필터.)
- **대조군 추적**(`signal.control-tracking`, 기본 on — 선택 편향 제거/필터 검증): 거래량 급증은 했지만 **전략 조건/점수에서 탈락한 종목**도 `TradeOutcome(control=true, rejectReason)`로 기록 — **알림·주문·exit-mark·후속Discord 전부 없이 horizon 수익만** 수집(경량). 탈락 사유는 `TradingStrategy.rejectReason(ctx)`(전략별 분해: A `NOT_FRESH`/`SCORE`, B `DIRECTION_DOWN`/`ALREADY_UP`/`SCORE`, C `DROP_RANGE`/`NO_REBOUND`/`SCORE`; `shouldEnter`=`rejectReason==null`). **분석 `GET /api/v1/admin/control-analysis?horizon=`**(`ControlAnalysisService`) → 전략별 **진입(ENTERED) vs 미진입(REJECT:사유) net 평균·승률** 비교 + hint("미진입이 진입보다 수익 좋은 사유 있으면 필터 완화 검토"). **최종목표: 알림 안 나간 종목까지 분석해 더 수익 좋은 전략 발굴.** ⚠️ 대조군 표본만큼 추적 부담↑(`SIGNAL_CONTROL_TRACKING=false`로 off). 단위테스트 `ControlAnalysisServiceTest` 4. 🐞 **기간 정렬 부재 — 같은 유형 두 번째 재발(2026-08-27 실측·수정)**: `feature-mining`이 8/25에 받은 `overlapWindow` 정렬이 **여기엔 없어** 전체 구간 net끼리 비교했다. 대조군 사유는 도입 시점이 제각각이라(강제 기록 사유는 특히) **진입군보다 늦게 시작**하므로, 도입 직후엔 **구조적으로 부풀려진다**. 실측 `REVERSAL_L`: ENTERED **n=209(4거래일, 8/25 급등일 포함)** vs `NOT_WEAK` **n=61(1거래일)**을 그대로 빼서 edge를 **+1.85%p**로 보고했는데, 같은 날로 맞추면 **+0.69%p**였다. → `Stat`에 `alignedFrom`/`alignedTo`/`alignedSamples`/`alignedNetPct`/`alignedEnteredSamples`/`alignedEnteredNetPct`/`edgeVsEnteredPct` 신설. ⚠️ **동작 변경의 핵심은 `hint`·`diagnose`의 비교 기준을 정렬값으로 바꾼 것** — 노출만 늘리면 사람이 여전히 부풀려진 수치를 읽는다. 정렬값이 없으면(진입일 미상) **비교를 생략**한다(부풀린 숫자로 필터를 흔드는 것보다 침묵이 낫다). ⚠️ 구간은 **교집합이 아니라 범위**(min/max) — 그룹마다 신호 나는 날이 달라 교집합은 표본을 과하게 깎는다. 막으려는 건 "한쪽이 통째로 다른 기간"이지 "중간 날짜가 비는 것"이 아니다. 판정 `overlapWindow`(순수 정적). 테스트 `ControlAlignmentTest` 5. **배포 즉시 결론이 바뀐 사례 2건**: ① **G `FLOW_DOWN`** — 정렬 전엔 −0.52% > ENTERED −0.69%라 "흐름 필터가 역효과"로 읽혔으나, 정렬하면 같은 창(8/24~8/27) 진입 **+0.92%(n=4)** vs 차단분 −0.52% → **edge −1.44 = 필터 유효**로 뒤집힌다(hint도 "현 필터 유지"로 정정). ② **L `NOT_WEAK`** — 정렬 후 진입 **+0.15%(n=15)** vs 차단분 −0.50%(n=61), 같은 날(8/27) 기준 **edge −0.65 = 필터 유효**. 즉 정렬 전 수치로 필터를 완화했다면 **둘 다 잘못된 방향**이었다. ⚠️ 단 정렬 창의 진입 n이 4·15로 얇으니 "판정 불가"에 가깝다 — 정렬의 효용은 *올바른 값을 준다*기보다 **부풀려진 값으로 오판하는 것을 막는다**는 데 있다. ⚠️ **교훈: 클러스터 가드(8/21·8/24·8/26 3회)와 기간 정렬(8/25·8/27 2회)이 각각 재발했다** — 새 집계 엔드포인트를 만들 때 두 가드를 체크리스트로 강제할 것. 🐞 **단일일 클러스터 가드 부재(2026-08-21 수정)**: `feature-mining`·`strategy-gate`는 클러스터 가드를 갖고 있는데 `control-diagnosis`·`swing-trail-analysis`엔 없어서 **같은 데이터가 엔드포인트에 따라 정반대 결론**을 냈다 — C가 `verdict OK / net +4.71%(n=195)`로 **유일한 흑자 전략**으로 보고됐으나 195건 중 **134건(69%)이 2026-06-26 하루**였다. ⚠️ **건수 비중 문턱(80%)으론 이걸 못 잡는다**(69% < 80%, 거래일 24개로 통과) → 게이트가 쓰는 **LOO(최대기여일 제외 net)**를 판정 기준으로 채택: **net 합 기여 최대인 하루를 빼고 부호가 뒤집히면 `CLUSTERED`(판정 보류)**. 실측 결과 C는 06-26 제외 시 **익일보유 +4.71% → −0.71%**, 트레일 3% **+4.86% → −0.22%** 로 **네 방식 전부 부호 반전** — 즉 **C의 흑자 기록은 통째로 그 하루이고, 제외하면 C도 나머지 전략들과 같은 손실군**이다. 클러스터된 탈락 버킷은 `outperformingRejects` 비교에서도 제외(하루 이벤트로 필터를 흔들지 않게). `SwingExitProvider`도 클러스터 우위는 채택 안 함(기존 조건에 AND라 **닫히는 방향으로만** 작용). ⚠️ 손실 전략은 종전대로 `LOSER_*`로 남는다(부호가 안 뒤집히므로) — 경고를 덮지 않는다. `Stat`에 `distinctDays`/`maxDaySharePct`/`topDay`/`netExTopDayPct` 노출. ⚠️ 전 시장 전수 아님 — **거래량 급증 후보로 한정**(thesis 있는 모집단; 비급증 전종목 추적은 KIS 비현실적).
- **청산방식 비교**(`ExitStrategyService`, `GET /api/v1/admin/exit-comparison`): 기록된 가격경로에 ① 시간기반(권장 보유시간) ② 트레일링 스탑(되돌림 격자 1~5%) ③ 신호기반(VWAP 이탈, 마크별 실측 `wghn_avrg_stck_prc` 하향돌파) ④ 신호기반(추세전환 근사, 직전 마크 대비 하락)을 시뮬레이션해 전략별 평균수익 비교 → 높은 쪽 추천. **고정 TP/SL은 제외**(사용자 요구: 데이터 기반 청산만). VWAP/거래량은 `OutcomeSample.vwap`/`acml_volume`에 마크별 저장(`TradeFollowUpService.withSignals`→`KisApiClient.fetchVwapVolume`); ③은 vwap 저장된 표본만 집계(과거 표본 vwap=null → 제외). MFE/MAE(`TradeOutcome.peak_price`/`trough_price`, 매 분 갱신)로 잠재 상·하방 근거 제공. ⚠️ 마크 단위 근사. 평균수익은 레이어 4 슬리피지 차감(net)이나 MFE/MAE는 gross. → 표본 충분 후 해석. 이 결과가 향후 자동매매 청산 규칙 결정 입력.
- **보유 점수 관찰(2026-07-23, 조회 전용)**: `HoldScoreService` + `GET /api/v1/admin/hold-score` — 미청산 포지션별 **7신호 합의**(①분봉모멘텀 ②거래활성도 ③VWAP위 ④고점되돌림<1% ⑤지수mom30 ⑥시장폭≥50% ⑦체결강도≥100)로 "계속 들고갈만한가" 관찰. **⚠️ 청산 결정 미연결(의도)** — 7/23 what-if(1,210표본)에서 신호합의는 마크시점 승자 식별력(+1.5~2%p)만 있고 **연장·조기청산 양방향 모두 엣지 없음**(권장 마크가 국소 최적, [[hold-extension-whatif]] 메모리) → 관찰 + 표본 축적 후 재검용. 비용: 온디맨드 조회 시에만 포지션당 분봉·VWAP·체결강도 각 1콜. **+ 마크 컨텍스트 저장**: `OutcomeSample.idx_mom30/breadth_pct`(nullable, ddl-auto 안전) — 매 마크에 지수흐름·시장폭 실측 저장(TTL 캐시 재사용, KIS 0). what-if의 앵커 보간 근사를 실측으로 대체해 수 주 뒤 조건부 청산/기대수익 테이블 정밀 재검. 테스트 `HoldScoreServiceTest`.
- **당일 청산시점 분석**(데이트레이딩 목표): `OutcomeSample` 테이블에 진입 후 보유시간 마크별 가격을 `TradeFollowUpService.sampleExitMarks`가 적재. **가격 마크는 `followup.exit-marks`=5분 단위 300분까지(60개, 청산시점 5분 해상도 최적화용)** — 가격은 매분 조회값 재활용이라 **추가 KIS 호출 0**. ⚠️ **VWAP만 `followup.vwap-marks`(성긴 세트 5/10/15/30/45/60/90/120/180/240/300)에서만 조회**(`withSignals`) — VWAP은 청산방식(VWAP이탈) 시뮬용이라 5분 정밀도 불요, 마크마다 VWAP 1콜씩이면 부하 5.5배라 분리. env `FOLLOWUP_EXIT_MARKS`/`FOLLOWUP_VWAP_MARKS`. `GET /api/v1/admin/exit-timing`(`ExitTimingService`) → 전략별 **보유시간별 평균수익률·승률 곡선 + 평균수익 최대 마크(권장 청산시점)**. 예: B는 진입 45분, C는 120분 부근이 정점. (분석 목표는 "평균적으로 언제 팔면 수익 최대"이며 장기/익일은 불필요.)
- **장중흐름 태깅·분석**(2026-07-09, 단타 인트라데이 반전 대응): 일봉 국면 엔진은 장중 반전(오전↑→오후↓)을 못 봐서, **진입 시점 지수(프록시 ETF) 장중 흐름**을 태깅. `MarketRegimeService.intradayFlow`가 프록시 분봉 최근 **10/30/60분 등락%**(방향/기울기 — 고점대비 위치 아님) 계산(시장별 90초 캐시), `StrategyEvaluator`가 진입 시 `TradeOutcome.entry_index_mom10/30/60`에 기록(인버스 제외). **소급**: `FlowBacktagService`+`POST /admin/backfill-flow-tags` — 저장된 `entry_market_change`(60초 캐시)들로 (시장,날짜)별 지수경로 보간해 mom30/60 채움(mom10 제외=해상도부족·backfill 구분자, KIS 과거 지수분봉 부재로 minute 정밀 복원 불가). **분석**: `FlowAnalysisService`+`GET /admin/flow-analysis?lag=30|60` — 전 전략 × 흐름부호 × 국면 net·승률(**exit-horizon**=perf-gate와 동일)·what-if. 실측: **B=딥바잉(흐름↓ 우위, 흐름↓ +0.29% vs 흐름↑ −0.63%)·D·E는 흐름↑ 쪽·C 무관 → 흐름은 전략별 방향 반대라 단일 게이트 금지**. 아직 게이트 미적용(what-if 문턱 미달 + 표본 전부 약세장이라 국면교란 미분리). 테스트 `MarketRegimeServiceTest`(momPct)·`FlowBacktagServiceTest`(보간)·`FlowAnalysisServiceTest`. **+ G·J 흐름↓ 스킵(2026-08-21, 사용자 결정)**: D의 `SIGNAL_D_REQUIRE_RISING_FLOW`와 **같은 필터를 G·J에도** 추가 (`SIGNAL_G_REQUIRE_RISING_FLOW`/`SIGNAL_J_REQUIRE_RISING_FLOW`, 코드 기본 false, prod만 true). 근거는 **lag30·lag60 양쪽에서 일관**한 것만 채택 — G 흐름↓ **−0.90%(n=217)/−0.88%(n=236)** vs 흐름↑ −0.08/−0.05(n=404/389), J 흐름↓ **−0.90%(n=79)** vs 흐름↑ −0.17/−0.15(n=156/155). 둘 다 반등 계열이라 방향이 같다(지수가 내리는 중의 반등 매수 = 떨어지는 칼날). ⚠️ **전역 게이트 금지** — 흐름 유불리는 전략마다 반대다(B는 딥바잉이라 흐름↓ 우위). ⚠️ **판정 위치는 마지막 게이트** — 전략 고유 조건을 다 통과한 후보에만 걸어야 `FLOW_DOWN` 대조군이 "조건은 다 맞았는데 흐름↓"가 돼 ENTERED와 직접 비교된다(D와 동일 설계). 차단분은 `tracksControl` 무관 **강제 대조군 기록**(G·J 둘 다 control-off). 흐름 미산출(개장 ~30분·조회실패)이면 미적용(degrade open). ⚠️ **기대효과는 흑자 전환이 아니라 출혈 감소**다 — 흐름↑만 남겨도 G −0.05%·J −0.15%로 여전히 음수. ⚠️ J의 흐름↓ 표본은 79로 G(217/236)보다 약한 근거. 진입 조건이 바뀌므로 **`TRADING_PERF_GATE_STRATEGY_SINCE`에 G·J를 `20260824`로 추가**했다. 테스트 2. **+ A 흐름↓ 스킵(2026-08-24, 사용자 결정)**: 같은 필터를 A에도 추가(`SIGNAL_A_REQUIRE_RISING_FLOW`, 코드 기본 false, prod만 true). 근거도 같은 형식으로 **양 lag 일관** — A 흐름↓ **−1.85%(n=41)/−1.87%(n=41)** vs 흐름↑ **−0.35%(n=42)**로 격차 1.5%p. 지수가 내리는 중엔 공시 촉매가 시장에 먹힌다. ⚠️ **표본이 G(217/236)보다 훨씬 얇다** — A는 공시 경로라 진입 자체가 드물어(40일 79건) 표본이 느리게 쌓인다. 방향은 믿되 차단분 대조군으로 계속 재검할 것. ⚠️ 기대효과는 흑자 전환이 아니라 **출혈 감소**(흐름↑만 남겨도 −0.35%로 음수). ⚠️ A는 `tracksControl` 기본 경로라 `FLOW_DOWN` 기록이 자동으로 남는다(G·J는 강제 기록 대상). 테스트 4.
- **Feature-space 마이닝**(`FeatureMiningService`, `GET /api/v1/admin/feature-mining?horizon=&includeControl=&minSamples=&maxDayShare=&regime=&market=`, 2026-08-12): 전략 단위 채점이 아니라 `TradeOutcome`을 **feature 축으로 bin**해 "어떤 조건 구간이 수익이었나"를 스캔(아직 전략화 안 된 pocket 발굴). bucket마다 단일일 점유율 가드(`clustered`)로 이벤트성 허수 제외. `includeControl=true`면 같은 pocket의 **미진입 대조군**과 비교해 `edgeVsControlPct`(진입−대조군) 산출. 🐞 **대조군 horizon 불일치 버그(2026-08-14 실측·수정)**: 대조군은 경량 추적이라 **exit 마크를 거의 수집하지 않는데**(커버리지 ~15%) 그 편향 부분집합으로 controlNet을 계산해 **edge 부호가 뒤집혔다**(ret5d%<−5: exit −2.51%p ↔ close **+0.13%p** / 지수mom30≥0: −1.56 ↔ −0.30). 수정: bucket별 **`controlCoveragePct`(가격 보유 대조군/전체) 산출 → `MIN_CONTROL_COVERAGE_PCT`(70%) 미만이면 `controlNetPct`·`edgeVsControlPct`를 null**로 비우고 커버리지를 응답에 노출(수집 확대는 부하상 비현실적 → "신뢰도 노출"이 해법). ⚠️ **진입군 pocket 랭킹은 exit horizon, 진입-대조군 반사실 비교는 close horizon**으로 볼 것(`control-analysis?horizon=exit`도 같은 이유로 reject 버킷이 공집합). `daily-analysis` 스킬 문서에도 반영됨. 테스트 `FeatureMiningServiceTest` 6. 🐞 **LOO 가드 부재 — 같은 버그의 세 번째 재발(2026-08-26 실측·수정)**: bucket `clustered`가 **건수 점유율만** 봐서, 점유율은 문턱 아래인데 net은 하루가 만든 pocket을 못 걸렀다. 실측 highlights **1위가 `전략=REVERSAL_L`**(net **+1.44%**, n=194, 점유율 45.9% < 80 → `clustered=false`)였는데 **최대기여일(20260825) 제외 −0.08%로 부호가 뒤집힌다**. 같은 데이터를 `control-analysis`는 **이미 `clustered=true`로 판정 중**이었다 = **엔드포인트끼리 정반대 결론**(`control-diagnosis` 8/21 · `multiday-exit` 8/24와 **완전히 같은 유형**). → 판정을 **점유율 + 거래일 ≥3 + LOO 부호 유지**로 통일(`ControlAnalysisService.toStat`·`MultidayExitAnalysisService.cluster`와 동일 규칙), `topDay`/`netExTopDayPct` 노출. ⚠️ **교훈이 세 번 반복됐다는 게 요점** — 클러스터 가드는 분석 엔드포인트 **전체**에 같이 깔려야 하고, 새 집계 엔드포인트를 만들 때 체크리스트에 넣을 것. 🐞 **edge가 기간 차이를 재던 버그(2026-08-25 실측·수정)**: 커버리지 가드(70%)는 "가격 보유 비율"만 봐서 **진입군과 대조군의 기간이 어긋나는 것을 못 잡았다** — 축마다 대조군 태깅 시작일이 다르다(수급·뉴스는 소급돼 전 구간, 호가불균형 2026-08-22~, **체결강도 대조군은 2026-08-25~**). 실측: 체결강도 축 edge가 **−1.89~−6.37**로 나왔는데 진입군은 27거래일인데 대조군은 **그날 하루뿐**이었고 하필 V자 반등일이라 대조군 net만 부풀었다(전부 허수). 수정: **두 군이 겹치는 거래일 구간으로 양쪽을 자른 뒤에만** edge를 계산하고, `edgeFrom`/`edgeTo`/`edgeEnteredN`/`edgeEnteredNetPct`를 응답에 노출해 정렬이 얼마나 잘랐는지 보이게 했다(체결강도는 진입 n 1,429→**80**·구간 하루로 축소돼 근거 부족이 드러난다). `netAvgPct`(pocket 랭킹)는 전체 구간 그대로 둔다. 판정 `overlapWindow`(순수 정적 — yyyyMMdd 고정폭이라 사전식 비교가 곧 시간순). ⚠️ 배포 직후 **뉴스 축이 20260716~20260824로 잘리는 것**도 드러났다 — 뉴스는 진입 시에만 lazy 태깅이라 **당일 대조군이 비어 있고**, 소급 백필을 돌려야 그날이 창에 들어온다(⑥ 루틴이 분석 전 필수인 이유가 하나 더 늘었다). ⚠️ **교훈: 커버리지와 기간은 다른 축이다** — 커버리지 100%여도 비교 기간이 어긋나면 edge는 무의미하다. **+ 시간분할 `?since=`/`?until=`(yyyyMMdd, 2026-08-21)**: pocket 후보를 전·후반으로 갈라 부호가 유지되는지 보는 게 다중검정 허수를 거르는 가장 실용적 방법인데 파라미터가 없어 매번 raw SQL로 내려가야 했다. `since`는 lookbackDays cutoff를 대체하고 응답에 노출. 구간 필터는 저장소 쿼리(하한)와 **별개로 인메모리에서도 확인**(저장소 구현 의존 제거). **🔴 발굴 세션 결론(2026-08-21) — 신규 전략 후보 0건, 그리고 그 이유가 중요하다**: 40일·4,086진입을 전 축으로 스캔하고 발굴 필터(비클러스터·n≥30·거래일≥5·net>0·edge>0·커버리지≥70)를 통과한 pocket이 50개 나왔는데, **상위 30개 중 22개가 `시장=KOSPI × 국면=NEUTRAL` 한 세그먼트**였다. 세그먼트 기준선을 재보니 **KOSPI×NEUTRAL만 절대 net 양수(+0.32%, n=601, edge +0.28)이고 나머지 5개 셀은 전부 음수**(KOSDAQ은 세 국면 모두 음수, edge −0.22) — 즉 **feature pocket으로 보이던 것들은 전부 이 세그먼트가 비쳐 보인 것**이었다. ⚠️ **교훈 1 — 단일 feature 마이닝은 시장×국면 교란을 먼저 제거하고 읽을 것.** 두 후보를 끝까지 검증했으나 둘 다 기각: ① **ATR≥7** — KOSPI×NEUTRAL에서 7전략 중 6개가 같은 방향이라 전략 아티팩트가 아니었지만, **시장 통합 시 단조성 소멸**(중립 ATR≥7 edge +0.04로 오히려 최약)하고 **KOSDAQ 중립에선 edge −0.79로 부호 반전**, 결정적으로 **시간분할에서 전반(n=345, 12거래일) edge −0.21**. 18개 셀(3국면×2시장×3ATR) 중 1개가 좋았던 것 = 다중검정 산물. ② **업종=유통** — 세그먼트 기준선 대비 6/6 셀 양(+0.77%p)이라 유일하게 일관성 테스트를 통과하는 듯했으나, **대조군으로 다시 보면 4/6**(BEAR 두 셀은 음수)이고 절대 net −0.07%(n=248), 시간분할 −0.47 → +1.15로 부호 반전. ⚠️ **교훈 2 — `lift`(세그먼트 기준선 대비)는 반사실이 아니다.** 유통이 잘 나간 것과 '우리가 유통에서 고른 종목이 나았던 것'은 다르며, 후자는 **대조군으로만** 측정된다. ⚠️ **교훈 3 — 실제로 유효한 축은 이미 게이트가 쓰고 있다**(전략×시장×국면). 발굴이 찾아낸 유일한 실구조가 게이트의 버킷팅 축과 같다는 건 **미탐색 pocket이 없다**는 뜻이고, 다음 진전은 새 조건 발굴이 아니라 **검증 역량**(대조군 태깅 커버리지·유니버스 스냅샷)에서 나온다.
- **멀티데이 청산 비교**(`MultidayExitAnalysisService`, `GET /api/v1/admin/multiday-exit-comparison?fullPathsOnly=`): C·D·J의 일봉 경로(`OutcomeDailyMark`, D0~D+15)로 **보유 D+N / 트레일 / MA이탈 / 손절**을 시뮬. **⚠️ 고정 코호트 `fullPathsOnly=true`(2026-08-21)** — 기본(혼합) 비교는 **horizon마다 표본이 다르다**(D는 D+1 n=890 → D+10 n=533 → D+15 n=174): 뒤쪽 horizon일수록 "그만큼 오래 전에 진입한 것"만 남아 **서로 다른 시장 국면의 부분집합**을 비교하게 된다. 완주 표본만 고정하면 보유기간 효과만 분리된다. **🔴 결과 — D 스윙 전환 가설 반증**: 혼합에선 D+1 +0.57% → D+3 +1.12 → D+5 +1.62 → **D+10 +3.16%**로 단조 증가해 "오래 들수록 좋다"로 보였는데, **고정 코호트(n=174 동일)에선 D+1 −1.27 → D+3 −3.44 → D+5 −2.20 → D+10 −8.12%**로 **전부 음수이고 방향도 반대**다 — 그 단조 증가는 순전히 **코호트 교체의 산물**이었다. J도 동일(혼합 D+1 +0.07 vs 고정 −1.37). C만 고정에서도 양수(D+1 +5.91)지만 06-26 단일일 클러스터라 별개로 신뢰 불가. ⚠️ **두 모드의 교란이 서로 다르다** — 혼합은 horizon 교란, 고정은 **기간 선택 편향**(완주 표본 = 오래된 진입만)이라 함께 볼 것. 다만 "동일 표본 안에서 보유 연장이 이득인가"는 고정 코호트만 답할 수 있고 답은 **아니오**다. `outcomes`/`fullPaths`는 두 모드 모두 전체 기준으로 보고돼 표본 축소를 알아볼 수 있다. 🐞 **단일일 클러스터 가드 부재(2026-08-24 실측·수정)**: `control-diagnosis`가 8/21에 받은 LOO 가드가 **여기엔 전파되지 않아** 이 엔드포인트가 "D 보유 D+5 **+3.53%**"를 보고했다 — 완주 코호트 313건 중 **139건(44%)이 20260731 하루**이고, 그날을 빼면 D+1 −1.49 / D+3 −3.66 / D+5 −2.42 / D+10 −8.34 / D+15 −2.48 로 **전 horizon 부호가 뒤집힌다**(=8/21의 D 스윙 반증은 뒤집힌 게 아니라 그대로이며, 뒤집혀 보인 건 7/31이 코호트에 들어왔기 때문이다). C는 더 심해 완주 150건 중 **134건(89%)이 20260626**. ⚠️ **건수 비중 44%는 문턱(80%)을 통과하므로 LOO 부호 반전 없이는 못 잡는다** — 8/21과 똑같은 교훈이 다른 엔드포인트에서 반복됐다. 수정: `MethodResult`에 `distinctDays`/`maxDaySharePct`/`topDay`/`netExTopDayPct`/`clustered` 노출 + **권장(recommended) 선정에서 clustered 방식 제외**. ⚠️ 진입일 조달이 설계 포인트 — `OutcomeDailyMark`엔 진입일이 없고(`businessDate`는 **마크가 찍힌 날**) D0 마크는 구 백필분에 없어(실측 mark_days=0 이 1,222행 vs D+1 1,348행) 대용이 안 된다 → `TradeOutcome`과 조인하는 쿼리(`findEntryDatesByStrategy`)를 신설. 진입일 미상 표본은 판정 생략(clustered=false + `distinctDays=0`으로 **진단 부재임이 드러나게**). ⚠️ **교훈: 클러스터 가드는 분석 엔드포인트 전체에 같이 깔려야 한다** — 한 곳만 고치면 나머지가 조용히 반대 결론을 낸다(horizon 규칙이 게이트·집행품질에서 겪은 것과 같은 유형). 테스트 4.
- **MAE 히트 분석**(`MaeAnalysisService`, `GET /api/v1/admin/heat-analysis`, 2026-07-09): 저장된 `peak_price`(MFE)/`trough_price`(MAE)로 전략별 승자/패자 MAE 분포(평균·중앙·최악10%)+손절선 시뮬(−3/−5/−7/−10%: MAE≤S인 승자=죽인 승자·패자=방어) → **추측값이던 catastrophic-stop(−7%) 데이터 검증**. 승자/패자 분류 exit-horizon. ⚠️ peak/trough 순서 미상 근사(고점 먼저면 실제 승자희생 더 적음). 실측: **C는 −7% 적합(분리 깨끗)·B는 −5% 후보(승자 거의 안 물림)·D는 손절 취약(승자도 깊이 물림) → 손절선도 전략별로 다름**(전략별 적응형 손절 여지, 미구현). 테스트 `MaeAnalysisServiceTest`.
- **시장 폭(breadth) 집계·태깅**(`MarketBreadthService`, `GET /api/v1/admin/market-breadth`, 2026-07-09): 스캔 중 **전 종목 등락률**로 전체·시장별(KOSPI/KOSDAQ) **상승비율%·중앙등락%** 산출 — 지수(시총가중)가 못 보는 "참여 넓이/좁은 랠리". `WatchlistScanService`가 전체 스캔만 `beginScan/publish` 브래킷, `StrategyEvaluator`가 **볼륨 게이트 이전**(signal 직후)에 `record(code,changeRate)`(급증종목만 세면 편향 방지 — 단 세션가드 뒤라 장중만), 시장은 `Company.market` 룩업. 진입 시 `entry_breadth_pct`·`entry_market_breadth_pct` 태깅. forward-only(backfill 불가). nullable→ddl-auto 안전. 테스트 `MarketBreadthServiceTest`. **+ `feature-mining` `시장폭%` 축 신설(2026-08-24, 경계 30/50/70)**: 태깅은 2026-07-09부터 하고 있었는데 **분석 축이 없어 한 번도 물어본 적이 없었다**. 계기는 8/24 실측 — KOSPI −1.62%(삼성전자 −8.7%) / KOSDAQ +1.51%인데 **양 시장 다 BEAR 라벨**이었고 breadth는 58% 상승·중앙 +0.47%였다. 즉 **BEAR 라벨 아래에서 종목 다수가 오른** 날이고, 게이트는 BEAR 버킷으로 대부분 닫혔는데 섀도우는 +0.68%였다(기회 상실). "국면 라벨이 **종목 단위 승률**과 얼마나 상관하는가"를 직접 잰 적이 없어 이 괴리를 물을 수가 없었다. 저장된 컬럼 계산만이라 **비용 0·소급 즉시 적용**. ⚠️ **국면과의 교차는 별도 축을 만들지 않고 기존 `?regime=` 파라미터와 조합**해 본다(축을 늘리지 않아 다중검정 비용이 안 붙는다). ⚠️ 전체(overall) breadth는 **일부러 뺐다** — 시장별 값과 강상관이라 축만 늘고 정보가 안 는다. ⚠️ 경계 30/50/70은 breadth 리스크오프 문턱(15%)보다 훨씬 위 — 저건 '붕괴' 판정이고 이건 '평상시 참여 넓이'라 목적이 다르다. 🐞 **부분 스캔이 정상 스냅샷을 덮는 버그(2026-08-14 실측·수정)**: 15:20 세션가드가 전수 스캔을 중간에 끊어 **n=718 부분 스냅샷이 직전 정상 n=1,334를 덮어썼고**, 순회 순서상 KOSDAQ 구간에 도달 못 해 **KOSDAQ 행 자체가 소실**(=`breadthRiskOff("KOSDAQ")` 판정 소스 상실. 당시엔 `isFresh` 40분 가드 덕에 실주문 영향 없었음). 수정: `publish()`가 **커버리지(기록 종목/워치리스트 유니버스) < `breadth-min-coverage-ratio`(0.8)면 기존 스냅샷 유지**(판정 `hasEnoughCoverage` 순수, 유니버스 미상이면 degrade open). 부분 데이터보다 조금 오래된 완전 데이터가 낫다(신선도는 isFresh가 별도 판정). env `SIGNAL_BREADTH_MIN_COVERAGE_RATIO`(0=비활성, compose 패스스루). 테스트 2.
- **유니버스 스냅샷**(`UniverseSnapshotService`+`UniverseSnapshot`, `GET /api/v1/admin/universe-snapshot-status`, 2026-08-14): 기존 표본이 전부 **거래량 급증 모집단 안에서만** 뽑히는 구조적 편향을 벗어나려고, 정해진 시각(`universe-snapshot-times` 기본 09:30/11:00/13:00/14:30)마다 워치리스트 **전 종목**의 일봉 feature + 사후 수익을 한 줄로 남긴다 = **P(승자|feature)의 분모**. 수집 훅이 breadth와 같은 자리(볼륨 게이트 이전)라 **추가 KIS 0**. 🐞 **종가 타깃이 순회 순서에 종속돼 시장 편중된 버그(2026-08-19 실측·수정)**: 종가를 "`CLOSE_MARK`(15:15) 이후 스캔에서 그 종목을 다시 만난 현재가"로 채웠는데, 전수 스캔이 12분 주기이고 세션가드가 15:20에 자르므로 **채울 수 있는 창이 5분뿐**이었다. 순회 순서가 매일 같아 채워지는 종목이 고정됐고 실측 **KOSDAQ 460/500(92%) vs KOSPI 302/834(36%)**, 전체 762/1334(57%) — **급증 편향을 없애려 만든 분모에 순회 순서라는 새 편향**이 들어갔다(익일종가는 `priceClose!=null` 전제라 편향 이월). 수정: **익일 첫 스캔에서 일봉의 확정 종가로 채운다** — `SignalResult`에 `prevClose`(rows[1].close)·`prevBusinessDate`(rows[1].businessDate) 신설(호환 생성자 유지), `fillTargets`가 **`prevBusinessDate == 우리 DB의 직전 스냅샷일`일 때만** 전일 스냅샷의 `price_close`를, 전전일 스냅샷의 `price_next_close`를 채운다. 커버리지 100% · 근사 아닌 **공식 종가** · 추가 KIS 0. 날짜 대조는 앱 다운·휴장·일봉 지연 시 엉뚱한 날 종가가 들어가는 것을 막는 **fail-closed 가드**(`closeFillUsable` 순수). 대가는 종가가 **익일에** 채워지는 것(연구용이라 무해). ⚠️ **소급 불가** — 8/18~19 수집분의 종가 결손(KOSPI 36%)은 복구되지 않으니 그 전제로만 해석할 것. + **14:30 버킷 90분 타깃 제외(같은 수정)**: 14:30+90분=16:00이라 당일 스캔으로 영원히 못 채우는데(두 날 모두 `filled90m=0`) pending으로 세고 있어 매 스캔 헛조회를 유발했다 → `reachable90mBuckets`(버킷+target ≤ `session-end`)로 대상 버킷을 한정하고 `describe()`에 `has90mTarget` 노출(결손이 아니라 **해당 없음**임을 구분). 테스트 `UniverseSnapshotServiceTest` 3 추가. **+ 분석 엔드포인트(Phase 2, 2026-08-21)**: `UniverseAnalysisService` + `GET /api/v1/admin/universe-analysis?horizon=m90|close|nextClose&market=&snapTime=&minSamples=&since=&until=` — feature 구간별 **base rate 대비 lift**(bucket net − 전체 net)를 낸다. **lift가 주지표**인 이유는 어떤 날은 시장 전체가 −2%라 모든 bucket이 음수로 나오기 때문 — 매매 가능한 엣지는 절대 net이 아니라 상대치에 있다. 단일일 클러스터 bucket은 랭킹에서 제외, 해석 함정(거래일 부족·+90분 근사·8/19 이전 종가 편향)은 `caveats`로 응답에 실어 보낸다. 테스트 `UniverseAnalysisServiceTest` 5. **🔴 첫 실측 — 거래량 급증 스크리닝에 엣지가 없다(2026-08-21)**: 시스템의 거의 모든 전략이 `volumeSpike`를 전제로 후보를 좁히는데, 급증 밖 표본이 없어 지금까지 **그 전제 자체를 검증할 수 없었다**. 유니버스 분모로 재보니 **급증 종목의 lift가 전 horizon·전 거래일에서 음수**다 — m90 4거래일 **−0.20%p**(급증 −0.75% vs 미급증 −0.50%), close −0.08%p, nextClose **−1.06%p**(승률도 −5.79%p). ⚠️ **일별로도 4/4 전부 음수**(−0.05·−0.05·−0.01·−0.55)이고 **시간분할 양쪽 반기도 음수**(−0.14 / −0.27) — 다른 후보들을 죽였던 일관성 테스트를 통과한다. ⚠️ 더 정확히는 **비대칭 베타**다: 지수가 크게 오른 8/20(KOSPI +6.17%)에도 lift가 **−0.01로 이득이 없고**, 하락일엔 −0.55로 손해다. 즉 급증은 **상방은 안 주고 하방만 주는** 셈. 이 방향은 기존 3개 증거와도 정합(B의 `WEAK_VOLUME` 대조군 +0.33% > 진입 −0.50%, 거래량배수 pocket이 8~15 −1.42%·≥15 −2.65%로 단조 악화, 전역 필터 `max-entry-volume-ratio` 15배 edge −0.55%p) — **네 번째 독립 증거**. ⚠️ **다만 이건 '무조건 매수'의 평균**이라 "우리의 급증 기반 선택이 나쁘다"와는 다른 명제다(후자는 대조군 edge가 재는 것). ⚠️ **표본 4거래일**(close/nextClose는 2일)이라 확정적 결론 금지 — 상승일 표본이 특히 부족하다. ⚠️ 저ATR·저거래량·MA아래 pocket도 양의 lift가 나왔으나 **하락 편향 구간의 저베타 효과와 구분되지 않는다**(저ATR lift가 폭락일 8/18에 +0.61로 최대) — 상승일 누적 후 재검.
- **셋업 feature 태깅**(2026-07-09): `TradeOutcome.entry_atr_pct`(변동성)·`entry_dist_high_pct`(직전20일고가 대비 거리)·`entry_ret5d_pct`(최근5일 수익률) — `MarketSignalService`가 이미 조회한 일봉에서 계산(`PositionSizer.computeAtr` 재사용·추가 KIS 0), `SignalResult`에 실어(11인자 호환 생성자로 기존 호출 무변경) `StrategyEvaluator` 태깅. "어떤 셋업이 이기나" 분석용, forward-only(누적 후 outcome-analysis). nullable→ddl-auto 안전.
- **호가 불균형 태깅(2026-08-22, 측정 먼저)**: `TradeOutcome.entry_obi1`(최우선 1호가)·`entry_obi5`(상위 5호가 누적) = **(매수잔량−매도잔량)/(매수잔량+매도잔량)×100**. 체결강도가 "**이미 체결된**" 압력이라면 이건 "**아직 대기 중인**" 물량의 압력이라 서로 다른 축이고, 단기(30~90분) 예측력이 문헌에 문서화돼 있어 **가설이 선행하는** 몇 안 되는 후보다(발굴 세션 교훈: 가설 없이 feature만 늘리면 다중검정 허수가 는다). ⚠️ **수집 지점이 핵심** — `StrategyEvaluator`가 슬리피지용으로 이미 호출하던 호가 조회(`fetchAskingPrice`→**`fetchOrderBook`으로 교체**, 같은 1콜에서 스프레드+불균형을 함께 뽑음)에 얹었다. 이 자리는 **볼륨게이트 통과 후보 전원**이 지나므로 **대조군도 함께 태깅**된다 → **뉴스 축이 겪은 "대조군 커버리지 0% → `edgeVsControlPct` 구조적 null"을 처음부터 회피**(뉴스는 진입 시에만 종목당 1콜 lazy였다). **추가 KIS 호출 0**. ⚠️ KIS 응답 DTO가 **매도측만 매핑**하고 있어(`bidp1` 가격 하나뿐, 매수 잔량 없음) `KisAskingPriceResponse.Output1`에 `bidpN`/`bidp_rsqnN` 10단계를 추가했다. 계산은 `KisApiClient.OrderBook.imbalancePct(levels)`(순수 — KIS 없이 테스트), 한쪽 사다리가 비면(장전·거래정지) **0이 아니라 null**(균형 bin 오염 방지). `OrderBook`은 3인자로 늘리되 **2인자 호환 생성자 유지**(`PositionSizer` 시장충격 캡은 매도측만 쓴다). ⚠️ **판정 주지표는 obi5**(깊이·안정적), obi1은 즉시 압력이라 노이즈가 크다 — 둘이 어긋나면 obi5를 믿을 것. 두 축을 함께 두는 건 다중검정 비용이지만 forward-only라 지금 안 모으면 몇 주를 잃는다(**수집 ≠ 검정**). ⚠️ **평균 비교로 판정 금지** — 뉴스경과분에서 왜도 큰 feature의 승자/패자 평균이 정반대 결론을 낸 전례가 있다. `feature-mining` bin별 net(경계 −30/0/30)과 `edgeVsControlPct`로 볼 것. `outcome-analysis` "호가불균형1%/5%" 노출, 검증 `GET /api/v1/admin/spread?stockCode=`(obi1/obi5·사다리 단계수 포함). forward-only, nullable→ddl-auto 안전. 단위테스트 `OrderBookImbalanceTest` 7. ⚠️ 스냅샷 지표라 체결 직전 취소가 잦은 종목은 실제 압력과 다를 수 있다(측정 단계 수용). ⚠️ `UniverseSnapshot`엔 미수집 — 그 훅은 전 종목이라 호가 조회가 1,500콜이 된다.
- **수급(외국인·기관) 소급 태깅 + 🔴 가설 반증(2026-08-22)**: `TradeOutcome.entry_frgn_ntby_ratio`·`entry_orgn_ntby_ratio`(순매수 수량/거래량×100)·`entry_ntby_basis_date`. 소스는 KIS 종목별 투자자매매동향(`fetchInvestorDaily`, FHKST01010900). ⚠️ **이 축만 소급(backfill)이 된다** — 1콜에 최근 **30거래일치**가 오므로 **종목당 1콜로 그 종목의 모든 과거 진입일이 덮인다**(뉴스·체결강도·호가불균형은 전부 forward-only였다). 실측 **990종목 콜 → 33,067행 태깅(진입+대조군), 2분 22초, 실패 0, 대조군 커버리지 100%**. `InvestorFlowBacktagService` + `POST /admin/backfill-investor-flow`(null만 채움, 종목별 실패 격리, 재실행 안전). ⚠️ **기준일은 진입일 '직전' 거래일**(`KisInvestorResponse.priorTo`, 순수·테스트 7) — 진입일 당일 행은 장 마감까지의 수급이라 진입 시점엔 없던 정보(look-ahead)다. **소급 태깅은 미래를 보기가 너무 쉬워서 이 규칙이 유효성의 전부**다. ⚠️ 저장값은 **비중**이다 — 절대 수량은 종목 크기에 지배돼 feature가 못 된다(삼성전자 150만주 vs 소형주 1만주). 분모는 3주체 매수수량 합(실측 총거래량의 99.2%, 응답 내 자족이라 추가 조회 0). ⚠️ **시장 전체 가집계(FHPTJ04400000)는 폐기** — 시장당 **상위 30종목뿐**(워치리스트의 4%)이고 그마저 순매수 랭킹이라 태깅 자체가 선택 편향이 되며, **잠정치**다(실측 005930 8/21 외국인: 확정 1,567,349 vs 가집계 1,222,000 = 22% 차이). **🔴 결과 — "수급이 뒷받침된 급등은 유지된다"는 반증됐다. 방향이 오히려 반대다**: 강한 순매수(≥2%) net **−0.80%**(n=1,841) ≈ 강한 순매도(<−2%) −0.99%(n=1,637)로 **단조 관계가 없고**, **순매수가 양(+)인 구간의 진입-대조군 edge가 양쪽 반기·양쪽 주체 모두 음수**(외국인 ≥2: −0.10→−0.17 / 0~2: 0→−0.50, 기관 ≥2: +0.03→−0.26 / 0~2: −0.16→−0.21 = **4/4 셀**). 즉 **전일 외국인·기관이 사들인 종목을 우리가 고르면 같은 풀에서 안 고른 것보다 나빴다**. ⚠️ 반대편 발견("약한 순매도 −2~0 구간이 최고", 외국인 edge +1.03·기관 +0.62)은 **시간분할을 통과하지 못한다** — 기관이 전반 +0.84 → 후반 **+0.06**으로 붕괴하고 외국인은 방향은 유지되나 후반 n=69뿐이다. 인접 구간이 뒷받침하지 않는 고립 스파이크 = 8/21에 ATR·업종 후보를 죽인 것과 같은 노이즈 서명. → **매매 경로에 태깅을 붙이지 않았다**(후보당 1콜 비용을 낼 근거 없음). ⚠️ **이 판정 순서가 요점** — 소급이 되니 "붙이고 몇 주 기다렸다가 판정"이 아니라 **"먼저 판정하고 질 것 같으면 안 붙인다"**가 가능했다. 그리고 소급으로 계속 채울 수 있으므로 **라이브 태깅은 영영 불필요**하다(분석 전에 backfill 재실행하면 된다). ⚠️ API 이력이 ~30거래일이라 **창이 매일 뒤로 밀린다** — 소급은 미룰수록 손해(실측 4,016행이 이미 창 밖이라 못 채워졌다). ⚠️ 이 반증은 **누적 증거와 같은 방향**이다(급증 lift 음수·거래량배수 단조 역방향·`WEAK_VOLUME` 대조군 우위·전역 필터 3종 edge 음수) — "**이미 관심을 받은 것을 사는 것**"이 지는 다섯 번째 독립 증거이며, 전략 L(눌림 반전)의 전제와 정합한다. 축 노출: `feature-mining` 외국인순매수%/기관순매수%/수급합%(경계 −2/0/2, 합산은 −3/0/3 — 합산은 컬럼 없이 파생), `outcome-analysis` 노출. 검증 `GET /admin/investor-probe?stockCode=`(raw, 두 API 동시). **+ 정기 재실행 필요(2026-08-24)**: KIS 이력이 ~30거래일이라 **창이 매일 뒤로 밀리므로** 분석 전에 `POST /admin/backfill-investor-flow`를 돌려야 한다. 8/24 실행 실측 **281종목 콜 → 948행 태깅, 41초, 실패 0** (전일 이전은 이미 태깅돼 스킵). 현재 커버리지 진입 85.4% / 대조군 90.1%이고, 남은 결손 **3,985행은 7/12 이전이라 창 밖 = 영구 복구 불가**. 같은 이유로 뉴스도 `POST /admin/backfill-news?force=false`를 함께 돌린다(8/24 실측 280콜·948행·37초 — `force=false`면 `entry_news_basis_date`가 null인 행만 처리하므로 **라이브 태깅분만 정확히 덮어써 방법이 통일된다**. 34k행 전체를 다시 도는 `force=true`는 불필요). 결과 뉴스 커버리지 진입 90.7% / 대조군 92.9%로 **진입-대조군 대칭** 유지. ⚠️ **장중에 오늘 행이 갱신되는지는 미확인**(토요일 배포라 최신 행이 금요일) — 갱신된다면 당일 촉매 feature 경로가 별도로 열린다. 월요일 장중 `investor-probe`로 판정할 것.
- **주문 추격 비용 분석**(`OrderChaseAnalysisService`, `GET /api/v1/admin/order-chase?since=`, 2026-08-27, **측정 먼저**): 미체결 주문은 `unfilled-timeout-minutes`(3분) 경과 시 취소되고 멱등 판정이 상태인식이라 **다음 틱에 현재가로 재주문**된다(=가격 추격). 의도된 동작이지만 **비용이 한 번도 측정된 적이 없었다**. 계기는 2026-08-27 112040 실측 — 매도가 **15,170 → 15,130 → 15,120 → 15,120 → 15,110**으로 5회 시도·4회 취소 끝에 체결됐다(하락 중 종목이라 호가를 따라 내려가는 **계단식 하향 추격**). 첫 호가 대비 **−0.40%(60원)** 불리, 보유시간도 **90분 → 106분**. 매수는 거울상(상승 중 추격 매수). ⚠️ **소급 가능 — 신규 태깅 0**: 같은 `idempotencyKey`를 공유하는 행이 곧 한 체인이고(취소분은 부분 유니크 인덱스에서 빠져 여러 행이 남는다) 첫 시도가·최종 체결가가 모두 저장돼 있다. ⚠️ **부호 규약: 음수가 불리**(매도=더 싸게 팔림, 매수=더 비싸게 삼) — 두 side의 추격 방향이 반대라 정규화해야 같은 축에서 읽힌다. (side, 전략)별로 분리 집계. 미체결·전량실패 체인은 비용 미확정이라 제외. 판정 `toChase`(순수 정적), 테스트 `OrderChaseAnalysisTest` 6. **🔴 첫 실측 결과 — "추격이 비용을 낸다"는 가설이 표본 전체로는 기각됐다(배포 즉시)**: SELL **67체인 가중평균 drift +0.031%** = 사실상 0이다. 계기였던 112040(−0.40%)은 **꼬리 사례**였고, 전략별로도 B **+0.57%** · D **+0.29%**(체인 31로 최다) · I +0.09%처럼 오히려 양수가 여럿이다. 음수는 E −1.39(n=1) · G −0.56(3) · H −0.49(9) · K −0.43(2) · F −0.42(4) · L −0.40(1)로 **전부 표본 한 자리**. → **조치 불요**. ⚠️ **BUY 체인은 0건** — 매수 미체결은 `settleCancelled`로 종결되고 멱등키가 `전략:종목:일자`라 같은 날 재주문이 안 생긴다(추격은 매도 전용 현상). ⚠️ H의 "평균 시도 48회·지연 600분"은 **2026-07-16 휴장일 사건 하나**(`SELL:220` 411회)가 만든 것으로, 컷오프·휴장일 가드로 **이미 수정된 과거 흔적**이다 — 그 체인을 빼면 전 체인이 2~5회다. ⚠️ **교훈: 소급 가능한 축은 "붙이고 기다린다"가 아니라 "먼저 재고 아니면 버린다"** — 수급 태깅(8/22)과 같은 패턴으로, 1건 일화에서 출발한 가설을 배포 당일 비용 0으로 죽였다.
- **집행품질 분석**(`ExecutionQualityService`, `GET /api/v1/admin/execution-quality`, 2026-07-09): **실제 LIVE 청산완료 매수**(`trade_order`, mode=LIVE)를 **같은 (전략·종목·진입일) 섀도우 진입분**(`trade_outcome`, control 제외)과 대조 → 분석집계(섀도우만 읽음)와 실매매의 **괴리를 정량화**. 전략별 `realNet`(실현손익%/매수체결 − 왕복비용, 슬리피지는 실체결가에 이미 반영돼 재차감 안 함) vs `shadowNet`(섀도우 (마크−신호)/신호 − 왕복비용 − 추정슬리피지) vs **`gap`(=real−shadow, 음수면 집행 드래그)** + `entrySlip`((실매수−섀도우신호)/신호). **horizon은 perf-gate와 동일 정렬**: 인트라데이=`exit`(전략별 권장 청산마크 ±30분 근접, `StrategyHoldTimeProvider`+`OutcomeSample` — 게이트와 같은 로직), 스윙=`swing-horizon`(nextClose). → gap이 "종가보유 대비"가 아니라 **"게이트가 검증한 청산시점 대비"** = 순수 집행품질(진입슬리피지+체결시점). exit 마크 미수집 섀도우는 대조 제외(gap=null). DRY_RUN은 실슬리피지 없어 제외(순수 LIVE만). 단위테스트 `ExecutionQualityServiceTest` 6. 🐞 **비-TIME 청산방식에서 horizon이 실제 청산과 무관하던 버그(2026-08-21 수정)**: 게이트가 2026-08-18에 받은 보정(`ExitMethodProvider.methodFor(strategy).type()`이 TIME이 아니면 horizon을 `close`로)이 **여기엔 안 들어가 있었다** — D(TRAILING 3%)를 **실제로 거의 팔지 않는 20분 마크**의 섀도우와 대조해 **gap +0.18%p("괴리 미미")**로 보고했다. 수정 후 같은 데이터가 **shadow +1.07% vs real −0.11% = gap −1.18%p(집행 드래그)**로 뒤집혔다(n=90) — B(−1.21%p) 다음으로 큰 드래그이며, **D의 트레일링 조기 트리거가 승자를 잘라내고 있다**는 신호(라이브 D 보유는 1~10분과 350~374분의 양봉 분포). ⚠️ 교훈: **horizon 규칙은 게이트·집행품질·대조군 분석이 같이 움직여야 한다** — 한 곳만 고치면 나머지가 조용히 반대 결론을 낸다. 라벨에 `close·트레일링청산` 표기. `ExitMethodProvider`는 필드주입(생성자 무churn, 미주입 테스트는 종전 동작). 테스트 2(비TIME=종가/TIME=종전 마크). (실측 2026-07-09: B entrySlip 0·gap +0.44%p·매칭 11/11 — 집행 드래그 없음, "전략+실집행−" 괴리 아님. 오늘 B 손실은 하락장 롱 신호 자체 손실이고 섀도우도 −2.49 동반.)
- **순수익(net) 기준 분석**: `exit-comparison`/`exit-timing`/`outcome-analysis` + 성과게이트의 평균수익률·승률은 **왕복 매매비용**(`stockadvisor.cost.round-trip-pct`, 기본 **0.22%** — 2026-07-13 실계좌 대사로 0.18→0.22 상향: 실측 수수료 0.0397%×2 + 매도 거래세 0.15% ≈ 매수금액 대비 0.22%. env `TRADING_ROUND_TRIP_COST_PCT`, compose 패스스루 완료) **+ 추정 슬리피지**(레이어 4 `ExecutionCostModel`, 매수가 tick 기반)를 차감한 순수익. **승/패 분류도 net>0** 기준. ⚠️ **슬리피지는 이제 반영됨**(레이어 4) — 호가단위 스프레드 추정(소형주는 `spread-ticks` 상향 필요). `ExitStrategyService`의 MFE/MAE는 잠재 변동폭이라 비용 미차감(gross).
- ⚠️ **포워드 테스트는 표본 누적에 수 주 소요**. 임계값(B의 ±1%, C의 -3% 등)은 예시값(`SIGNAL_B_*`/`SIGNAL_C_*`/`SIGNAL_INTRADAY_*` env). 슬리피지는 레이어 4에서 반영됨(tick 기반 추정).
- entry 재평가 창 `observation-window` 기본 1d→**1h**(진입은 공시 직후가 현실적, 호출량 절감).

### 매수가정 가격추적 후속알림 (`TradeFollowUpService`)
- 알림 발송 시 `DisclosureAlertProcessor`가 `TradeOutcome`(매수가=신호 시점 종가)을 기록. 1분 주기 `trackFollowUps()`가 알림 후 **+5분/+10분 가격을 샘플링**하고 **+10분에 변동률 포함 후속 Discord 1회 발송**.
- 후속용 가격은 `KisApiClient.fetchLatestClose`(비캐시)로 조회 — 1h 캐시 quote를 쓰면 +5/+10이 같은 값이 되므로 주의.
- 지연은 `stockadvisor.followup.first-delay`(5m)/`second-delay`(10m)로 조정. 검증: 두산 매수가 1,605,000 → +5/+10분 1,600,000(-0.31%) 후속 발송 확인.
- ⚠️ 워치리스트 동기화는 ~20분 장기 실행 → `spring.task.scheduling.pool.size=3`으로 폴링/평가가 막히지 않게 함. (기본 단일 스레드면 동기화 중 다른 스케줄이 블록됨)
- 핵심 빈: `DisclosurePollingService` → `MarketSignalService` → `DisclosureAlertProcessor`(공시별 **REQUIRES_NEW** 독립 트랜잭션) → `DiscordNotifier`.
- 신호 임계값은 `SIGNAL_*` 환경변수로 override 가능(테스트 시 강제 트리거에 사용). Discord `webhook-url` 비면 **dry-run(로그만)**.
- **감시 범위**: `company` 테이블 종목(watchlist). **코스피 1000 + 코스닥 500 = 1500종목 적재 완료** (`WatchlistSyncService`, Phase 2).
- **Discord 실연동 완료**: `DISCORD_WEBHOOK_URL`(set-env.sh) 설정됨 → 실제 채널 발송 e2e 검증 완료(SK하이닉스 강제 트리거 1건).
- ⚠️ KIS 초당 20건(TPS) 제한 → `KisApiClient`에 **전역 속도 제한기**(`rateGate`, `MIN_INTERVAL_MS=120` → 모든 KIS 호출 직렬화 ~8건/초) + EGW00201 선형 백오프 재시도. 워치리스트 스캔(대량) + 후속추적 + 신호평가가 동시에 KIS를 호출해도 경합으로 인한 초당제한 초과를 방지. (이전엔 스캔이 KIS를 독점해 B/C 후속추적 호출이 실패 → 후속알림 누락. 전역 게이트로 2배 과부하에서도 후속실패 0 확인). 노이즈 많으면 MIN_INTERVAL 상향 또는 스캔 주기 하향.

### 워치리스트 동기화 (`WatchlistSyncService`, Phase 2)
- 소스: KRX가 이 환경에서 **차단**(OTP LOGOUT)되어, **DART `corpCode.xml`(전 상장 ~3,971종목) + KIS 현재가**로 대체. KIS 현재가의 `rprs_mrkt_kor_name`(시장 분류: KOSPI/KSQ)·`hts_avls`(시총)로 시장별 시총 상위 N 선정.
- 트리거: 정기 스케줄(`watchlist-sync-cron`, 기본 평일 16:00) 또는 수동 `POST /api/v1/admin/sync-watchlist?kospiTop=1000&kosdaqTop=500&limit=`. **전 종목 순회라 ~20분(시세 캐시 없을 때)** 소요. `saveAll`+reconcile은 마지막 1회.
- **reconcile**: 전체 실행(limit 미지정)일 때만 선정 목록에 없는 종목을 삭제(`deleteByStockCodeNotIn`). limit 지정(테스트) 실행은 reconcile 생략. 전면 실패로 selected가 비면 삭제 안 함(워치리스트 전체 삭제 방지). 검증: 1501→1500(removed=1).
- ⚠️ **알려진 한계**: 코스닥 신형 **영문 종목코드**(예: `0001A0`)가 적재되는데, `RecommendationController`의 `@Pattern("\\d{6}")`은 숫자만 허용 → 해당 종목 추천 API 직접 조회 시 400. 알림 파이프라인 내부 경로는 영향 없음.

### 실전 매매 안전장치 (Phase 0 — PolicyGate 스캐폴딩, **본인 계좌 자기매매**)
- 방향: 모의투자(VTS) **건너뛰고** 소액 실전. 단 VTS가 주던 "주문코드 버그 무료 검출"을 ① 실계좌 dry-run(조회만 실계좌, 주문은 로깅) ② 코드 강제 하드캡 ③ 첫 N건 수동승인으로 대체. 컴플라이언스: 본인 자산 자기매매라 라이선스 불요.
- **`PolicyGate`**(`service/PolicyGate`): 신호→주문 사이에서 모든 진입을 검증하는 단일 안전 게이트. `evaluate(OrderRequest, today)` → `PolicyDecision(allowed, reason)`. 검증 순서: ⓪ 마스터 스위치(`enabled`)/런타임 킬스위치 → ① 멱등성(중복주문, 매수·매도 공통) → (매도는 리스크 축소라 이하 면제·허용) → ② **1주문 금액 상한**(`req.maxKrw`=순자산×maxOrderPct, 수량/가격 버그 최후 방어막) → ③ **시간기반 청산 진입 규칙**(진입시각+`timeExitHoldMinutes` ≤ `sessionEnd`일 때만; 청산이 장마감 넘기면 금지) → ④ 1일 주문 수 한도(0이면 무제한) → ⑤ 최대 동시 보유 → ⑥ 일일 손실 한도. **단위테스트 `PolicyGateTest` 12케이스**.
- 설정 `stockadvisor.trading`(`TradingPolicyProperties`): ⚠️ **기본 `enabled=false` + `mode=DRY_RUN`** — 명시 활성화 전엔 실주문 0. 디폴트: **`max-order-pct=0`=자동(100/max-positions, 2026-07-13 — `TradingPolicyProperties.effectiveMaxOrderPct()`, >0이면 고정%)/**`max-orders-per-day=0`**(무제한)/`daily-loss-limit=50000`(⚠️ **2026-07-24 순자산 비율 모드 추가**: `daily-loss-limit-pct`>0이면 순자산×pct%를 한도로 — 순자산은 maxKrw÷effectiveMaxOrderPct 역산, maxKrw 미가용 시 절대액 fallback. **prod=10%** env `TRADING_DAILY_LOSS_LIMIT_PCT`. 절대액도 5만→50만 상향. 인버스는 한도 자체 면제. 테스트 `일일손실한도_순자산비율_모드`)/**`max-positions=20`**(10→20, 종목당 자동 5%)/`session-end=15:20`/`time-exit-hold-minutes=60`/`manual-confirm=true`/`live-strategies=`(빈값). env `TRADING_*` 오버라이드. (1주문 절대금액→비율, 강제청산시각→시간기반 진입규칙으로 변경됨.)
  - ⚠️ **prod 실제 설정(2026-07-31 확인)**: `TRADING_MODE=LIVE`·`TRADING_ENABLED=true`·`TRADING_MANUAL_CONFIRM=false`·`TRADING_PERF_GATE_ENABLED=true` + **`TRADING_LIVE_STRATEGIES`=11개 전략 전부**(`MOMENTUM_A,VOLUME_LEADING_B,MEAN_REVERSION_C,INDEX_RELATIVE_D,BREAKOUT_E,MA_TREND_F,RSI_REVERSAL_G,SQUEEZE_BREAKOUT_H,INVERSE_INDEX_I,VALUE_REVERSAL_J,OPENING_GAP_K`). **⚠️ 2026-08-25 현행화 — B·F·H(8/21)·E(8/24)에 이어 C도 제외돼 6개**(`MOMENTUM_A,INDEX_RELATIVE_D,RSI_REVERSAL_G,INVERSE_INDEX_I,VALUE_REVERSAL_J,OPENING_GAP_K`). 직전 8/24 상태는 7개(`MOMENTUM_A,MEAN_REVERSION_C,INDEX_RELATIVE_D,RSI_REVERSAL_G,INVERSE_INDEX_I,VALUE_REVERSAL_J,OPENING_GAP_K`). 직전 8/21 상태는 8개(`MOMENTUM_A,MEAN_REVERSION_C,INDEX_RELATIVE_D,BREAKOUT_E,RSI_REVERSAL_G,INVERSE_INDEX_I,VALUE_REVERSAL_J,OPENING_GAP_K`. B는 아래 B 항목, F·H는 아래 **F·H 화이트리스트 제외** 항목 참조). 즉 **코드 기본값(빈 화이트리스트)과 달리 prod는 전 전략이 LIVE 실주문 가능**하며 **perf-gate가 유일한 실주문 판정자**(국면·흐름 버킷 net 검증 통과 전략만 실제 진입). 따라서 아래 D·E·I·F/G/H/J/K 서술의 "화이트리스트 미포함/실주문 0/순수 검증 모드"는 **코드 기본값 기준 설명이며 현행 prod에는 해당 안 됨**(전부 화이트리스트 편입, alerts()=false여도 LIVE 주문 접수 시 신호 알림 강제 규칙 적용). 실측: 2026-07-31 오전 D·G·C가 LIVE 라운드트립 실매매 수행(D 스캘핑 다수, 실현 +22,288원).
- `Order` 엔티티(`trade_order`, 예약어 회피) + `OrderRepository`: idempotencyKey unique, `OrderSide`/`OrderStatus`/`TradingMode` enum(STRING). 집계 쿼리 `countByOrderDateAndSide`/`countOpenPositions`(미청산 매수)/`sumRealizedPnlByDate`. prod ddl-auto update로 테이블 자동 생성.
- **`OrderService`**(`service/OrderService`): 주문 실행 진입점. `submit(OrderCommand)` → `PolicyGate.evaluate()` → 거부면 기록 없이 `REJECTED`, 통과면 `Order` 생성 후 **`mode=DRY_RUN`이면 전송 없이 기록만(흐름 검증)**, `mode=LIVE`면 KIS 주문 전송. ⚠️ **LIVE 경로는 KIS 주문 클라이언트(계좌 연동) 전까지 안전하게 실패 처리** — 미검증 실주문 차단. `OrderResult(status: DRY_RUN|SUBMITTED|REJECTED|FAILED)`. 단위테스트 `OrderServiceTest` 3케이스(거부 시 미기록 / DRY_RUN 1회 기록 / LIVE 미구현 실패).
- **모의투자(VTS) 스킵 확정** — 새 키 발급 없이 기존 실전 키 사용. 주문·잔고 API는 **계좌번호 필수**(시세는 불요): `KisProperties.accountNumber`(CANO 8자리)+`accountProductCode`(상품코드 2자리, env `KIS_ACCOUNT_NO`/`KIS_ACCOUNT_PRODUCT_CODE`). dry-run엔 계좌번호 불요(전송 안 하므로).
- **잔고·예수금 조회**(①, 완료): `KisApiClient.fetchBalance()` — 주식잔고조회(`inquire-balance`, TR `TTTC8434R`, 계좌번호 필수). 전역 rateGate/토큰 공유. `GET /api/v1/admin/account-balance` → 보유종목(output1)+계좌요약(output2: 예수금총금액 `dnca_tot_amt`, D+2예수금 `prvs_rcdl_excc_amt`, 총평가·순자산·평가손익). **주문 없이 계좌 API 권한 검증용**(권한 없으면 rt_cd≠0).
- **매수/매도 주문**(②, 완료): `KisApiClient.orderCash(stockCode, side, qty, price, ordDvsn)` — 현금주문(`order-cash`, 실전 매수 `TTTC0802U`/매도 `TTTC0801U`). POST 본문은 `hashkey(body)`(/uapi/hashkey) 발급 후 `hashkey` 헤더로 전송. GET/POST 공통 재시도·rateGate는 `execute()`로 추출. rt_cd≠0(거부)는 응답 반환(호출측 판정), 네트워크오류는 예외. `OrderService` LIVE 경로가 이를 호출 → 접수성공(rt_cd=0)이면 `SUBMITTED`+`Order.brokerOrderNo`(ODNO) 기록, 거부면 `FAILED`. **기본 지정가(`ORD_DVSN="00"`, 슬리피지 통제)**, 시장가는 추후.
- **수동 주문 트리거**: `POST /api/v1/admin/test-order?stockCode=&side=BUY&qty=1&price=` — OrderService 통해 정책·모드 그대로 적용(enabled=false면 거부, DRY_RUN이면 기록만, LIVE면 실주문). dry-run 흐름 검증 + 수동승인 소액 실전 도구. 단위테스트 `OrderServiceTest` 4케이스(거부 미기록 / DRY_RUN 기록 / LIVE 접수성공 SUBMITTED / LIVE 거부 FAILED).
- **신호 흐름 연결**(③, 완료): `StrategyEvaluator.evaluateStock`이 진입 전략마다 가상매수 기록 직후 `OrderService.submitEntry(strategy, stockCode, price, idem)` 호출(A/B/C 공통 경로). idem=`strategy:stockCode:alertDate`(종목당 1회와 정합). **주문 실패는 try/catch로 격리**해 신호 처리/가상매수/알림을 절대 안 깨뜨림. `submit`은 `REQUIRES_NEW`라 주문 트랜잭션이 호출자와 분리.
  - **포지션 사이징**: `submitEntry`가 잔고조회로 순자산을 받아 `cap = 순자산 × maxOrderPct(10%)`, `qty = floor(cap / price)`. 1주도 못 사면 스킵. cap을 `OrderCommand.maxKrw`로 실어 PolicyGate가 동일값으로 재검증. 잔고조회 실패 시 사이징 불가로 거부. 수동주문은 `submitManual`(동일 사이징).
  - **같은-계열주 가드(2026-07-24, HLB제약·이노베이션·생명과학 3종 동반붕괴 −75,214원 계기)**: 그룹명이 같은 종목들은 동일 뉴스로 동반 급등락(사실상 한 베팅)인데 섹터 한도(3)로 못 막음 → `submitEntry`가 회사명에서 **그룹 키(`groupKeyOf` — 선행 영문 그룹명 HLB/SK/LG… 또는 한글 앞 2자 삼성/현대…)**를 뽑아 같은 키 활성 포지션 ≥ `max-positions-per-group`(1)이면 실주문 스킵(섀도우 무관). ⚠️ 휴리스틱 오탐 가능("대한항공/대한해운") — 비용이 진입 스킵뿐이라 보수 수용. env `TRADING_MAX_POSITIONS_PER_GROUP`(0=비활성, compose 패스스루). **+ 전략별 동시 보유 상한(2026-08-25)**: 기존 가드(종목당 1·계열당 1·업종당 3)는 전부 "**무엇을** 사느냐" 축이고 "**누가** 사느냐" 축이 비어 있었다 — 슬롯이 선착순이라 신호량이 많은 전략이 리스크 예산을 통째로 가져간다. L 편입 시 실측 계산: 순자산 1,054만 · 부트스트랩 ×0.5(2.5%/종목) · BEAR 노출상한 30% → **L 혼자 ~12종목 = 예산 전액**. → `trading.max-positions-per-strategy`(csv `"STRATEGY:N"`, **미지정 전략은 무제한 = 종전 동작**), prod `REVERSAL_L:5`. 판정 `OrderRepository.countActivePositionsByStrategy`(상태 조건은 종목당 가드와 동일). ⚠️ **판정 위치는 잔고조회 앞** — 어차피 막힐 진입에 KIS 호출을 쓰지 않는다(같은-종목·계열 가드와 동일). ⚠️ 섀도우(`TradeOutcome`) 기록은 이 가드와 무관하다. ⚠️ csv 오타·0·음수는 조용히 무시(degrade open — 설정 실수로 진입이 전면 차단되는 것보다 낫다). ⚠️ **동시 보유 상한이지 일일 진입 상한이 아니다** — 청산되면 슬롯이 비어 다시 채워진다. env `TRADING_MAX_POSITIONS_PER_STRATEGY`(compose 패스스루). 테스트 3. ⚠️ **첫 거래일(2026-08-26) 실측 — 상한은 한 번도 물리지 않았다**: 그날 LIVE 진입 9건이 전부 L이었는데 동시 보유 최대 4였다. 게이트가 L 후보 17건 중 **8건을 먼저 차단**해(장중 국면 라벨이 중립/강세 → **약세** → 중립/강세로 두 번 뒤집히면서, 약세 구간에선 흐름버킷 n≥30 엄격 경로로 넘어가 LOO가 막았다) 슬롯 경쟁 자체가 안 생겼다. → **상한 로직은 아직 미검증**이며, 다음에 게이트가 활짝 열린 날에야 검증된다. **⚠️ 현재 prod는 OFF(=0, 사용자 결정 7/24 — 오탐 우려 우선, HLB형 재발 시 활성화 검토)** — 코드만 배포된 상태, 켜려면 .env를 1로. 테스트 `계열_그룹키_추출`.
  - **같은-종목 크로스전략 가드**(`submitEntry`, 항상 on): 전략이 여러 개라 A·B·C·D가 **같은 종목을 각각 매수해 한 종목에 노출이 스태킹**되는 것을 방지 — 진입 전 `OrderRepository.countActivePositionsByStockCode`(미청산 BUY: 승인대기/접수/체결)로 **이미 활성 포지션이 있으면 전략 무관 스킵**(종목당 실포지션 1개). 잔고/사이징 호출 전에 조기 차단. **각 전략의 가상매수(`TradeOutcome`) 기록은 이 가드와 무관하게 남아 섀도우 성과는 영향 없음.** DRY_RUN/LIVE 공통(포지션 관리 흐름 dry-run 시뮬). ⚠️ 멱등키는 `전략:종목:일자`라 크로스전략 스태킹을 막지 못해 이 가드가 필요. 순차 재진입(청산 후)은 허용. 단위테스트 `OrderServiceTest`(같은종목 중복진입 스킵).
  - 🐞 **`countOpenPositions` 죽은주문 오집계 버그(2026-07-03 인버스 실거래 테스트에서 발견·수정)**: 최대 동시보유 한도용 `countOpenPositions`/`countOpenPositionsBySector`가 `side=BUY and closed=false`만 세어, **REJECTED/FAILED/CANCELLED(체결된 적 없는 죽은 주문)까지 포지션으로 오집계**. 실패·거부 주문 10건이 쌓여 max-positions(10)를 채우자 **실계좌는 flat인데 모든 신규 실주문이 "최대 동시보유 도달"로 차단**됐음(검증된 전략도 막힘 — 사일런트 전면 차단). 수정: 두 쿼리에 `status not in (REJECTED,FAILED,CANCELLED)` 추가. 기존 죽은 행은 `closed=true`로 정리. ⚠️ mock 기반 단위테스트로는 못 잡음(@Query 필터 이슈) — 실거래로 발견.
  - **LIVE 전략 화이트리스트**(`trading.live-strategies`, env `TRADING_LIVE_STRATEGIES` csv, 기본 빈값): LIVE에서 목록에 없는 전략은 실주문 차단("검증 안 된 전략 실전 금지"). **DRY_RUN은 관찰용이라 전 전략 기록**(화이트리스트 무시). 수동 `test-order`는 `submit()` 직접 호출이라 화이트리스트 무관(항상 허용).
  - **전략 성과 게이트**(`StrategyPerformanceGate`, `trading.perf-gate`, 화이트리스트의 동적·자동 보강): A/B/C는 선택이 아니라 **셋 다 동시 실행**되는데, LIVE 실주문만 **전략별 최근 성과로 자동 on/off**한다. `submitEntry`가 화이트리스트 통과 후 LIVE일 때 `evaluate(strategy)` 호출 → 전략별 최근 `lookback-days`(기본 20)일 `TradeOutcome`의 **net 평균수익률**(왕복비용 차감)이 `min-net-avg-pct`(기본 **0.3**) 미만이거나 표본 `min-samples`(기본 **30**) 미만이면 **실주문만 차단**(REJECTED). **INVERSE 버킷은 전용 `inverse-min-samples`(기본 10, 2026-07-13 인버스 승격 경로)** — 인버스 표본은 폭락일에만 쌓여 30 도달이 비현실적이라 표본 요건만 하향(net 기준 동일). env `TRADING_PERF_GATE_INVERSE_MIN_SAMPLES`. **INVERSE 전용 net 문턱(2026-07-24, 사용자 결정)**: 인버스는 헤지 다리(폭락일 커버리지)라 알파 문턱(공통 min-net 0.3%)이 과함 → `perf-gate.inverse-min-net-avg-pct`(≤0이면 공통값 상속) 추가, **prod=0.2%**(net이라 비용 차감 후 흑자 기준은 유지). env `TRADING_PERF_GATE_INVERSE_MIN_NET`(compose 패스스루). **INVERSE 버킷 전략 무관 통합(2026-08-20)**: 인버스 손익은 어느 전략이 신호를 냈느냐가 아니라 **공통 청산 로직**(`trading.inverse-exit`)이 결정하는데 버킷만 전략별로 쪼개져 있었다 — 8/20 실측: **I가 실현 net −0.52%(n=10)로 닫혔는데 나머지 10개 전략(A·B·C·D·E·F·G·H·J·K)은 각자 '실현표본 0/10'이라 부트스트랩으로 열린 채** 같은 인버스 매매를 계속 낼 수 있었다. → `perf-gate.inverse-pooled`(기본 true)로 **INVERSE 실현표본을 전략 무관 단일 풀로 채점** — 한 번 쌓인 인버스 실적이 전 경로에 즉시 반영된다. 사유문에 `[INVERSE·실현손익·통합]`로 표기. env `TRADING_PERF_GATE_INVERSE_POOLED`(false=전략별, compose 패스스루). 테스트 1. ⚠️ 인버스 **진입** 조건은 전략마다 다르다(I만 `InverseIndexStrategy` 판정, B는 `SIGNAL_B_INVERSE_REQUIRE_INDEX`로 위임, 나머지는 각자 모멘텀 로직) — 통합된 건 **채점**뿐이다. **INVERSE 버킷 채점은 실현손익(2026-07-15)**: 인버스 실제 청산이 지수 기반(반등/회복, 수분 단위)이라 고정 보유마크 시뮬과 어긋남("60분 마크 채점 vs 4분 실청산") → INVERSE 버킷은 `evaluateInverseRealized`가 **LIVE 청산완료 실현손익(net%, trade_order — 부트스트랩이 전 진입을 실주문화)**으로 직접 채점. 표본=실현 청산 건수(섀도우 행 아님 — I 섀도우 9건→실현 1건으로 정직 리셋), 임계(inverseMinSamples/부트스트랩/minNet)는 동일. 채점과 청산 규칙이 정의상 일치. OrderRepository 필드주입(미주입 테스트는 기존 마크 채점 fallback). 게이트 테스트 3(통과/졸업차단/부트스트랩유지). **+ 로직 세대 교체 리셋(2026-07-20)**: 7/16 휩쏘(구 청산로직) 실현 11표본이 개선판(mom30·낙폭비례·재진입 AND)을 룩백 만료(8월초)까지 영구 차단하는 캐치22 → `perf-gate.inverse-realized-since`(yyyyMMdd, 빈값=제한없음)로 **INVERSE 버킷 실현 채점 시작일** 지정, 구표본 제외 → 0/10 부트스트랩 재개. prod env `TRADING_PERF_GATE_INVERSE_REALIZED_SINCE`(compose 패스스루) — 이력: `20260717` → `20260819`(저점대비 레벨회복 도입) → **`20260821`(2026-08-21, fade 확인 요건·사이클 상한 도입 반영, 사용자 결정)**. ⚠️ 향후 인버스 진입/청산 로직을 또 바꾸면 이 날짜도 갱신 검토. ⚠️ **갱신하지 않으면 인버스가 자력으로 못 열리는 catch-22**가 생긴다 — INVERSE 버킷은 **실현손익으로만** 채점되는데(섀도우 무관) LIVE가 닫히면 새 실현표본이 안 쌓여, 룩백(20일)이 굴러 구표본이 만료될 때까지 정지가 이어진다(8/21 실측: 갱신 안 했으면 8/19~20의 11건 때문에 **9/9경까지 약 2.5주 정지**였을 상황). ⚠️ 리셋의 대가는 **실패 표본을 채점에서 버리는 것** — 새 로직이 구로직과 똑같이 나빠도 그걸 알기까지 다시 `inverse-min-samples`(10)건의 실거래 손실이 필요하다. 테스트 `신로직_시작일_이전_실현표본은_채점_제외되어_부트스트랩_재개`. **+ 인버스 부트스트랩**(`inverse-bootstrap-size-mult` 기본 0.3, 같은 날 추가): 표본이 10 미만이어도 **일반 비중의 30%(≈순자산 1.5%/종목) 축소사이징으로 실주문 허용** — 적은 비용으로 실표본 수집. 표본 10 도달 시 자동 졸업: net ≥ 0.3%면 정상 사이징(제한 해제), 미달이면 차단(**부트스트랩으로 성과 미달 우회 불가**). 노출 상한은 크로스전략 가드(인버스 코드당 실포지션 1)로 최대 2코드×1.5%=3%. 0=비활성. env `TRADING_PERF_GATE_INVERSE_BOOTSTRAP_SIZE_MULT`. 단위테스트 3(허용/비활성차단/졸업 후 성과미달 차단).
    - **⚠️ horizon=`exit`(기본, 실제 청산시점 측정)**: 게이트 net을 당일종가가 아니라 **전략이 실제로 청산하는 시점 가격**으로 측정 — 인트라데이는 `StrategyHoldTimeProvider.holdMinutes(strategy)`(권장 보유 마크, PositionExitService가 쓰는 그 값)의 `OutcomeSample` 가격, **스윙(C)은 nextClose**(D+1, 실제 청산). "45분에 팔 건데 종가로 채점"하던 불일치 해소 → **실질 수익으로 검증**. exit 마크 미수집 표본은 제외(fail-closed). ⚠️ 권장 마크는 ExitTiming이 평균net 최대로 고른 시점이라 **낙관 편향(이중딥)** → 이를 상쇄하려 **min-samples 20→30·min-net 0→0.3 상향**(함께 도입). 🐞 **비-TIME 청산방식에서 horizon이 실제 청산과 무관하던 버그(2026-08-18 수정)**: exit horizon은 '보유시간 마크'라 **채택 청산방식이 TIME일 때만** 실제 청산 시점과 일치한다. TRAILING/VWAP/추세전환은 시간 상한이 없어 트리거 or 장마감까지 가는데도 마크로 채점됐다 — D 실측: 게이트는 65분 마크로 채점하는데 **라이브 보유는 1~10분(트레일 조기 트리거) 26건과 350~374분(장마감) 40건의 양봉 분포**였고 65분 부근은 사실상 비어 있었다(= 게이트가 **검증한 적 없는 청산**으로 실주문을 열어줌). 수정: `ExitMethodProvider.methodFor(strategy).type()`이 TIME이 아니면 **horizon을 `close`로 낮춰 채점**(장마감 보유가 지배적이라 실제에 가깝고, 마크 max-pick 낙관 편향도 함께 제거돼 보수적). 사유 라벨에 `close·트레일링청산` 표기. 필드주입(생성자 무churn, 미주입 테스트는 종전 동작). 테스트 2(비TIME=종가 채점 / TIME=종전 마크 채점). `TRADING_PERF_GATE_HORIZON=close`로 되돌리면 종전(당일종가) 방식. 단위테스트 `exit_horizon은_당일종가가_아니라_권장청산마크_실제가로_측정`. **표본 부족=미검증으로 보고 fail-closed**(그림자 표본은 LIVE 차단 중에도 `StrategyEvaluator`가 가상매수·알림을 게이트와 무관하게 먼저 기록하므로 계속 쌓임 → 회복 시 자동 재가동). DRY_RUN·가상매수·알림·수동 `test-order`·매도는 영향 없음. env `TRADING_PERF_GATE_*`, 가시화 `GET /api/v1/admin/strategy-gate`(전략별 허용여부·표본수·net평균·적용국면). 단위테스트 `StrategyPerformanceGateTest` 8 + `OrderServiceTest` 게이트 차단 케이스.
    - **레이어 2.5 — 국면+흐름 조건부(3차원, 2026-07-15)**: `flow-conditional`(기본 true) — 현재 (시장,국면,**장중 흐름부호** mom30≥0/미만) 버킷 표본이 `flow-min-samples`(30) 이상이면 **그 버킷만으로 판정**(가장 정밀), 미만이면 국면 버킷으로 자연 fallback(기존 동작), 흐름 미산출(개장 ~30분·조회실패)이면 자동으로 국면만. 실측 근거: 흐름 엣지는 국면 내부에서 갈림(H 중립·흐름↑ +0.94 vs 흐름↓ −0.37). 사유 라벨 `[시장·국면·h·흐름↑/↓] 흐름버킷 통과/성과 미달`. 현재 흐름은 `MarketRegimeService.intradayFlow`(90s 캐시) mom30 부호. 데이터 공백 셀(BULL 전체 등)은 표본 미달로 자동 국면-fallback — 강세장 도래 시 표본이 차면 자동 활성. env `TRADING_PERF_GATE_FLOW_CONDITIONAL`/`FLOW_MIN_SAMPLES`. 게이트 테스트 3(흐름차단/흐름통과/부족시 국면fallback). 배포 직후 실측: G·B 코스피가 흐름↓ 버킷으로 판정됨.
    - **전략별 net 재검증 `strategy-since`(2026-08-12 배포, 2026-08-14 최초 활성화)**: 진입/청산 로직·임계를 바꾸면 구표본은 **옛 로직의 성과**라 새 로직 채점에 섞이면 안 됨 → `perf-gate.strategy-since`(csv `"STRATEGY:yyyyMMdd,.."`)로 전략별 재검증 시작일 지정, 그 이전 표본은 채점 제외. **정밀판이 `GateRefilter`**(`perf-gate.refilter`) — **조이는 필터** 추가 시 구표본을 전부 버리는 대신 새 임계를 구표본에 재적용해 "새 로직이라면 여전히 진입했을 표본"만 남김(fail-closed 공백 없이 유효 구표본 보존). **+ 전역 규칙 `*`(2026-08-18)**: 전략 자리에 `*`를 쓰면 모든 전략에 적용되고, 전략별 규칙과는 **AND**로 합쳐진다(`GateRefilter.forStrategy`). 전략 무관 진입 필터(위 전역 필터 3종)를 11개 전략에 일일이 나열하지 않아도 되고, 무엇보다 **since 리셋의 fail-closed 공백 없이** 구표본을 새 임계로 재채점한다. prod 적용: `*:change_rate<10;volume_ratio<15;market_cap>=1000`. ⚠️ 호출측은 반드시 `forStrategy`를 쓸 것 — `map.get(strategy)`만 보면 전역 규칙을 놓친다. 문법 `STRATEGY:feat OP val;..|..`, feature는 **persisted 태그만**(volume_ratio·rec_score·ret5d·exec_strength·per·pbr·atr·dist_high·mom30·breadth·market_cap·change_rate) — 분봉 신선도처럼 transient는 재필터 불가라 since 사용. ⚠️ **완화(loosening) 변경엔 refilter가 no-op**(구표본이 전부 새 조건 통과) — 대신 구표본이 좁은 조건에 치우친 부분집합이라 대표성이 없으므로 since가 맞는 처방. ⚠️ since 리셋은 fail-closed라 재검증 표본이 찰 때까지 LIVE 정지 — 부담되면 `bootstrap-strategies`로 축소사이징(×`fallback-size-mult` 0.5) 실주문 허용(인버스 부트스트랩과 동일 메커니즘). 🐞 **`bootstrap-strategies`가 prod에서 죽은 코드였음(2026-08-21 발견·수정)**: fallback 분기(`fallbackEnabled && regimeName != null`)가 **미달 시 무조건 return**해 그 아래 일반 부트스트랩 분기에 **영영 도달하지 못했다** — prod는 `TRADING_PERF_GATE_FALLBACK_ENABLED=true` + 국면 산출됨이라 항상 fallback 종단에서 끝났고, 전략을 csv에 넣어도 **아무 일도 안 일어났다**(J 추가 시도 중 발견). 즉 위 '완화 처방' 자체가 작동하지 않았다. 수정: 미달 return을 `bootstrapStrategies` 미포함일 때만 하도록 바꿔 지정 전략은 재검증 다리로 흘려보낸다(fallback '통과'는 전국면 pool로 검증된 더 강한 근거라 우선순위 불변). ⚠️ 이 버그는 **국면조건부 on + fallback on 조합에서만** 재현되므로 테스트도 그 조합(`prodLikeProps`)으로 짜야 한다 — 기존 테스트는 `regimeConditional=false`라 `regimeName==null`로 fallback을 건너뛰어 버그를 못 잡았다. 테스트 2(도달/대조). **⚠️ J 부트스트랩 활성화(2026-08-21, 사용자 결정)**: `feature-mining` 진입-대조군에서 **J의 edge가 전 전략 최대(+1.40%p** — 진입 net +0.04% vs 대조군 −1.36%, n=206, 20거래일, close, 커버리지 100%)인데 게이트는 표본부족(KOSPI 4/30·KOSDAQ 1/30)으로 계속 차단 → **edge가 가장 큰 전략을 영영 못 켜는** 구조라, prod `TRADING_PERF_GATE_BOOTSTRAP_STRATEGIES=VALUE_REVERSAL_J`로 **축소사이징(×`fallback-size-mult` 0.5 ≈ 순자산 2.5%/종목) 실주문 허용** — 표본 30 도달 시 엄격 경로로 자동 졸업(net 미달이면 차단, 우회 불가). ⚠️ **edge > 0은 '덜 나쁘다'이지 '수익난다'가 아니다** — J의 절대 net은 close −0.28%·exit −0.37%로 **둘 다 음수**다. 대조군은 '안 샀으면 얻었을 값'이 아니라 '같은 스크리닝 풀에서 J 필터에 걸러진 종목들의 성과'이고, 실제 대안인 '거래 안 함'은 0%다. 즉 이 활성화는 **기대손실을 내며 실표본을 사는 것**(졸업까지 대략 −0.3%×30건 ≈ 순자산 0.2%대). 부담되면 `TRADING_PERF_GATE_FALLBACK_SIZE_MULT` 하향. **+ G 부트스트랩 추가(2026-08-24, 사용자 결정)**: G도 같은 날 `strategy-since` 리셋(`RSI_REVERSAL_G:20260824`, 흐름↓ 스킵 도입)으로 표본 0/30이 됐는데 부트스트랩 목록엔 J만 있어 **근거가 더 강한 쪽이 더 오래 닫혀 있는 비대칭**이 생겼다 — `feature-mining` 진입-대조군에서 G edge **+1.28%p**(대조군 n=37, 진입 net +0.27%) vs J +1.40%p(대조군 n=25)이고, 국면 분해에서 G는 **BULL +2.32 / NEUTRAL +2.32 / BEAR −0.31**이다. → prod `TRADING_PERF_GATE_BOOTSTRAP_STRATEGIES=VALUE_REVERSAL_J,RSI_REVERSAL_G`. ⚠️ **현 국면(양 시장 BEAR)에선 G의 절대 net이 −0.81%라 당장은 기대손실을 내며 실표본을 산다** — J와 같은 성격의 결정이다. ⚠️ G의 국면별 대조군 n은 6~19로 얇아 '국면 제한' 처방을 내리기엔 근거가 부족하다(그래서 제한 없이 부트스트랩만 열었다) — 표본이 쌓이면 D·K처럼 국면 제한을 재검할 것. ⚠️ multiday 실측상 J는 보유 D+1 +0.08% → D+3 −1.29%로 급락하는 **초단기(당일~D+1) 전략** — 현행 30분 보유와 정합. 가드(EXEC_OVERHEAT −1.57%·RET5D_OVERHEAT −0.51%)는 둘 다 ENTERED(−0.28%)보다 나빠 **유지가 옳다**. **prod 실적용(2026-08-14)**: `TRADING_PERF_GATE_STRATEGY_SINCE=VOLUME_LEADING_B:20260817,MA_TREND_F:20260817,OPENING_GAP_K:20260817` — B 볼륨하한 원복·F 신선도 off·K 지수갭업일 제외 3건의 로직 변경 반영(검증: B/F/K가 n=0으로 리셋된 반면 since 미지정 D는 n=148/169 유지). env `TRADING_PERF_GATE_STRATEGY_SINCE`/`TRADING_PERF_GATE_REFILTER`(둘 다 compose 패스스루 완료). **+ 최대기여일 제외 net(LOO, 2026-08-20)**: 점유율 가드(`max-single-day-share-pct` 80%)만으로는 부족했다 — D-KOSPI가 **74/100(74%)로 문턱을 간발로 통과**했는데 그 74건이 **7/31 하루(+3.82%)**라 버킷 net **+2.52%가 사실상 그 하루**였다. 게다가 점유율은 **새 진입/섀도우가 분모에 쌓이기만 해도 희석**된다 — 8/20 장중엔 90%로 차단됐다가 그날 섀도우 18건이 들어가며 74%로 떨어져 **7/31에 대한 새 증거 0인 채로 밤사이 다시 열렸다**(자기충족 구조). → `perf-gate.loo-top-day`(기본 true): **net 합이 가장 큰 하루를 뺀 나머지 net도** 기준을 넘어야 LIVE 허용(전체 net과 **AND**). 세 판정 경로(흐름버킷·국면버킷·전국면 fallback) 전부 적용. ⚠️ **게이트 동작은 단조적으로 엄격** — 기존 조건에 AND로 얹기만 하므로 닫히는 방향으로만 작용한다(이 요건 때문에 열리는 버킷은 없다). 단 **남은 net이 전체 net 이하라는 보장은 없다** — 표본 많고 평균 낮은 날이 'net 합 최대'로 뽑히면 나머지 평균이 오히려 오른다(실측 8/20 H-KOSPI 전체 −0.32% → 제외 −0.26%). 즉 이 값은 '단일일 의존도 진단'이지 하한이 아니다. 거래일 1개거나 남는 표본 0이면 판정 생략(점유율 가드가 처리). 사유문에 `·최대기여일(20260731, n=74) 제외 net -0.54%`로 **통과/차단 무관 항상 노출**(진단). 🐞 **사유 라벨이 LOO 차단을 "성과 미달"로 자기모순 표기하던 것 수정(2026-08-26)**: 라벨은 `net AND LOO` 결과인데 바로 뒤 부등호는 **net만** 반영해, LOO 단독 차단이 **`성과 미달(net 0.61% ≥ 기준 0.30%)`**로 찍혔다(실측 8/26 L 후보 8건 전부 이 문장 — 읽는 사람은 게이트 오작동으로 오해한다). 동작은 정상이었고 진짜 원인은 뒤의 LOO 태그에만 있었다. → `verdictLabel(allow, netOk)`로 **`성과 미달`(net 미달 = 전략 문제)**과 **`단일일 편중(LOO 미달)`(표본 문제)**을 분리. ⚠️ **원인 구분이 곧 처방 구분**이다 — net 미달은 임계·필터를 손봐야 하지만 LOO 미달은 다른 날 표본이 쌓이면 저절로 풀린다. ⚠️ fallback 분기는 차단 사유가 **셋**(표본부족·net·LOO)이라 LOO 단독일 때만 라벨을 바꾸고(나머지는 종전 `fallback미달` 유지) LOO 태그를 함께 싣는다 — 종전엔 그 분기에 **태그조차 없어 원인 미상**이었다. 테스트 1. env `TRADING_PERF_GATE_LOO_TOP_DAY`(false=종전, compose 패스스루). 테스트 3(클러스터 차단/균등분산 통과/비활성 종전).
    - **게이트 히스테리시스(2026-08-06 도입, 2026-08-13 버킷 스코프 수정)**: 게이트가 문턱 근처에서 여닫이를 반복하면 "평균회귀 성과곡선을 고점매수-저점매도"하는 역선택이 생김(실측 G Δ−3.95·B Δ−1.81) → **비대칭 밴드**: 열 땐 `min-net-avg-pct`(0.3), 이미 열려있으면 `close-net-avg-pct`(**prod −0.05**, 2026-08-14 −0.2에서 축소)까지 유지. ⚠️ **−0.2 밴드의 부작용(2026-08-14 실측)**: 밴드가 넓어 **net이 비용선 위로 회복된 적 없는 버킷이 계속 열린 채 유지**됐고(K-KOSPI +0.05·K-KOSDAQ +0.10·D-KOSDAQ 0.00 — 셋 다 여는 문턱 0.3 기준이면 닫혔을 버킷), 8월 LIVE 손실 −208,166원의 대부분이 이 세 버킷에서 나왔다. 진동 억제라는 도입 취지는 유지하되 폭만 −0.05로 축소. 활성 조건 `closeNet < minNet`(기본 0.3=off). 상태는 **인메모리**(재기동 시 전부 닫힘=fail-safe), 사유에 `·히스테리시스(닫기바 유지)` 노출. 🐞 **stale 상태 버그(2026-08-13 수정)**: ① 키가 `전략:시장`뿐이라 **국면·흐름 버킷이 바뀌어도 같은 상태를 물려받아**, 옛 버킷에서 얻은 '열림'이 표본도 net도 다른 새 버킷에 완화된 닫기바를 잘못 적용 ② 표본부족/fallback/부트스트랩 경로가 `openState`를 **갱신조차 안 해** 열림이 stale 방치. → 키를 **`전략:시장:국면:흐름`(판정에 실제 쓰인 표본 풀)**으로 스코프 + 엄격(흐름·국면) 분기에 도달 못 한 경로는 **그 버킷을 닫힘으로 명시**(클러스터 차단이 이미 false를 쓰던 것과 동일 사상). 두 엄격 분기가 모두 `return`이라 분기 직후 한 줄이 6개 반환점을 커버. ⚠️ **선(先)clear 방식은 배제** — 스케줄러 스레드 동시 평가 시 다른 스레드가 `wasOpen`을 false로 오독하는 창이 생김(판정 시점에만 원자적 전이). ⚠️ 키가 잘게 쪼개진 만큼 각 버킷 상태는 드물게 갱신됨(진동 억제 ↓ / 정확도 ↑ 트레이드오프). env `TRADING_PERF_GATE_CLOSE_NET_AVG_PCT`. 테스트 4(밴드 유지/off/버킷 분리/같은 버킷 표본부족 정리). ⚠️ `GET /strategy-gate`(`evaluateAll`)는 `forcedRegime=null`이라 **조회만 해도 상태가 갱신됨**(읽기 엔드포인트의 쓰기 부작용) — `strategy-gate-by-regime`은 시뮬이라 KOSPI/KOSDAQ 미갱신(INVERSE 줄만 갱신).
- **레이어 2 — 국면조건부**(`regime-conditional`, 기본 true): 게이트가 전체 표본이 아니라 **종목 시장의 현재 국면(`MarketRegimeService.trendOf(market)` — KOSDAQ 종목은 KOSDAQ 국면, KOSPI는 KOSPI 국면, 미산출 시 overall fallback)과 같은 국면에서 진입한 표본**(`TradeOutcome.entryMarketTrend`, 진입 시 `StrategyEvaluator`가 종목 시장별로 태깅)만으로 net평균을 계산. 게이트는 `evaluate(strategy, market)`로 시장 받음(`OrderService.submitEntry`가 시장 전달), 가시화 `strategy-gate`는 전략×{KOSPI,KOSDAQ}. **`regime-market-split`(기본 true)이면 (market,trend) 2차원** — 같은 국면 + 같은 시장(`TradeOutcome.entryMarket`) 진입분만 집계(KOSPI-강세 후보 → 과거 KOSPI-강세만). ⚠️ 표본이 시장×국면으로 더 쪼개져 minSamples 도달이 느려짐(fail-closed, 의도된 보수성) — off면 trend만으로 풀링. 시장 미상이면 trend만. → 강세장에선 강세장 성과로, 약세장에선 약세장 성과로 게이팅돼 **강세=모멘텀(B)·약세=역추세(C)가 자동으로 해당 국면에서만 켜짐**. 현재 국면 미산출(데이터부족) 시 국면 무관 전체로 fallback. ⚠️ 국면별로 표본이 ~1/3로 쪼개져 minSamples 도달이 느려짐 — fail-closed라 안전하나 그만큼 그림자 누적 기간이 길어짐(의도된 보수성). 기존 표본은 `entryMarketTrend`=null이라 국면조건부 평가에서 제외(신규 진입분부터 태깅). **소급 태깅**: `RegimeBacktagService`(`POST /api/v1/admin/backfill-regime-tags`)가 null 태그 (시장,진입일)마다 프록시 ETF 일봉으로 **그날까지 종가 시계열 → `computeRegime` 재구성** 후 채움(라이브와 동일 판정식, 확정종가 기준이라 문턱 근처는 한 단계 다를 수 있는 근사). **null만 채우고 라이브 태그는 불변, ~30영업일 창 밖(표본부족)은 skip, 재실행 안전.** → 태깅 도입 전 진입분을 국면 버킷에 배정해 게이트 검증 가속.
- 🐞 **장 종료 후 청산 제출 스팸(2026-07-16 안트로젠 실측·수정)**: 청산 cron(follow-up-cron, 9-16시 매분)이 15:30 KRX 접수 마감 후에도 매도를 제출 → KIS "장운영시간이 아닙니다" 거부 → **매분 REJECTED 행 + 🔴 알림 스팸**(장중이면 안 드러나고, 장후에 미청산 포지션이 생기는 경로 — 부분체결 복원·수동 개입 등 — 에서만 발현). 수정: `PositionExitService`에 **주문 컷오프**(`trading.exit-order-cutoff`, 기본 15:30) — 컷오프 이후엔 서킷/breadth 전이 알림만 수행하고 청산 제출은 보류(다음 거래일 개장 후 첫 점검이 처리). ⚠️ 필드 초기값은 의도적으로 23:59(실시계 단위테스트 26곳 보호) — 프로덕션은 @Value가 15:30 주입. env `TRADING_EXIT_ORDER_CUTOFF`(compose 패스스루). 테스트 `주문컷오프_이후엔_청산_제출_보류`. **+ 휴장일 가드(2026-07-20)**: 컷오프는 장중 시각만 알아 **휴장일**(7/17 실측: 매분 제출 → "장운영일자가 주문일과 상이합니다" 거부 390건)은 못 막음 → 별도 휴장 캘린더 없이 **KIS 거부 메시지("장운영일자")를 휴장 신호로 사용** — 첫 거부에서 `marketHolidayDate` 당일 플래그 세팅 → 같은 날 청산 제출 전면 보류(자정/재기동 리셋, 다음 거래일 처리). 판정 `isHolidayRejection`(정적). 테스트 `휴장일_거부_감지시_당일_청산_제출_전면중단`.
- 🐞 **Order 동시성 lost-update(버그 8호, 2026-07-21 실측 · 7/22 수정)**: FillSync(스레드A)가 매도 전량체결 확인→부모 청산확정(closed=true, +761원)을 커밋하는 사이, 청산점검(스레드B)이 직전에 읽은 스테일 엔티티(closed=false)를 고점추적(peak/lastPrice) 저장으로 **덮어써 청산이 유실** → 매분 중복매도 시도→멱등 거부 ~350회 + 실현손익 미기록. 수정: **`Order`에 `@Version` 낙관적 잠금** — 늦은(스테일) 저장이 OptimisticLockingFailure로 실패하고 건별 try/catch 격리로 다음 틱 재조회. ⚠️ **배포 前 수동 마이그레이션 필수**: `ALTER TABLE trade_order ADD COLUMN IF NOT EXISTS version bigint NOT NULL DEFAULT 0;` — ddl-auto는 nullable로 추가해 기존 행 version=null → Spring Data가 isNew로 오판(persist 시도)해 전 기능 마비 위험(ddl 함정 목록과 동류).
- **매도(청산) 연결**(완료): `PositionExitService.closeDuePositions()`(1분 스케줄 `closePositions`, 수동 `POST /api/v1/admin/close-positions`) — `findOpenBuyPositions`(미청산 BUY) 점검 → **보유시간 경과 OR 장마감 `sessionEnd`(15:20) 도달** 시 현재가(`fetchLatestClose`)로 SELL 주문(`OrderService.submit`, SELL은 PolicyGate 한도 면제). 청산 성공 시 원 BUY `Order.closePosition(realizedPnl)` — **`realizedPnl`이 일일 손실 한도 집계의 입력**이 됨. idem=`SELL:{buyOrderId}`(중복청산 방지). **`realizedPnl`은 net 기록(2026-07-13~)**: LIVE(`FillSyncService.closeParentPosition`)·DRY_RUN(`PositionExitService.closePosition`) 모두 체결 차익에서 **왕복비용(`round-trip-pct` 0.22% × 매수금액)을 차감**해 기록 — 실계좌 실현손익과 정합(당일 대사 −24,019원 = 시스템 gross −21,220 − 비용 2,799 검증), 일일손실한도·일일리포트가 실질 기준이 됨. `ExecutionQualityService`는 재차감 안 함(이중차감 방지 — **그 이전 청산분은 gross 기록이라 realNet ≈0.22%p 과대**, 근사 수용). ⚠️ 증권사는 실현손익을 매도일에 귀속, 시스템은 매수 주문일(order_date) 귀속 — 오버나잇 포지션이 있는 날은 일자별 합계가 어긋나는 게 정상. ⚠️ 청산가는 조회 현재가(지정가 근사), 실체결가 아님(체결조회 미구현). 단위테스트 `PositionExitServiceTest` 4.
  - **적응형 보유시간**(`StrategyHoldTimeProvider`, `trading.adaptive-exit`): 청산 보유시간을 전 전략 고정값이 아니라 **전략별 권장 청산시점으로 자동 설정**. `ExitTimingService.analyze()`의 보유시간별 평균 net 수익 곡선에서 **표본 ≥`min-samples`(20)인 마크 중 평균수익 최대 마크의 분**을 그 전략 보유시간으로 채택(예: B~45분, C~120분). 자격 마크 없으면(데이터 부족) 고정 `time-exit-hold-minutes`(60분)로 **fallback**, 종가(EOD) 권장이면 `max-hold-minutes`(300)로 캡. 매분 청산점검마다 재계산 않도록 `refresh-minutes`(30) TTL 캐시. env `TRADING_ADAPTIVE_EXIT_*`, 가시화 `GET /api/v1/admin/exit-hold`(전략별 적용 보유시간·출처·표본). 🐞 **가시화가 A/B/C만 반환하던 버그(2026-08-14 수정)**: `describe()`의 전략 목록이 하드코딩이라 실제로는 D 90분·K 90분·F 70분·G/H 35분·E 5분으로 청산·게이팅되는데도 **조회로 확인할 수 없었음**(게이트 사유 문자열에서 역추론해야 했음). 수정: **등록된 `TradingStrategy` 빈 ∪ 분석 캐시 키**를 노출(필드주입 — 생성자 무churn). `exit-method`도 동일 결손·동일 수정. **`PolicyGate`의 진입규칙(시간기반 청산이 장마감 넘기면 진입금지)도 동일 provider의 전략별 보유시간을 사용** — 청산 실행(`PositionExitService`)과 진입 규칙이 일치(예: C 120분이면 13:20 이후 C 진입은 장마감 초과로 차단). 단위테스트 `StrategyHoldTimeProviderTest` 7 + `PolicyGateTest` 적응형 케이스 1. ⚠️ **권장 마크 노이즈 픽 억제(2026-08-18)**: 마크마다 표본이 서로 다른 부분집합(n 60~230)이라 곡선에 ±0.4%p 노이즈가 있는데, **60개 마크에서 단순 최대를 뽑으면 그 노이즈의 상위 극단이 잡힌다** — 실측: K 95분 +0.94%인데 이웃 90분 −0.44%·100분 −0.56%, J 245분 +0.19% / 이웃 −0.60%, I 255분 +0.45% / 250분 −0.36%. 이 마크는 곧 게이트 채점 horizon이기도 해서 **게이트 net까지 같은 크기로 낙관 편향**된다. → `adaptive-exit.smooth-window`(기본 3, 1=종전 max-pick)로 **이웃 창 평균이 최대인 마크**를 고른다(인접 마크가 함께 좋은 '구간'만 생존). ⚠️ 창을 온전히 채우는 마크만 후보(양 끝 half개 제외) — 잘린 창은 분모가 작아 경계 마크가 구조적으로 유리해진다. 자격 마크가 창+2 미만이면 평활 무의미 → max-pick. 평활은 **선택에만** 쓰고 보고값(표본·수익)은 원시 마크 그대로 노출. 판정 `StrategyHoldTimeProvider.pickBest`(순수 정적, 테스트 2). env `TRADING_ADAPTIVE_EXIT_SMOOTH_WINDOW`(compose 패스스루). **⚠️ 곡선 클러스터 가드 신설(2026-08-26) — 파급이 가장 큰 자리**: 평활은 *마크 간* 노이즈를 막지만 **곡선 전체가 하루로 만들어진 경우엔 무력하다**. 실측 L 곡선은 5분 −0.35 → 90분 +0.31 → 240분 +1.45 → **300분 +2.28%(승률 75.6%)**로 매끄럽게 단조 상승해 평활을 그대로 통과했는데, **진입일로 쪼개면 8/24는 +0.18 → +0.11로 완전히 평평**하고 상승분이 통째로 8/25(+0.97 → +4.34)였다. ⚠️ 이 마크는 **실제 청산 시점이자 게이트 채점 horizon**이라, 오염되면 **청산과 게이트 net이 함께** 하루짜리 허수 위에 선다 — 그래서 다른 어느 엔드포인트보다 가드의 파급이 크다. → `ExitTimingService.MarkStat`에 마크별 `distinctDays`/`maxDaySharePct`/`topDay`/`netExTopDayPct`/`clustered` 추가(진입일은 `OutcomeSampleRepository.findEntryDates()`로 `TradeOutcome` 조인 — `OutcomeSample`엔 진입일이 없다. outcome당 1행이라 결과 크기는 진입·대조군 행 수 수준), **권장 마크 선정과 `pickBest` 자격에서 클러스터 마크 제외**. 판정 `ExitTimingService.cluster`(순수 정적, `ControlAnalysisService`와 동일 규칙). 진입일 미상 마크는 판정 생략 + `distinctDays=0`으로 **진단 부재임이 드러나게**. ⚠️ **두 가드는 서로 다른 축**이다(평활=마크 간 노이즈, 클러스터=거래일 편중) — 하나가 다른 하나를 대체하지 않는다. 🐞 **같은 날 회귀 — 클러스터 마크를 목록에서 빼자 평활이 깨졌다**: 남은 목록이 성겨지면서(L 실측 자격 61개 → **8개**: 5·10·15·25·35·110·300·EOD) 평활이 *시간축 이웃*이 아니라 **리스트 인덱스 이웃**을 평균하게 됐다 — 110분의 "이웃"이 35분과 **300분**(265분 떨어짐)이 되어, **단발 스파이크를 막으려던 장치가 오히려 스파이크를 실어 날랐다**(110분 평활 1.07 = 300분 +2.28이 섞인 값). → ① 평활은 **클러스터 포함 전체 곡선의 시간축 이웃**으로 계산하고 **선택 자격만** 비클러스터로 제한 ② ⚠️ 단 **창의 위치는 클러스터 마크가 채우되 값은 빼고 평균한다** — 허수 값을 평균에 넣으면 그 하루짜리 수익이 **이웃의 평활값으로 새어나가** 고를 수 없는 마크가 옆 마크를 대신 뽑게 만든다. 창에 성한 이웃이 없으면 평활값은 자기 값으로 수렴(= "믿을 이웃이 없다"의 정직한 표현) ③ 정렬을 `ExitTimingService`와 통일해 **종가(EOD)를 맨 뒤로** — 종전 raw 정렬은 EOD(−1)를 맨 앞에 둬 **5분 마크의 이웃이 "종가 보유"**가 됐다(클러스터 가드 이전부터 있던 잠재 결함인데 자격 마크가 촘촘할 땐 안 드러났다). ⚠️ **실동작 영향은 0**(L은 110→300분으로 바뀌지만 캡 90이 다시 자른다) — 보고값의 정합성 문제였다. 테스트 3. ⚠️ **L에는 실제로 안 걸린다**(배포 시점 실측): 300분 net +2.28 → 8/25 제외 **+0.10**으로 **부호가 유지**되고 점유율 56%<80·거래일 정확히 3이라 통과한다. 즉 L 곡선의 상승은 8/25를 빼도 **살아남되 훨씬 작아질 뿐**이다(90분 ex-8/25는 −0.21). 이 가드의 값어치는 L이 아니라 **앞으로 부호가 뒤집히는 마크를 잡는 것 + 엔드포인트 간 규칙 통일**에 있다. 테스트 `ExitTimingClusterTest` 5 + `pickBest` 2. **⚠️ 캡이 조회에 안 보이던 것 수정(같은 날)**: prod `TRADING_ADAPTIVE_EXIT_MAX_HOLD_MINUTES=90`(**코드 기본 300**)이 분석 권장을 자르는데, `HoldInfo`가 `holdMinutes`만 캡된 값이고 `samples`/`avgReturnPct`는 **캡 전 마크** 값을 그대로 실어 조회가 "90분 마크가 n=89·+1.46%"로 읽혔다 — 실제로는 분석이 **295분**(n=89, +1.46%)을 고르고 90분으로 잘린 것이고 **진짜 90분 마크는 n=104·+0.31%**다. 캡은 정상 동작이지만 **캡이 걸렸다는 사실이 어디에도 안 보여** 진단이 불가능했다. → `rawMarkMinutes`·`capped` 노출 + 갱신 로그에 `권장 295분에서 캡` 표기. ⚠️ **캡 90은 트레이드오프지 버그가 아니다** — 올리면 `PolicyGate`의 "진입시각+보유시간 ≤ session-end" 규칙 때문에 **진입 마감 시각이 앞당겨진다**(295분이면 ~10:25 이후 전면 차단. 8/26 L 진입 9건 중 5건이 그 이후였다). 테스트 2.
    - **⚠️ 진입시점 락(lock, 사용자 요청)**: 청산 보유시간을 **진입 시점 provider값으로 `Order.holdMinutes`에 저장(락)** — 이후 provider가 갱신돼도 **이미 열린 포지션은 진입 때 정한 값으로 청산**(진입 후 최신 추정치에 흔들리지 않음). `OrderService.submitEntry`가 `holdTimeProvider.holdMinutes(strategy)`를 `OrderCommand`→`Order`에 저장, `PositionExitService`가 `pos.getHoldMinutes()>0`이면 그 값, 없으면(수동·구주문 null) live provider fallback. (매도 OrderCommand는 holdMinutes=null.) ddl-auto가 `hold_minutes`(nullable Integer) 자동 추가. 단위테스트 `진입시점_락한_보유시간을_provider보다_우선사용`. ⚠️ Mockito가 Integer getter에 0 반환하는 이슈로 fallback 조건을 `null || ≤0`으로 둠(프로덕션 락값은 항상 ≥5라 무해).
  - **적응형 청산방식**(`ExitMethodProvider`, `trading.adaptive-exit-method`): 청산방식을 시간기반에 고정하지 않고, **`ExitStrategyService.recommend()`(과거 가격경로 시뮬레이션의 평균 net 수익 최대 방식)**을 전략별로 자동 채택 — **TIME(보유시간) / TRAILING(고점 대비 되돌림%) / VWAP(가격<VWAP) / TREND_REVERSAL(직전 점검가 대비 하락) / FLOW_REVERSAL(지수흐름 반전, 2026-07-15)**. FLOW_REVERSAL: 진입 시장의 지수 mom30 ≤ param(0/−0.3% 격자) 음전 시 청산 — 시뮬은 (시장|날짜)별 entry_market_change 앵커 보간으로 마크 시각의 지수 mom30 재구성(`simulateFlowReversal` 정적, 앵커<3 표본 제외), 라이브는 `intradayFlow` mom30(미가용 시 보유, 장마감 백스톱=시뮬 EOD와 정합). **도입 시점 시뮬에선 전 전략에서 TIME을 못 이겨 미채택**(폭락 표본 지배 — 흐름이 진입부터 음전이라 즉시청산→반등 놓침). 표본이 쌓여 이기면 자동 채택(fail-closed). 테스트 `FlowReversalExitTest` 4. 표본 `min-samples`(30) 미만이면 TIME fallback. `PositionExitService`가 매 청산점검마다 미청산 포지션 현재가를 조회해 **고점(`Order.peak_price`)·직전가(`Order.last_price`)를 추적**하며 채택 방식으로 판정. **안전 트리거(손절·서킷·장마감)는 항상 우선**, 그 다음 적응형 방식. VWAP 방식은 방식 채택 종목당 분당 VWAP 1콜 추가. env `TRADING_ADAPTIVE_EXIT_METHOD_*`, 가시화 `GET /api/v1/admin/exit-method`. 단위테스트 `ExitMethodProviderTest` 4 + `PositionExitServiceTest`(트레일링·VWAP 청산). ⚠️ TREND_REVERSAL은 시뮬은 마크(5~15분) 간격이지만 라이브는 1분 점검이라 더 민감(첫 하락분에 청산) — 채택 시 유의. ⚠️ 트레일링/VWAP는 트레일링/VWAP 방식이 시뮬상 1등일 때만 적용되고, 기본 다수는 여전히 TIME일 수 있음(표본·곡선 의존). **[조기청산 방지②] `min-hold-minutes` 5 → 15(2026-08-25, prod)**: `execution-quality`에서 **D의 gap −1.18%p**(real −0.11% vs shadow +1.07%, n=90, 진입슬립 0.01%라 전부 청산 쪽)가 나와 트레일링 조기 발사를 의심했으나, ⚠️ **전체 표본으로 재검하면 근거가 오차 범위다** — D 청산곡선(n=904)이 5분 −0.24 / 15분 −0.22 / 20분 −0.21 / 65분 −0.23 / **종가 −0.25**로 평평하고, 시뮬도 트레일링 −0.16 ≈ TIME 20분 −0.21 ≈ 종가로 갈리지 않는다. 즉 gap −1.18%p는 **LIVE 90건 부분집합에서 종가 보유가 유난히 좋았던 것**이지 "트레일링이 승자를 자른다"의 증거가 아니다. → 방식 전환(TIME 고정)은 **하지 않고**, 신호기반 청산의 최소 보유만 5→15분으로 올렸다(env 하나, 되돌리기 즉시 가능). ⚠️ **근거가 약한 변경임을 명시해 둔다** — 다음 분석에서 D의 라이브 보유시간 분포가 15분 이후로 이동했는지, 그리고 gap이 줄었는지로 재검할 것. TIME·손절·서킷·상한가·장마감은 이 가드 밖이라 무관.
  - **스윙(오버나잇) 전략**(`trading.swing-strategies` csv, 기본 `MEAN_REVERSION_C`): C는 당일 청산이 아니라 **익일 종가 보유**(분석상 C의 엣지가 익일 — 당일종가 −% vs 익일종가 +%). 스윙 포지션은 `PositionExitService`에서 **장마감 강제청산·적응형 방식 미적용**, 대신 **진입일 다음 거래일 session-end에 청산**("스윙청산(익일종가)"). **손절·서킷브레이커는 장중 그대로 적용**(단 ⚠️ 오버나잇엔 점검 없음 — 갭 리스크 노출, 손절로 못 막음). 또 `PolicyGate`는 스윙 전략에 한해 **"청산이 장마감 넘기면 진입금지" 규칙 면제** → **C는 장중 아무 때나(예: 15:00) 진입 가능**. env `TRADING_SWING_STRATEGIES`. 단위테스트 `PositionExitServiceTest`(진입일 보유/익일 청산) + `PolicyGateTest`(스윙 장마감초과 진입허용). ⚠️ v1은 "다음 거래일 종가"(D+1, 관측 엣지) 고정 — D+2/D+3 보유는 거래일 카운팅 필요(후속).
    - **스윙 트레일링(fail-closed, 2026-07-09)**: 익일종가 보유 대신 **고점(>매수) 대비 되돌림% 조기청산**을 옵션으로. **`SwingExitProvider`가 fail-closed로 선택** — `SwingTrailAnalysisService`(`GET /admin/swing-trail-analysis`)에서 트레일(3/5/7%)이 익일보유보다 `swing-trail.margin-pct`(0.2%p) 넘게 낫고 트리거표본 ≥`min-triggered`(20)일 때만 그 %(최선) 채택, 아니면 **0=익일종가 보유**(검증 전엔 실청산 무변경). `PositionExitService`가 스윙 분기에서 provider값으로 트레일(이익구간 peak>매수에서만 arm). 검증가 3/5/7은 `TradeFollowUpService`가 스윙 outcome에 매분(당일+익일) `trail3/5/7_price`로 수집(수집 이후 진입분부터 유효). ⚠️ **완전 백테스트는 KIS 익일 분봉 부재로 불가 → forward 수집 검증**. 가시화 `GET /admin/swing-exit`, env `TRADING_SWING_TRAIL_ENABLED`. 단위테스트 `SwingExitProviderTest`·`SwingTrailAnalysisServiceTest`·`PositionExitServiceTest`(트레일 청산). (⚠️ 최초 검증 없이 즉시적용했다가 사용자 지적으로 fail-closed 전환.)
    - **스윙 게이트 horizon 일치**: 스윙 전략은 청산이 D+1이라, 성과게이트도 **당일종가(close) 아닌 `swing-horizon`(기본 nextClose)으로 검증** — "당일종가가 +라 게이트 열렸는데 실제 D+1은 −"인 불일치 방지. 즉 C 스윙은 **D+1 net이 +일 때만** 실전 진입 허용. ⚠️ **일봉 백테스트상 C는 D+1 −4.75%·D+2 −8.2%·D+3 −11.5%(163진입, 느슨한 C=반등확인 제외)** — "떨어지는 칼날" 신호라, 실제 C 스윙은 (반등확인 필터가 이걸 뒤집어 D+1이 +로 검증되기 전엔) 게이트에 막혀 실행 안 됨. `GET /admin/backtest-c`로 재확인.
- **잔고 reconcile**(완료): `PositionReconcileService.reconcile()` — 기동 직후 `@EventListener(ApplicationReadyEvent)` 1회 + 수동 `POST /api/v1/admin/reconcile`. 내부 미청산 BUY ↔ KIS 실보유(`fetchBalance` output1) 대조: ① 내부 미청산인데 실계좌 미보유 → `markReconciledClosed`(실현손익 null 유지) ② 실보유인데 내부 미추적 → 경고(자동매매 금지). **DRY_RUN은 가상 포지션이라 skip**, LIVE만 실동작. 단위테스트 `PositionReconcileServiceTest` 3.
- ✅ **비협상 4종+1 완료**: 킬스위치·일일손실한도·멱등성·잔고 reconcile·1주문 금액상한 — 전부 구현·테스트됨.
- **체결조회**(완료): `KisApiClient.fetchTodayFills()` — 일별주문체결조회(`inquire-daily-ccld`, TR `TTTC8001R`) → 주문번호별 `FillInfo(orderNo, orderQty, filledQty, remainingQty, avgFillPrice)`(평균체결가=총체결금액/총체결수량). `FillSyncService.syncFills()`(1분 스케줄 `syncFills`, 수동 `POST /api/v1/admin/sync-fills`)가 **LIVE 접수(SUBMITTED) 주문**을 실제 체결과 대조 → 전량 `markFilled`/부분 `markPartiallyFilled`/미체결 유지. **LIVE 주문 없으면 KIS 호출 생략(DRY_RUN no-op)**. `PositionExitService`는 이제 미체결(SUBMITTED) 포지션 청산 제외 + 청산 수량·매입가에 실체결값(`filledQty`/`avgFillPrice`, 없으면 주문값) 사용. 단위테스트 `FillSyncServiceTest` 4.
- **정정/취소**(완료): `KisApiClient.cancelOrder(orgNo, odno)`(정정취소 `order-rvsecncl`, TR `TTTC0803U`, 전량취소). `OrderCancelService.cancelStaleOrders()`(1분 스케줄 + `POST /api/v1/admin/cancel-stale-orders`) — LIVE 미체결(SUBMITTED/PARTIALLY_FILLED)이 `unfilledTimeoutMinutes`(기본 3분) 경과 시 취소→`CANCELLED`. **멱등성 상태인식화**: `existsByIdempotencyKeyAndStatusNotIn(CANCELLED/REJECTED/FAILED)` → 취소분은 중복판정 제외돼 **다음 틱에 현재가로 재주문=가격 추격**. 🐞 **부분체결 매도 취소 미정산 버그(2026-07-15 모베이스전자 012860 실측·수정)**: 매도 239주 중 66주만 체결된 채 취소되면 원 매수 포지션이 여전히 239주로 인식 → 재매도가 239주로 나가 KIS "주문 가능 수량 초과" 거부 **반복(포지션 고착)**. 수정: `cancelStaleOrders`가 취소 시(양 경로) `settlePartialSell` — 멱등키 `SELL:{buyId}`로 부모 매수 찾아 **`Order.applyPartialSell(판수량, 부분 net손익)` = 잔여수량 차감 + 손익 누적**(미청산 유지 → 잔여분 재매도 정상). `closePosition`도 **누적 방식으로 변경**(덮어쓰면 부분 손익 유실). 테스트 `부분체결_매도_취소시_부모포지션에_수량차감과_손익누적`. 🐞 **부분체결 매수 취소 고아 포지션 버그(2026-07-16 안트로젠 065660 실측·수정)**: 매수 29주 중 28주 체결된 채 잔여 취소되면 주문이 CANCELLED — `findOpenBuyPositions`가 status로 제외해 **체결 28주가 청산 추적에서 사라짐**(하루 넘게 미청산 방치, reconcile은 경고만). 모베이스(매도) 사건의 매수 쪽 거울상. 수정: `settleCancelled`(양 취소 경로 공통 진입점) — **매수+체결분>0이면 CANCELLED 대신 체결수량 기준 `markFilled` 전환**(보유·청산 추적 유지), 미체결/매도만 CANCELLED(매도는 부분매도 정산). 잔존 고아는 `update status=FILLED`로 복원(멱등키 부분유니크와 정합). 테스트 `부분체결_매수_취소시_체결분은_FILLED_포지션으로_전환`. 취소 API에 필요한 거래소조직번호 `Order.brokerOrgNo` 저장. `Order.markCancelled`. 🐞 **전일 만료 SUBMITTED 고착 수정(2026-07-13, 삼천리 004690 실측)**: 마감 직전 추격 매도가 미체결로 장 마감하면 브로커 측은 만료(지정가 당일 유효)인데 DB는 SUBMITTED 유지 — FillSync는 당일 체결만 조회, KIS 취소는 "원주문 없음" 거부 → **멱등키 `SELL:{buyId}` 점유로 재매도 영구 차단(포지션 오버나잇 고착)**. 수정: `cancelStaleOrders`가 **`order_date < 오늘`인 미체결은 KIS 호출 없이 로컬 `markCancelled()`**(브로커 이미 만료). 테스트 `전일_만료_주문은_KIS호출없이_로컬종결`.
- **LIVE 청산 체결정합화**: 매도 '접수'≠체결 → `PositionExitService`는 LIVE에서 매도 접수만 하고 BUY를 즉시 closed 안 함. `FillSyncService`가 **매도 전량체결 확인 후 원 매수 포지션을 실체결가 기준으로 청산 확정**(idem `SELL:{buyId}`로 매칭, `closeParentPosition`). DRY_RUN은 종전대로 즉시 청산(현재가). 단위테스트 `OrderCancelServiceTest` 3 + `FillSyncServiceTest` 매도→청산 케이스.
- 🐞 **지정가 호가단위 미정렬 → "주식주문호가단위 오류" 거부(2026-07-09 006110 실거래 발견·수정)**: 청산가(`fetchLatestClose`)를 그대로 지정가 전송했는데 33,375(2만~5만원대 tick=50, ÷50 아님)라 KIS가 거부. **수정**: `ExecutionCostModel.snapToTick(price, roundUp)`(매수 올림·매도 내림, ETF도 stock tick 배수는 항상 유효) 추가 + `OrderService.submit`이 **전송 전 스냅**(정책검증·기록·전송 모두 스냅값 통일 — 단일 초크포인트라 진입/청산/수동 전부 커버). 단위테스트 `ExecutionCostModelTest.호가단위_스냅`, `OrderServiceTest.LIVE_전송가는_호가단위로_스냅된다`.
- 🐞 **멱등키 DB 전(全)유니크가 status 무시 → 거부 후 재주문 영구차단(2026-07-09 006110, 위 호가버그와 연쇄)**: SELL 거부(REJECTED) 후 포지션이 미청산으로 남아 다음 틱에 같은 `SELL:{buyId}` 재시도. `PolicyGate` 앱 멱등체크는 **상태인식**(`existsByIdempotencyKeyAndStatusNotIn(REJECTED,FAILED,CANCELLED)` → 재주문 허용)인데 **DB 유니크 인덱스 `idx_order_idem`은 status 무시**라 INSERT가 duplicate key로 실패 → 포지션 영구 미청산 + 매분 에러. 모든 LIVE 거부/실패 재시도의 잠재버그. **수정**: `Order` 엔티티 전유니크 제거(@Column/@Index `unique` 삭제, 조회용 비유니크만) + **부분 유니크 인덱스**로 활성분만 유니크 강제. ⚠️ **ddl-auto update는 기존 유니크를 안 드롭 → 배포 후 수동 마이그레이션 필수**(enum CHECK·boolean default 함정과 동류): `ALTER TABLE trade_order DROP CONSTRAINT idx_order_idem; CREATE INDEX idx_order_idem ON trade_order (idempotency_key); CREATE UNIQUE INDEX idx_order_idem_active ON trade_order (idempotency_key) WHERE status NOT IN ('REJECTED','FAILED','CANCELLED');` 배포+마이그 후 006110 다음 청산틱에 스냅가(33,600)로 자동 접수·청산 검증됨.
- **수동 승인 게이트**(완료): `manual-confirm=true` + **LIVE + 매수**일 때 진입을 즉시 발사하지 않고 `PENDING_APPROVAL`로 큐잉 + **Discord 통지**(승인/거부 엔드포인트·만료시각 안내). 승인 `POST /api/v1/admin/orders/{id}/approve`→발사, 거부 `.../reject`, 목록 `GET /api/v1/admin/orders/pending`. **`approval-timeout-minutes`(기본 5) 경과 시 자동 거부**(`expireStaleApprovals`, 1분 스케줄 — 늦은 진입 방지). **DRY_RUN·매도는 게이트 미적용**(즉시 실행). `OrderService.executeOrder`로 발사 로직 분리, 승인은 이를 호출. 단위테스트 `OrderServiceTest` 10(큐잉/승인발사/만료 포함). ⚠️ **실측(배포본)**: 수동 `test-order`도 LIVE+매수면 `submitManual`→`submit()`이 manual-confirm을 그대로 태워 **`PENDING_APPROVAL`로 큐잉됨**(즉시 발사 아님) — 승인 엔드포인트로 발사해야 함(문서상 "test-order 게이트 미적용"과 배포본 동작 불일치).
- 🐞 **EGW00202 "GW라우팅 오류" = POST 본문 chunked 전송 버그(2026-07-01 소액 1주 실매매 테스트에서 발견·수정)**: LIVE로 088350 1주 지정가 매수 발사 시 **KIS 주문 전송이 `rt_cd=1 / msg_cd=EGW00202`(HTTP 500)로 결정적 실패**(재시도 동일). 조회(잔고 TTTC8434R)는 rt_cd=0 정상인데 주문 TR만 실패 → **KIS 계좌/권한 문제 아니고 코드 버그**. 원인: **Spring `RestClient`에 `Map` 본문을 그대로 넘기면 `Transfer-Encoding: chunked`(Content-Length 없음)로 전송되는데, KIS 게이트웨이는 라우팅 단계에서 Content-Length를 요구** → chunked면 EGW00202. **GET 조회는 본문이 없어 무관**(그래서 조회만 되고 주문만 실패). curl은 자동 처리라 재현 안 됨. **수정(`KisApiClient`)**: `ObjectMapper.writeValueAsBytes(body)`로 **`byte[]` 직렬화 후 `.body(byte[])`** 전송(→ Content-Length 자동 설정), hashkey도 동일 바이트로 계산해 정합. `serializeBody()` 추가, `postOrder`/`hashkey(byte[])` 변경. (참조: velog @parkdanil pongtrader #28.) **모든 POST 주문(매수·매도·정정취소)에 적용됨.** ⚠️ 재배포+재테스트로 접수(rt_cd=0)까지 검증 필요.
- ✅ **자동 사이클 통합 테스트 실거래 검증 완료(2026-07-01)**: 수동 1주 테스트(088350)로 주문 접수·체결·매도 검증 후, **B 전략만 화이트리스트+소액(cap 5%)+수동승인+perf-gate 임시우회**로 LIVE 세팅 → 시장스캔 자동신호(003490 대한항공)가 `submitEntry`(화이트리스트→게이트→ATR사이징 1주→리스크가드)→`PENDING_APPROVAL` 큐잉→승인→**FILLED @27,500**. 5분 후 **`PositionExitService`가 시간경과 자동매도 접수(@27,600)→`FillSyncService.closeParentPosition`이 매도 전량체결 확인 후 부모 BUY 청산확정(idem `SELL:37`, 손익 +100원 gross, `closed=true`)**. **미체결 대기 중 중복매도는 idempotency(`SELL:{buyId}`)로 정상 차단.** 즉 **신호→자동진입 실주문→자동청산 실주문→부모포지션 연결 전 구간이 실거래로 증명됨.** 테스트 후 전 임시설정 원복+DRY_RUN 복귀, 계좌 전량청산(하루 2왕복 순 −2원=비용만). 남은 건 전략 검증데이터 축적 → 검증된 전략을 `TRADING_LIVE_STRATEGIES`에 넣고 수동승인 소액 LIVE.
  - ⚠️ **adaptive-exit가 표본 쌓이면 늦은장 진입을 막음(설계상 정상, 실측)**: 그림자 표본 축적(B n=238)으로 `adaptive-exit`가 B 권장 보유시간을 **300분(max-hold 상한)**으로 산정 → `PolicyGate`의 "시간기반 청산이 장마감(15:20) 초과면 진입금지" 규칙에 걸려 **~10:20 이후 B 진입이 자동 차단**됨. B는 종가까지 보유가 유리하다는 데이터라 늦은 진입을 거부하는 정상 동작. (5분 테스트하려면 `TRADING_ADAPTIVE_EXIT_ENABLED=false`+`TIME_EXIT_HOLD_MINUTES=5`로 고정.)
- **운영 이벤트 알림**(무인 실매매 관측 1순위, 구현): 상태 변화를 Discord로 push. `OrderService.notifyEvent`(공개, 실패 격리)로 **LIVE 실주문 접수(🟢매수/🔵매도)/거부(🔴)/오류(🔴)** — `executeOrder`의 LIVE 경로에서 발송(DRY_RUN은 조기반환이라 미발송). **알림 포맷(2026-07-10)**: 전 주문 알림에 **종목명(코드)** 표시(`OrderService.stockDisplay`, CompanyRepository 필드주입 — 미매핑이면 코드만); 매도 접수엔 **청산 사유**(`OrderCommand.note`→`Order.note` @Transient, PositionExitService가 reason 전달) + **✅익절예상/🛑손절예상 %**(멱등키 `SELL:{buyId}`로 부모 매수가 조회); 💰청산 확정(FillSync)엔 **✅익절/🛑손절** 부호 태그. OrderCommand는 note 없는 10인자 호환 생성자 유지(기존 호출부 무churn). `PositionExitService`가 (생성자 변경 없이 주입된 `orderService.notifyEvent` 경유) **서킷브레이커 발동/해제(⚠️/✅, edge-trigger `wasRiskOff`로 전이 시 1회)** + **손절(catastrophic) 청산(🛑, 매수→현재가·손실%)** 통지. `StartupNotifier`(@ApplicationReadyEvent)가 **앱 기동 시 태세(♻️ 모드·enabled·수동승인·화이트리스트)** 통지 — 예기치 않은 재시작/재배포 인지용. **LIVE 실현손익 확정(💰)**: `FillSyncService.closeParentPosition`(매도 전량체결 확인 후 원 매수 청산 확정)에서 `orderService.notifyEvent` 경유로 매수→매도·손익·수익%를 통지(FillSync에 `OrderService` 주입, 테스트 5곳 mock). LIVE 전용(FillSync는 LIVE 미체결만 처리). ⚠️ **후속(미구현)**: 일일손실 한도 도달 알림, 다운타임 하트비트(2순위).
  - 🐞 **서킷 알림 미발송 버그(2026-07-02 −6% 폭락일 실측·수정)**: 서킷 발동/해제 알림이 `PositionExitService.closeDuePositions()`의 **`if (open.isEmpty()) return 0;` 조기반환 뒤**에 있어, **미청산 포지션이 0이면 알림 코드에 도달 못 함**. 7/2 KOSPI −6.14% 서킷이 신규진입을 막았지만(OrderService 경로) 포지션이 0이라 ⚠️알림이 안 나갔음. **수정**: 서킷 전이 점검·알림을 조기반환 **앞**으로 이동(포지션 유무와 무관한 시장 이벤트). 회귀테스트 `PositionExitServiceTest.서킷브레이커_발동시_미청산_0이어도_알림_발송`. ⚠️ 교훈: 시장 전체 이벤트 알림을 "포지션 있을 때만 도는" 경로에 넣지 말 것.
- ⚠️ LIVE 전 잔여(선택): 시장가/매수가능조회.

### 시장 국면 엔진 (레이어 1 — `MarketRegimeService`)
- **목표 비전**: "하락장 리스크↓·상승장 수익↑"의 상위 두뇌. 지금까지가 종목단위 상향식이라면, 이건 **시장 국면을 능동 판정해 상위 입력으로 제공**(이후 레이어 2 국면조건부 전략배분, 레이어 3 국면연동 리스크가 소비 예정).
- **추세**(`MarketTrend` 강세/중립/약세) + **변동성**(`VolatilityLevel` 저/중/고)을 KOSPI·KOSDAQ별로 산정. 판정식: 종가 vs MA(**기본 3일** — 단타용 단기국면) + MA 기울기(**최근 2일**) → 추세 / 일간수익률 실현 표준편차(%) → 변동성(고≥2.0%, 저<1.0%). ⚠️ **MA20→MA3로 단축**(사용자 요청): 단타(당일~수시간 청산)엔 20일 추세가 후행적이라(급락 후 며칠 회복해도 BEAR 고착) 최근 3일 추세로 판정 → 며칠 회복하면 즉시 강세/중립 전환. 반응 빠른 만큼 노이즈↑(하루 반등에도 흔들림). env `MARKET_REGIME_MA_PERIOD`/`SLOPE_LOOKBACK`. (서킷브레이커는 이와 별개로 당일 지수 등락률로 판정.) ⚠️ 기존 표본 국면태그는 옛 MA20 기준 — 신규분부터 MA3, 일관성 위해 소급 재태깅(`backfill-regime-tags`) 재실행 가능.
- **intraday 보정(2026-07-16)**: MA3 라벨 후행("BULL 라벨+폭락" 7/13·7/16 2회 실측 — 노출상한 확대된 채 폭락) → **당일 지수 등락률로 라벨 즉시 보정**: 강등(지수 단독, 빠르게) 당일 ≤−`intraday-demote-pct`(2%)면 1단계·≤−4%면 2단계(BULL→BEAR); 승격(보수적, 합의) 당일 ≥+`intraday-promote-pct`(2%) **AND breadth 상승비율 ≥60%(신선 40분)**면 1단계만(7/10 breadth 88% 다음날 폭락 반례 반영 — 비대칭 원칙). `regimeOf/trendOf/overallTrend` 전부 보정값 반환 → 게이트 버킷·노출상한·진입 태깅까지 일관 적용. 판정 `adjustTrend`(정적, 테스트 3). 배포 직후 실측: BULL·50% → BEAR·30% 즉시 강등. env `MARKET_REGIME_INTRADAY_DEMOTE_PCT`/`PROMOTE_PCT`(0=비활성). ⚠️ 임계 부근 깜빡임 가능(지수 60s 캐시가 완충) — 문제 시 히스테리시스 추가 검토. → **아래 승격 디바운스로 대응됨(2026-08-13).**
- **라벨 승격 디바운스(2026-08-13, K 실측 계기)**: 기저(MA3) 라벨은 시계열 끝에 **오늘 '부분봉'(=현재가)**이 들어가 장중 가격이 움직이면 중립↔강세가 수시로 뒤집힌다. 라벨이 바뀌면 **게이트 버킷(표본·net)이 통째로 교체**돼 같은 전략의 진입 가부가 하루에도 여러 번 갈림 — 실측 2026-08-13 K: 09:10~09:29 KOSDAQ 10건이 `[KOSDAQ·중립·90분] 국면표본부족(10/30)`으로 차단(같은 시각 KOSPI 8건은 통과), 마감 스냅샷은 `[KOSDAQ·강세·90분·흐름↑] n=77 통과`. → **승격은 같은 후보가 `upgrade-min-hold-minutes`(30분) 지속돼야 확정, 강등은 즉시**(intraday 보정과 동일한 비대칭 원칙 — 리스크 축소는 빠르게/확대는 느리게). 라벨이 깜빡이면(승격후보↔기존) 대기가 리셋돼 영영 승격 안 됨 = 의도된 damping. `adjustedTrendOf`(intraday 보정 뒤)에 적용 → `regimeOf/trendOf/overallTrend` 전 소비처 일관. `isReboundDay`는 기저(`base.trend()`) 직접 사용이라 **영향 없음**. 판정 `stabilizeTrend`(순수 정적, 테스트 4). env `MARKET_REGIME_UPGRADE_MIN_HOLD_MINUTES`(0=비활성, compose 패스스루). ⚠️ 승격 지연 = 노출상한 확대도 지연(보수적 방향).
- **전일 확정 라벨 `priorDayTrendOf`(2026-08-13)**: 시계열 끝의 오늘 부분봉을 빼고 `computeRegime` → **장중에 변하지 않는 "어제까지의 국면"**(intraday 보정·디바운스 미적용). `refresh()` 때 함께 산출해 `priorTrend` 맵 캐시(추가 KIS 0). `asOf≠오늘`(장전·휴장)이면 이미 전일까지라 현재 라벨 그대로. 미산출이면 `trendOf`로 degrade. → K가 소비(아래 전략 K 항목).
- ⚠️ **지수 일봉 전용 KIS API 대신 지수 추종 ETF를 프록시**로 사용(미검증 엔드포인트 회피, 검증된 `fetchDailyPrices` FHKST01010400 재사용): KOSPI≈KODEX 200(069500), KOSDAQ≈KODEX 코스닥150(229200). `MARKET_REGIME_*` env로 코드·기간·임계 오버라이드. 일봉 ~30영업일 한계라 MA60/120은 미지원(추후 진짜 지수일봉 API 또는 누적으로 확장).
- 계산은 `computeRegime`(순수 함수, 종가 시계열만)로 분리 → KIS 없이 단위테스트(`MarketRegimeServiceTest` 5). 일봉 기반이라 `refresh-minutes`(60) TTL 캐시. 가시화 `GET /api/v1/admin/market-regime`(추세·변동성+근거: 종가·MA·MA기울기·실현변동성·표본·기준일). 데이터 부족/조회실패 시 `available=false`의 중립/중변동으로 degrade.
- **레이어 2 완료**: 국면 입력은 **종목 시장별**(`trendOf(market)`) — KOSDAQ/KOSPI 분리(`overallTrend`=`combineTrend` 부호합산은 시장 미상/fallback 시만). 게이트가 **종목 시장의 현재 국면 진입분만으로 평가**(위 "레이어 2 — 국면조건부" 참조) → 국면별로 검증된 전략만 그 국면에서 LIVE.
- **레이어 3 완료 — 국면연동 리스크 관리**(`MarketRiskGuard`, `trading.risk`, 하락장 방어): 개별 전략 검증(성과게이트)과 직교한 **포트폴리오 전체 리스크 통제**. 두 축:
  - **① 총노출 상한**: 현재 국면별 순자산 대비 노출 상한(강세 100%/중립 60%/약세 30%, 고변동이면 ×`high-vol-exposure-mult` 0.5). `OrderService.submitEntry`가 사이징 후 `riskGuard.allowEntry(주문액, 순자산)` 호출 → (미청산 매수 총액 + 이번 주문) > 상한이면 진입 차단. 국면 미산출 시 제약 없음(degrade open).
    - **①-b 개장 창 노출 상한(2026-08-14, 기본 off)**: 총노출 예산이 **선착순**으로 배분돼 개장 갭 구간에 상관도 높은 롱이 집중되는 구조 대응. 실측 8/14: **LIVE 15건이 전부 09:01~09:24(27분)에 진입**, 09:07 시점 노출 5,012,064원(순자산의 47%, 한도 5,370,338원에 도달)으로 **6분 만에 리스크 예산이 소진**됐고 그 구간이 갭업 고점이라 **−188,665원(계좌 −1.78%)** — K 4건이 같은 되돌림에 손절선(−5.3%)으로 동반 청산. → `opening-exposure-cap-pct`(순자산 대비 %)를 **09:00~09:00+`opening-window-minutes`(30분)** 동안 국면 상한과 **min 결합**.
      - ⚠️ **초기 서술 정정(2026-08-14, 사용자 지적)**: "예산 소진이 오후까지 이어져 회복 구간에 미참여"는 **틀렸다**. 노출은 현재 보유 기준이라 청산되면 회복된다 — 주문 타임라인 재구성 결과 09:20 두 건 청산으로 여유가 생겼고(3,997,224원), 09:24 재도달 후 09:38부터 계속 여유, **10:42엔 0원**이었다. 즉 예산이 실제로 물린 구간은 **09:07~09:20·09:24~09:38 두 번뿐**이다. 또 차단 30건은 전부 노출이 아니라 **섹터 집중 한도가 다수**였다(로그 head만 보고 확대 해석한 오류). 오후에 LIVE 진입이 없었던 실제 이유는 ① **K는 설계상 09:00~09:30에만 발동** ② D 오후 신호 중 KOSPI 3건은 **perf-gate 차단**(net −0.16%)이며, 노출과 무관하다(KOSDAQ 2건은 게이트·노출 모두 여유였는데 주문이 없어 원인 미상 — 거부는 DB 행을 안 남기고 컨테이너 재생성으로 장중 로그가 소실돼 재확인 불가). → **이 knob의 근거는 "오후 기회 확보"가 아니라 "갭 구간 상관 노출 축소" 하나로 좁혀진다**(제시했던 것보다 약함). 기본 0이라 실동작 영향은 없었음. 판정 `openingCapped`(순수 정적, 창 시작 포함·끝 배타). 로그 사유에 `·개장창N분` 표기. ⚠️ **기본 0=비활성이라 기존 동작 무변경** — forward 검증 후 켤 것(예 25). **+ 성과 근거 확보(2026-08-21, `feature-mining` 개장후경과분 축 신설로 측정)**: 8/14·8/20의 '아침 진입만 지는' 패턴이 우연인지 물으려고 시각 축을 넣고 21거래일(n=3,032 섀도우, close net) 집계 — **개장 15분 이내 진입이 −0.70%(n=808)로 최악**이고 나머지 구간은 −0.13~−0.50%다(21거래일 창). ⚠️ **효과 크기는 창 의존적** — 40일 창으로 다시 뽑으면 `<15` −0.81% vs `15~60` −0.79%로 **절대 net 격차가 거의 사라진다**(21일 창의 −0.70 vs −0.24와 다름). 다만 **진입-대조군 edge는 40일 창에서도 방향 유지**: `<15` **edge −0.31%p**(진입이 미진입보다 나쁨) vs `15~60` **edge +0.22%p**(진입이 나음), 격차 0.53%p. 즉 '개장 직후를 조인다'는 처방은 지지되나 **기대 효과는 21일 창 수치보다 작게 잡을 것**. ⚠️ K가 09:00~09:30에만 발동해 이 버킷을 채우는 교란이 의심돼 **전략별로 분해**하니 **8개 전략 중 7개가 같은 방향**(자기 전략의 늦은 진입 대비 개장 15분 진입이 열위): H −1.49%p(n=65) · F −0.52 · G −0.48 · J −0.39 · K −0.24 · D −0.23 · E −0.06, **예외는 B(+0.68)뿐**. 즉 K 아티팩트가 아니라 **전 전략 공통 효과**다(중앙 penalty ≈ −0.32%p). → 이 knob의 근거가 '갭 구간 상관 노출 축소'(약함)에서 **성과 실측**으로 강화됐고, **창을 기본 30분이 아니라 15분으로 좁히는 편이 데이터에 맞는다**(15~60분 구간은 −0.24%로 오히려 두 번째로 좋다). ⚠️ 원인은 미규명 — '개장 직후 신호의 질'인지 '그 시간대 변동성'인지 구분 못 했다(처방은 같지만 해석이 다름). ⚠️ 섀도우 close net 기준이라 LIVE 체결 기준과는 다를 수 있다. env `TRADING_RISK_OPENING_EXPOSURE_CAP_PCT`/`TRADING_RISK_OPENING_WINDOW_MINUTES`(compose 패스스루). 테스트 3. **✅ 활성화(2026-08-24, 사용자 결정) — prod `cap=25%` · `window=15분`**: 8/21에 확보한 성과 근거(개장 15분 진입이 8개 전략 중 7개에서 열위)가 40일 창에서도 **edge 기준으로 유지**됐다 — `<15분` **edge −0.31%p**(n=1,032) vs `15~60분` **+0.22%p**(n=1,193), 격차 0.67%p. ⚠️ **창은 기본 30분이 아니라 15분** — `15~60분` 구간은 오히려 두 번째로 좋아서 30분 창은 좋은 구간까지 함께 조인다. ⚠️ cap 25%는 `max-positions=20`(종목당 5%) 기준 **개장 15분에 동시 5종목**에 해당한다(8/14엔 27분 만에 47%가 소진됐다). BEAR 국면 상한(30%)과 min 결합이라 약세장에선 거의 안 물리고 BULL(100%)에서 주로 작동한다. ⚠️ **K가 가장 큰 영향을 받는다**(09:00~09:30 전용 전략). ⚠️ 이건 진입을 고르는 게 아니라 **예산을 선착순으로 막는** 레버라 원인(신호 질)에 직접 대응하진 않는다 — 차단이 대조군을 안 남기므로 forward 검증이 안 된다는 한계도 있다(대안이던 전역 `OPENING_TOO_EARLY` 진입 필터는 미채택).
  - **② 서킷브레이커(저점대비 반등 상태기계)**: `isRiskOff()`는 지수별 **장중 저점 추적 상태기계**(메모리, 일별 리셋, `synchronized`). **발동**: 지수가 장중 고점 대비 `crash-halt-pct`(3.0)%p 이상 급락 + 절대 -3% 이하 → 신규진입 즉시 중단 + `PositionExitService` 청산 가속. **재개(둘 중 하나)**: ① 장중 저점 대비 `rebound-pct`(2.0)%p 반등(아직 −3% 이하여도 조기 재개, 예 −6%→−4%) **OR** ② 지수가 **−`crash-halt-pct`(−3%) 위로 회복**(레벨 기반). 즉 저점서 확 튀거나(①) 크래시 레벨을 벗어나거나(②) 하면 재개. **재개 후 재무장**: 새 고점 기준으로 다시 crash-halt%p 급락해야 재발동(−4% 유지로는 재발동 안 함 = 경계 깜빡임·데드캣 방지). 코스피/코스닥 중 하나라도 halt면 off. env `TRADING_RISK_CRASH_HALT_PCT`/`TRADING_RISK_REBOUND_PCT`. 단위테스트 `MarketRiskGuardTest`(저점반등 재개+새급락 재발동, 반등임계 미만 유지). ⚠️ 재개해도 하락장 국면·노출상한·perf-gate가 실거래는 계속 제한(서킷은 "칼날 구간 하드정지"만 해제). 상태는 1분 주기 `closeDuePositions`가 폴링해 갱신.
  - **②-b 시장폭(breadth) 리스크오프**(2026-07-13, 지수 서킷 사각지대 보완): 지수(시총가중 프록시)는 대형주 방어로 멀쩡한데 시장 전반이 무너진 날(실측 7/13: 코스닥 지수 서킷 미발동인데 중앙 종목 −4.4% 붕괴 → H 7건 −20,420원)을 잡는다. `MarketRiskGuard.breadthRiskOff(market)`: 해당 시장 **상승비율 < `breadth-riskoff-adv-pct`(15%) AND 중앙 등락 ≤ −`breadth-riskoff-median-pct`(3%)**면 신규진입 차단. **진입 차단 전용 — 강제청산 없음**(청산 가속은 지수 서킷만). degrade open: breadth 스냅샷이 없거나 **40분 초과(마감 후·전일분)면 판정 안 함**(`MarketBreadthService.isFresh` — 아침 첫 스캔 publish 전엔 미작동, 전일 폭락 데이터 오발동 방지). 발동/해제 전이 Discord 알림(⚠️/✅, PositionExitService edge-trigger — **컷오프(15:30) 이후 음소거, 2026-07-24**: 장 종료 40분 뒤 breadth 스냅샷 신선도 만료가 "판정 중단"→off 전이를 만들며 ✅해제 알림으로 위장(실측). 상태 갱신은 계속돼 익일 아침 가짜 전이도 방지. 지수 서킷 전이 알림도 동일 음소거. 테스트 `컷오프_이후_리스크전이는_알림_생략`). env `TRADING_RISK_BREADTH_ADV_PCT`/`TRADING_RISK_BREADTH_MEDIAN_PCT`(compose 패스스루 완료), 가시화 `risk-status`의 `kospiBreadthOff`/`kosdaqBreadthOff`. 단위테스트 `MarketRiskGuardTest` 3(발동/신선도/AND조건). ⚠️ **@ConfigurationProperties 레코드에 보조 생성자 금지** — Spring이 JavaBean 바인딩으로 폴백해 기동 실패(`NoSuchMethodException: <init>()`, 실측) — canonical 하나만.
  - **③ 섹터 집중 한도**(레이어 4.1): 같은 업종 미청산 보유가 `max-positions-per-sector`(3) 이상이면 진입 차단(동반급락 분산). `Order.sector`(진입 시 `quote.sectorName()` 태깅)로 `countOpenPositionsBySector` 카운트. 매도/수동주문은 sector=null이라 면제. ⚠️ 업종 분류는 KIS `bstp_kor_isnm` 단일 기준(세분류 아님).
  - **④-1 전략별 적응형 catastrophic stop**(`StrategyStopProvider`, `trading.adaptive-stop`, `GET /admin/exit-stop`, 2026-07-09): 고정 −7% 대신 **전략별 승자 MAE worst10(p10, 승자 10%만 희생 지점)**을 손절선으로 자동 채택, [`min-stop-pct` 3·`max-stop-pct` 10%] 클램프. **fail-closed**: 승자 표본 <`min-samples`(30)면 고정 −7% 유지, `catastrophic-stop-pct`=0이면 항상 0(마스터). `PositionExitService`가 `stopProvider.stopPct(strategy)`로 판정(`MarketRiskGuard.catastrophicStopHit(buy,price,pct)` 오버로드). TTL 30분·`MaeAnalysisService` 기반. 실측 채택: C −6.5%·G −5.7%·B −5.5%(n≥30), 나머지 −7% 고정. **🔴 손절 자체가 net을 갉아먹고 있다(2026-08-25 실측, 미조치)**: `heat-analysis`의 `stopSim`은 **히트 건수만** 주고 net을 안 줘서 "손절선을 조이면 나아지나"를 물을 수 없었다. `trade_outcome`의 MAE(trough)로 직접 시뮬(종가 horizon, net)하니 **모든 전략에서 손절이 없을수록 좋다**: D 무손절 **−0.12%** vs stop10 −1.53 vs stop8 −1.68 (n=904) · G −0.44 vs −0.90 vs −1.06 (n=677) · J −0.04 vs −0.70 (n=268) · K −0.39 vs −0.79 (n=387) · A −0.39 vs −0.56 (n=90). 즉 **현행 손절이 D 기준 약 1.4%p/건을 태우고 있고, 조이면 더 나빠진다**(제안됐던 max 클램프 10→8은 정확히 반대 방향이었다). 해석: 이 전략들은 일중 회복형이라 깊은 되돌림이 종가엔 대부분 복구되는데, 손절이 그걸 **실현 손실로 확정**시킨다. ⚠️ 그렇다고 손절 제거는 **다른 종류의 리스크**다 — 이 시뮬은 종가 보유 전제이고, catastrophic stop의 존재 이유는 평균 net이 아니라 **꼬리 사고**(거래정지·분식·연속 하한가) 방어다. 표본 27거래일에 그런 사건이 없었다면 시뮬은 그 가치를 0으로 친다. ⚠️ 또 MAE는 **순서 미상**이라 저점이 먼저 왔다고 가정한 근사다. → **미조치, 판단 대기**. 조치한다면 "손절 폐지"가 아니라 `TRADING_ADAPTIVE_STOP_MAX_PCT` 상향(꼬리만 남기기) 또는 전략별 차등이 먼저다. **B·C가 LIVE라 실손절 실제 변경**. ⚠️ peak/trough 순서 근사(승자희생 보수적 과대). env `TRADING_ADAPTIVE_STOP_ENABLED`(compose 패스스루). 테스트 `StrategyStopProviderTest`.
  - **④-2 상한가 조기청산(전 종목 최우선 익절, 2026-07-10)**: 당일 등락률(`fetchDayChangeRate`=일봉 prdy_ctrt) ≥ `trading.limit-up-lock-pct`(기본 **29.0**, +30% 상한가 근접)이면 **전 종목·전 전략 즉시 청산**("상한가익절") — 우선순위 최상(손절·리스크오프보다 앞). 근거: 상한가=오늘 상방 0 + 물량 풀리면 갭리스크 → 잠금이 비대칭 유리(튜닝TP 아니라 시장 하드천장 이용, 실전 금호타이어 관찰 반영). 이익 종목(price>buyPrice)만 등락률 조회(KIS콜 절감). `limitUpLockPct` 필드주입(생성자 무churn). ⚠️ 연속 상한가는 못 먹음(수용). env `TRADING_LIMIT_UP_LOCK_PCT`(0=비활성). 단위테스트 `PositionExitServiceTest.상한가_도달시_전략무관_즉시_익절청산`.
  - **④ 개별 catastrophic stop**: 매수가 대비 현재가가 -`catastrophic-stop-pct`(기본 7.0%) 이하면 **보유시간/국면 무관 즉시 청산**(`PositionExitService`가 매 틱 미청산 포지션 현재가 점검 → `riskGuard.catastrophicStopHit`). 우선순위: 손절 > 리스크오프 > 시간경과 > 장마감. ⚠️ **튜닝용 TP/SL이 아니라 재난 방지용 넓은 바닥**(데이터 기반 청산 정책은 그대로 유지 — 사용자 명시 요청으로 추가). 0이면 비활성. DRY_RUN/LIVE 공통. 단위테스트 `MarketRiskGuardTest`(손절 hit/비활성) + `PositionExitServiceTest`(손절 청산). ⚠️ 손절 활성 시 미청산 포지션마다 매 틱 현재가 1콜(≤max-positions 10).
  - DRY_RUN/LIVE 공통 적용(리스크관리 흐름을 dry-run으로도 검증). `TradeOutcome`(전략 섀도우)은 영향 없음 — `trade_order`(실제/가상 주문)만 리스크 통제 반영. env `TRADING_RISK_*`, 가시화 `GET /api/v1/admin/risk-status`(국면·노출상한·현노출·서킷여부). 단위테스트 `OrderServiceTest`(리스크 차단) + `PositionExitServiceTest`(서킷 청산가속).
- **레이어 3.1 완료 — ATR 변동성기반 사이징**(`PositionSizer`, `trading.sizing`): 고정 "순자산×maxOrderPct" 대신 **거래당 위험예산(순자산×`risk-per-trade-pct` 0.5%)을 ATR(평균진폭)×`atr-mult`(2.0)로 나눠** 수량 산정 → 변동성 큰 종목 적게·작은 종목 많이(거래당 손실폭 균등화). **1주문 상한(순자산×maxOrderPct)을 천장**으로 둬 절대 초과 금지(PolicyGate 하드캡과 정합). `OrderService.submitEntry`가 사이징 호출. ATR = 일별시세 True Range 평균(`KisDailyPriceResponse`에 고가 `stck_hgpr`/저가 `stck_lwpr` 추가; 고/저 있으면 정식 TR=max(H-L,|H-prevC|,|L-prevC|), 없으면 |ΔC|로 degrade — `computeAtr` 순수함수). **ATR 미산출/비활성(`atr-enabled=false`)이면 고정 상한 사이징으로 fallback**(기존 동작). env `TRADING_SIZING_*`, 미리보기 `GET /api/v1/admin/sizing?stockCode=&price=&netAssets=`(ATR·수량 vs 상한 비교, 튜닝용). 단위테스트 `PositionSizerTest` 6. ⚠️ ATR은 진입마다 일별시세 1콜(미캐시) — 진입 드물어 수용 가능하나 노이즈 많으면 캐시 고려.
- **레이어 4 완료 — 비용 현실화(`ExecutionCostModel`, `trading.cost.execution`)**: 모든 net 수치에 **슬리피지**를 반영하고 **유동성 필터**로 거래 유니버스를 정의해 LIVE 성과를 현실화. ⚠️ **완전한 과거 인트라데이 백테스트는 KIS 과거 분봉 부재(당일치만)·일봉 ~30일 한계로 비현실적** — 인트라데이 전략은 포워드 섀도우 + 청산 what-if가 검증 하니스이고, 거기에 정직한 비용을 넣는 게 레이어 4의 실효.
  - **슬리피지 모델**: **진입 시점엔 실측 호가 스프레드**(호가창 API **`inquire-asking-price-exp-ccn`** FHKST01010200 — ⚠️ 경로는 `-exp-ccn` 필수, `inquire-asking-price`는 404. 실배포로 검증: 005930 bestAsk/bestBid·스프레드 1틱 정상 → 최우선 매도/매수호가 → `(ask-bid)/mid×100` = `roundTripSlippagePctFromSpread`), `StrategyEvaluator`가 진입 시 조회해 `TradeOutcome.entrySlippagePct`에 저장 + 유동성 필터(`tradable(거래대금, 슬리피지)`)에 사용. **호가 미가용/실패 시 KRX 호가단위(tick) 추정으로 degrade**(`estimateRoundTripSlippagePct(가격)` = `spread-ticks`×tick/가격 + `base-slippage-pct`, `tickSize` 순수함수). 비활성(`enabled=false`)이면 0(기존 동작).
  - **net 통합**: `StrategyPerformanceGate`·`OutcomeAnalysisService`·`ExitTimingService`·`ExitStrategyService`가 슬리피지를 `round-trip-pct`에 가산해 차감. **`StrategyPerformanceGate`·`OutcomeAnalysisService`는 진입 시 실측 저장값 `entrySlippagePct` 우선**(없으면 tick 추정), `ExitTiming`·`ExitStrategy`는 `OutcomeSample.buyPrice` tick 추정. 검증 `GET /api/v1/admin/spread?stockCode=`(실측 호가·슬리피지).
  - **유동성 필터**(`tradable`): `StrategyEvaluator`가 건전성 체크 후 **거래대금(거래량×가격) < `min-turnover-krw`(5억) 또는 추정 슬리피지 > `max-round-trip-slippage-pct`(1%)면 진입 제외** → 슬리피지가 엣지를 잡아먹는 종목을 거래 유니버스에서 배제(저가주·품절주). env `COST_EXECUTION_*`, 미리보기 `GET /api/v1/admin/execution-cost?price=&turnoverKrw=`. 단위테스트 `ExecutionCostModelTest` 7. 🐞 **개장 후 40분 전 종목 블랙아웃 버그(2026-07-13 발견·수정)**: 거래량을 **quote(1h 캐시)의 누적거래량**으로 계산했는데, **08:40 섹터밸류에이션 배치가 전 종목 quote를 개장 전 상태(누적거래량 0)로 캐싱** → 09:00~09:40(캐시 만료 시각 = 배치시각+1h) 동안 **전 종목이 거래대금 0으로 tradable 탈락** → 스캔·공시 경로 전 전략 기록/진입/알림 0(일자별 첫 기록이 일관되게 09:40~09:45였던 원인, K 개장갭 전략 표본 0의 원인). **수정: 거래량 소스를 일봉 당일 누적거래량(`signal.todayVolume()`, 비캐시)으로 교체**(추가 KIS 콜 0). quote 캐시의 나머지 용도(관리종목·PER/PBR·업종명)는 정적이라 무해. 검증은 다음 거래일 09:00~09:30 첫 기록 존재로.
  - **시장충격 캡(호가 잔량 walk)**: `PositionSizer`가 사이징 후 호가창(`fetchOrderBook` 10단계 매도호가+잔량)을 받아, **매수 주문이 최우선 매도호가 대비 `max-impact-pct`(0.5%) 넘게 가격을 밀면 그 충격 내 흡수 가능 잔량으로 수량 캡**(`maxSharesWithinImpact` — 밴드 내 잔량 합). 캡 0이면 수량 0→진입 스킵. `marketImpactPct(asks, shares)`는 walk VWAP 충격(진단). DRY_RUN/LIVE 공통. env `COST_EXECUTION_MAX_IMPACT_PCT`(0=비활성). 단위테스트 `ExecutionCostModelTest`(밴드잔량·walk VWAP) + `PositionSizerTest`(캡 축소). ⚠️ 현재 최우선 1호가 잔량 기반 — 호가 갱신·취소 빈번한 종목은 실제와 차이 가능. 사이징 시 호가 1콜 추가(진입 드물어 수용).
  - ⚠️ **tick 추정은 실측 fallback** — 호가 미가용 시. `spread-ticks`·`base-slippage-pct`로 보수화.
  - **다음(후보)**: ~~일봉기반 스윙전략 제한적 백테스트(30일 일봉)~~ → **장기 일봉 확보로 대체·해소(2026-08-28, 아래 항목)**. (대부분의 설계 레이어 완료 — 이후는 포워드 표본 누적 후 임계값 튜닝이 본질.)

### 일봉 히스토리 (백테스트 인프라, 2026-08-28)
- **왜 지금까지 백테스트가 없었나 — 제약은 "일봉"이 아니라 "분봉"이었다**: 인트라데이 전략은 KIS가 **과거 분봉을 안 줘서**(당일치만) 포워드 섀도우가 유일한 검증 수단이었다. 그런데 **멀티데이 전략은 일봉만으로 완전히 재현된다**(진입·추가매수·평단·트레일링이 전부 종가/고저 계산) → **장기 일봉만 확보하면 진짜 백테스트가 열린다.** 계기는 "우량주 낙폭 분할매수 + 평단 트레일링" 멀티데이 전략 구상(사용자).
- **소스 = 네이버 금융 `siseJson`**(인증 불필요). 2026-08-28 실측 비교로 채택:
  - **1콜에 10년치(2,613행/161KB) — 페이징 없음.** 워치리스트 1,500종목 = **1,500콜**.
  - 대안이 전부 열위: KIS `inquire-daily-price`(FHKST01010400, 현행 `fetchDailyPrices`)는 **~30거래일**뿐 / KIS `inquire-daily-itemchartprice`(FHKST03010100)는 기간 지정은 되나 **1콜 100건**이라 10년이면 종목당 ~25콜 × 1,500 = 37,500콜, rateGate(8/s)로 **~78분** + 장중 라이브 호출과 경합 / **KRX 403**(CLAUDE.md 기존 "KRX 차단" 기록과 일치) / **Yahoo 429**.
  - 실측 확인: ① **수정주가 반영**(2017년 삼성전자 36,100원 = 50:1 분할 소급) ② **2000년까지** 존재 ③ **상장폐지 종목도 폐지 시점까지** 남음(000030 구 우리은행 ~20190212) ④ 코스닥 **영문 코드(0001A0)**도 조회됨 ⑤ **15콜/2초 무지연에 차단 없음**.
- 구성: `daily_price` 테이블(`DailyPrice`, `stock_code`+`business_date` 유니크) + `NaverDailyPriceClient`(fetch + **순수 파서**) + `DailyHistoryBackfillService` + `POST /api/v1/admin/backfill-daily-history?years=10&limit=&startDate=&force=` · `GET /api/v1/admin/daily-history-status`. 쓰기는 JPA가 아니라 **JdbcTemplate 배치 upsert**(`on conflict do nothing`) — 370만 행 규모라 영속성 컨텍스트를 태우면 느리고, 재실행 안전성도 여기서 나온다.
- ⚠️ **파서가 거르는 두 가지가 핵심**(둘 다 실측 함정): ① **오늘 부분봉** — 장중 조회 시 오늘 행이 미완성 거래량으로 온다(10:47 실측 삼성전자 579만 vs 전일 1,683만). 안 거르면 **백테스트가 존재하지 않던 봉을 본다** → `maxDateInclusive` 파라미터로 차단하고, 16:00 이전 실행은 전 거래일까지만 넣는다. ② **폐지 종목 잔행의 OHLC 0**(000030 20190212 시가 0). ⚠️ 없는 코드도 **HTTP 200 + 헤더만** 오므로 실패 판정은 상태코드가 아니라 **행 수 0**으로 한다. ⚠️ 헤더 행은 **작은따옴표**, 데이터 행은 큰따옴표 — 정규식이 헤더를 거르는 근거가 이 차이다(실제 10년 응답 2,613행 전부 매치 검증).
- ⚠️ **생존편향 — 이 데이터의 가장 큰 한계**: 대상이 **오늘 기준** 워치리스트(시총 상위 1,500)라, 여기 돌린 백테스트는 "10년을 살아남아 지금 상위권인 회사"만 본다. **물타기 계열 전략이 이 편향에 가장 취약**하다 — 물타기가 죽는 경로가 곧 "계속 내려가 밀려나거나 폐지되는" 시나리오인데 그게 표본에서 통째로 빠진다. → 결과는 **낙관 상한**으로 읽고 **절대 수익률이 아니라 파라미터 간 상대 비교**에 쓸 것. (소스가 폐지 종목 가격을 주므로 과거 시점 유니버스를 따로 구성하면 교정 가능 — **미실행**, 사용자가 현 워치리스트로 결정.)
- ⚠️ **수정주가라 과거 시점의 실제 호가·틱 사이즈와 다르다**(2017년 삼성전자를 36,100원으로 보면 tick이 실제와 어긋남) — 멀티데이 스케일에선 무시 가능하나 슬리피지를 이 값으로 정밀 추정하지 말 것.
- ⚠️ **라이브 매매 경로는 종전대로 KIS**다 — 소스 이원화가 의도(네이버는 비공식 엔드포인트라 포맷·가용성 변동 위험이 있고, 연구 데이터는 부분 실패가 유효하다). 클라이언트도 공용 `RestClient.Builder`(read timeout 5s)를 안 쓰고 전용 팩토리(30s)를 둬 대량 잡 설정이 라이브로 새지 않게 했다.
- env `DAILY_HISTORY_THROTTLE_MS`(기본 250, compose 패스스루). 테스트 `NaverDailyPriceParserTest` 5 + `DailyHistoryCoverageTest` 4.

### 종목 선정력 측정 (F-Score 랭킹 스프레드, 2026-08-28)
- **🔴 질문이 바뀐 계기(사용자 지적)**: "우상향한 종목에 물타기하면 당연히 +다 — 중요한 건 **우상향할 종목을 고를 수 있느냐**"다. 맞는 지적이고, 이게 백테스트 설계를 바꾼다 — 생존편향 유니버스에서 절대 수익률을 재면 **전략이 아니라 시장 드리프트**를 재게 된다. → 질문을 **"스코어 상위가 하위보다 나은가"**로 바꾼다.
- ⚠️ **왜 스프레드는 생존편향에 강한가**: 유니버스가 "오늘 살아남아 시총 상위"라 전 구간이 통째로 들어올려지지만 그 효과는 **상·하위 버킷에 공통**으로 걸린다. 절대값은 못 믿어도 **차이는 유효**하다(이 시스템의 진입-대조군 edge와 같은 논리 — **lift는 반사실이 아니고 차이만 반사실이다**).
- **왜 F-Score인가**: 유명해서가 아니라 **원 질문이 이 전략의 셋업과 같기 때문**이다 — "싸진(떨어진) 종목 중 재무적으로 건강해서 회복할 놈은 누구인가". 기준이 전부 **이진**이라 튜닝 여지가 거의 없어 과적합 저항이 좋다(ATR·업종 pocket을 다중검정으로 죽인 전례를 보면 중요한 성질).
- 구성: `financial_fact`(종목×사업연도 원시 항목) + `DartAnnualFactExtractor`(순수) + `FinancialScore`(순수, 7개 기준) + `FinancialFactBackfillService` + `FinancialSpreadAnalysisService`. 엔드포인트 `POST /admin/backfill-financial-facts?years=10&limit=&maxCalls=` · `GET /admin/financial-facts-status` · `GET /admin/financial-spread?horizonMonths=12&minEvaluated=5&highMin=6&lowMax=2` · `GET /admin/financial-score?stockCode=&businessYear=`(진단).
- ⚠️ **점수가 아니라 원시 항목을 저장한다** — DART는 일일 한도(~2만)라 재수집이 비싼데 점수 공식은 반드시 바뀐다(CFO 2개 추가). 원시값을 쥐면 공식 변경이 **재계산 0콜**로 끝난다. 비용: 사업보고서(11011)만 = 종목당 연도 1콜 = 10년×1,500 ≈ **15,000콜**(하루). ⚠️ 1콜이 **당기+전기**를 함께 줘 변화량 5개 기준을 추가 호출 없이 계산하지만, 연도별 점수를 내려면 **연도마다 1콜**이 필요하다(절약 불가).
- ⚠️ **백필은 캐시 우회**(`fetchSingleCompanyFinancialsUncached`) — 소급은 키마다 한 번뿐이라 캐시가 히트할 일이 없는데, Redis에 15,000건(수십 MB)이 1시간 쌓인다. 그 Redis는 **다른 프로젝트와 공유하는 컨테이너**(redis-local)이고 VM은 e2-medium(4GB)이다.
- ⚠️ **판정 불가를 미충족(0)으로 치지 않는 게 설계의 핵심** — 금융업은 유동자산/유동부채 항목이 아예 없는데 0으로 처리하면 업종 전체가 조용히 하위 버킷으로 밀려 **스코어가 업종 더미변수로 변질**된다. → `evaluated`(판정 가능 항목 수)를 함께 반환하고 분석이 `minEvaluated`(기본 5)로 거른다. 같은 이유로 레버리지는 부채비율(자본 분모)이 아니라 **부채/자산**을 쓴다(자본잠식에서 발산해 순위가 뒤집히는 것 방지).
- ⚠️ **look-ahead 차단이 이 분석 유효성의 전부**: 사업보고서는 사업연도 종료 후 90일(3월 말) 제출이라 Y년 재무를 Y년 중에 쓰면 미래를 보는 것이다 → 기준일 = **(Y+1)년 5월 첫 거래일**(제출 지연 여유 포함), 그 **이후** 첫 거래일 종가로 진입. horizon 끝이 데이터 밖이면 **표본에서 제외**(추정하지 않는다). 수급 소급의 '직전 거래일' 규칙에 해당하는 장치가 여기선 이것이다.
- ⚠️ **빠진 2개가 하필 중요하다** — `CFO>0`·`CFO>순이익`(발생액). 후자가 F-Score에서 **회계상 이익 부풀리기를 잡는 핵심**인데 주요계정엔 현금흐름표가 없다(`fnlttSinglAcntAll`로 바꾸면 가능). 즉 현 스코어는 **이익의 질을 못 본다** — 스프레드가 확인되면 그때 추가.
- ⚠️ **배당 미반영** — 일봉 소스가 수정주가(가격 조정)라 배당 재투자가 빠진다. **고배당 우량주가 구조적으로 불리**하게 평가된다.
- ✅ **3종목 → 30종목 분산으로 결정(2026-08-28, 사용자)**: 3종목이면 문헌상 초과수익 연 **3~5%p**가 포트폴리오 변동성 **~27%**(개별 35~45%, 상관 0.4 가정)에 완전히 묻혀, 선정력이 실재해도 "어느 3종목을 뽑았느냐의 운"이 압도한다. **선정력은 분산에서만 실현된다.** ⚠️ 대신 물타기는 종목당 자본을 키워 집중을 요구하므로 **분할매수 규모를 줄여야** 양립한다(30종목 × 3분할이면 최초 진입은 종목당 순자산의 ~1.1%). ⚠️ **아직 결정 안 된 것 둘**: ① 데이 트레이딩 북(현 LIVE 7전략)과의 **자본 배분** ② 순자산 ~1,054만 기준 종목당 35만·3분할 시 첫 진입 12만이라 **고가 우량주는 분할이 성립 안 함**(최소 1주 단위). 둘 다 스프레드 검증 통과 후에 정할 것.
- **⚠️ F-Score는 필터지 랭킹이 아니다 → 2차 정렬 축 필수**: 0~7 정수라 1,500종목을 30개로 못 좁힌다(6~7점이 수백 개). Piotroski 원 논문도 **저평가 구간 안에서** F-Score를 적용했다. 여기선 이미 가진 데이터로 **직전 12개월 수익률**이 그 역할을 한다(기준일 시점 과거 가격만 참조 → look-ahead 아님) — "우량기업 중 많이 떨어진 것"이라는 전략의 전제와 **정확히 같은 축**이다. → `PortfolioSim`(상위 N 동일가중, 기본 30) 시뮬을 버킷 통계와 **별도로** 낸다. 주지표는 절대 수익이 아니라 **같은 해 유니버스 평균 대비 초과분**(드리프트·생존편향이 유니버스 평균에도 걸려 차감된다).
- **⚠️ 내장 대조군 `RISEN`**: 같은 F-Score 풀에서 2차 정렬 **방향만 뒤집어** 뽑은 포트폴리오를 함께 낸다. **FALLEN > RISEN이어야 "떨어진 것을 산다"는 전제가 지지**되고, 비슷하면 F-Score만 작동한 것이며, **RISEN이 나으면 전제가 반증**된 것이다(모멘텀이 이긴다). 응답의 `portfolio`/`portfolioControl`을 반드시 함께 볼 것. 파라미터 `topN`·`scoreMin`.
- ⚠️ 판정은 **연도별 부호 유지**로 — 전체 스프레드가 양수여도 1~2년이 만든 것이면 채택 금지(클러스터 함정이 이 시스템에서 5회 재발했다). 응답의 `byYear`·`yearsPositive/yearsTotal`가 그 판정용이고 `caveats`가 해석 함정을 실어 보낸다.
- env `FINANCIAL_HISTORY_THROTTLE_MS`(기본 60, compose 패스스루). 테스트 `FinancialScoreTest` 6 + `FinancialSpreadLookaheadTest` 4 + `PortfolioSimulationTest` 5.
- **🔴 첫 실측 결과(2026-08-28) — 두 기둥이 모두 지지되지 않았다. 멀티데이 "우량주 낙폭 분할매수" 구상은 기각.** 적재 실측: 일봉 **1,502종목 2,811,208행 10.4분**(⚠️ 167종목은 빈 응답 = **워치리스트에 오래 전 상장폐지 종목이 11% 섞여 있다** — 씨앤상선·FnC코오롱·솔로몬저축은행 등. 별개 이슈로 미조치), 재무 **14,980콜 11,186행 21분**(실패 3,789 = 상장 전 연도, 정상). 분석 표본 **9,775행 / 1,287종목 / 기준연도 2017~2025**.
  - **① F-Score 선정력 없음 — 부호가 오히려 반대**: 상위(6~7) − 하위(0~2) 스프레드 **−6.63%p**, 상위 lift **−1.62%p**. 버킷별 lift가 score 0 **+9.17** · 2 **+6.77** ↔ 7 **−3.27**로 **저점수일수록 좋았다**. ⚠️ **단 이 결론은 신뢰도가 낮다** — 아래 ③ 참조.
  - **② 낙폭 축(전략의 전제)은 내장 대조군과 구분되지 않는다 = 사실상 신호 없음**: FALLEN(많이 떨어진 순) 평균 초과 **+5.87%p**로 좋아 보이나 **2025 한 해(+51.49%p)가 만든 것**이고 그 해를 빼면 **+0.17%p ≈ 0**이다(**클러스터 함정 6번째 재발**). 반면 대조군 RISEN(많이 오른 순)은 **+4.72%p·7/9년 양수**이고 최대기여연도를 빼도 **+2.96%p**로 부호가 유지된다. 연도별 맞대결은 **FALLEN 5 : RISEN 4 = 동전 던지기**. → **"떨어진 것을 산다"가 "오른 것을 산다"보다 낫다는 증거가 없다.** 전략의 핵심 전제가 반증됐다.
  - **③ ⚠️ 설계 시 서술("편향이 상·하위 버킷에 공통으로 걸리므로 스프레드는 유효")은 너무 강했다 — 생존편향은 버킷 간 비대칭이다**: 저품질 기업이 상장폐지될 확률이 훨씬 높으므로 **살아남은 저점수 표본이 훨씬 심하게 선택**돼 있다(죽은 정크는 유니버스에 없다). 즉 ①의 음수 스프레드는 **편향이 만든 아티팩트일 수 있다**. 유니버스 전체 평균이 **연 20.60%**(9년)라는 게 편향 크기를 보여준다 — 실제 KOSPI/KOSDAQ 수익률과 자릿수가 다르다. ⚠️ 반면 ②의 FALLEN vs RISEN 비교는 **같은 F-Score 풀 안에서** 이뤄져 이 교란이 훨씬 작다 → **②가 ①보다 신뢰할 만한 결론이다.**
  - ⚠️ **퀄리티는 정크 랠리에서 진다**는 알려진 패턴이 그대로 보인다 — 연도별 스프레드는 **6/9년 양수**인데(2018 +2.86 · 2019 +5.69 · 2021 +4.33 · 2022 +2.25 · 2023 +2.19 · 2024 +7.48), 대형 상승장 **2020(−8.53)·2025(−42.98)** 두 해가 합계를 뒤집었다.
  - ⚠️ 미해소 교란 둘: **배당 미반영**(수정주가라 고배당=대개 고퀄리티 가치주가 9년간 연 2~3%씩 불리하게 평가됐다) · **CFO 2개 누락**(이익의 질). 다만 이 둘이 ②의 결론(낙폭 축 무신호)을 되돌릴 여지는 작다.
  - ⚠️ **RISEN이 이겼으니 모멘텀으로 전환**은 하지 말 것 — RISEN은 **대조군으로 만든 것이지 사전 가설이 아니다.** 그걸 사후에 전략으로 승격하면 2026-08-21에 ATR·업종 pocket을 죽인 것과 **정확히 같은 다중검정 실수**다. 하려면 다른 기간·다른 유니버스에서 새 가설로 재검할 것.
  - 🐞 **horizon이 전략이 아니라 데이터 갱신 주기였던 설계 오류(2026-08-28, 사용자 지적)**: `horizon=12개월` 고정 보유로 돌렸는데 실제 전략은 **보유 상한 1개월 + 트레일링**이다. "DART 재무는 연 1회 갱신 → 연 1회 리밸런싱 → 보유 12개월"로 이으면서 **데이터 갱신 주기를 전략 보유기간으로 잘못 옮겼다.** 대가가 컸다 — **독립 관측이 연 단위 9개뿐**이라 2025 한 해가 전부를 지배했고(+51%p), 그게 "FALLEN +5.87%p"의 정체였다. ⚠️ **표본 10년은 보유기간과 무관하게 필요하다** — 하락장(2018·2020·2022)을 넣기 위해서다. 물타기·트레일링 계열은 정확히 하락장에서 죽는데 최근 3년만 보면 거의 상승장뿐이라 그 위험이 표본에서 사라진다. → 아래 `multiday-backtest`로 재검.

### 멀티데이 백테스트 (월별 진입 + 트레일링 청산, 2026-08-28)
- `MultidayBacktestService` + `TrailingExitSimulator`(순수) + `GET /admin/multiday-backtest?topN=&scoreMin=&armPct=&dropPct=&maxHoldMonths=&lookbackMonths=`. 재무 스코어는 공시 주기상 연 1회 갱신이 불가피하지만 **진입은 그 유효기간 안에서 매달** 가능하다(2024 재무는 2025-05~2026-04 동안 공개 정보) → 관측 **9 → 111 코호트**.
- ⚠️ **일봉 시뮬의 보수적 규약 둘**(하루 안의 고가·저가 순서를 모르므로): ① 오늘 고가로 스톱을 올리기 **전에** 오늘 저가로 스톱을 판정한다(반대로 하면 "고가가 먼저 왔다"고 가정하는 셈이라 낙관 편향) ② **갭 하락은 스톱 가격이 아니라 시가 체결**(빼면 하락장 손실이 조직적으로 과소평가된다).
- ⚠️ **분해용 벤치마크 둘이 이 엔드포인트의 값어치다**: `pickedHold`(같은 종목 단순보유) 대비 차이 = **청산 규칙의 기여**, `universeHold`(유니버스 전체 단순보유) 대비 차이 = **선정의 기여**. 합쳐 보면 무엇이 손실을 만들었는지 분리된다.
- ⚠️ **사전 등록 판정 `verdict`를 코드에 박았다** — 이 시스템은 클러스터 함정을 6회 재발시켰고 매번 수치를 **본 뒤에** 해석 기준을 만들었다. 기준: 코호트 ≥60 · 초과수익 >0 · 연도별 부호 ≥70% · LOO 부호 유지 · 대조군 대비 우위.
- **🔴 결과(2026-08-28, 111코호트·1,335종목·10년) — `verdict: FAIL`(4개 기준 전부 미달). 청산 규칙이 주범이고 낙폭 선정은 무효.**
  - 월평균 net: **유니버스 단순보유 +0.75%** > 선정 30종목 단순보유 **+0.62%** > **트레일링 적용 +0.23%**. 분해하면 **선정 −0.13%p · 청산 −0.39%p**.
  - **트레일링(+5% 무장 후 고점 −2%)이 두 방향 모두에서 손해**다(FALLEN −0.39%p / RISEN **−0.99%p**). 무장률 63.5%, 평균 MFE **+6.69%** — 즉 +5%에서 무장돼 −2%에 잘리는데 승자는 그 뒤로 더 갔다. **승자는 자르고 패자는 안는 구조**(손절이 없어 MAE −6.55%가 그대로 남는다). 이 시스템의 기존 발견(2026-08-25 "손절이 net을 갉아먹는다", D 트레일링 조기 트리거 의심)과 **같은 방향**이다.
  - **연도별 초과 부호 1/10** — 노이즈가 아니라 구조적으로 유니버스에 진다.
  - **최악 코호트 −17.80%**(30종목 분산인데도) — 손절 부재의 대가.
  - ⚠️ **주목할 비대칭**: 선정만 떼어 보면 RISEN(모멘텀)이 **+0.50%p/월**로 양수인데(FALLEN은 −0.13%p) 트레일링이 −0.99%p로 그걸 통째로 날린다. 연 6%p 수준이라 문헌상 모멘텀 프리미엄과 크기가 맞는다. ⚠️ **그래도 사후 승격 금지** — RISEN은 대조군이고 연도별 부호도 3/10이다. 승격하려면 **모멘텀 12-1을 사전 가설로** 다른 기간에서 재검할 것.
  - ⚠️ 절대 net(유니버스 +0.75%/월 ≈ 연 9.4%)은 **생존편향이 낀 값**이라 실제 인덱스 수익률로 읽으면 안 된다. 셋의 **순서**만 유효하다.

### 종목 선정 축 탐색 (`SelectionSweepService`, 2026-08-28) — 🔴 **0/16 통과, 선정 노선 기각**
- `GET /admin/selection-sweep?since=&until=&topN=&maxHoldMonths=`. 사전 근거가 있는 축 **8개 × 방향 2개**를 같은 규칙(월별 진입·상위 N 동일가중·단순보유 1개월·비용차감)으로 한 번에 잰다. 주지표는 **유니버스 동일가중 대비 초과수익**(생존편향이 벤치마크에도 걸려 차감된다). 청산은 단순보유 고정(트레일링 −0.39%p 확인됨), F-Score 필터는 해제(기각된 축을 필터로 남기면 실험이 오염된다).
- **⚠️ holdout 분리를 처음 도입했다 — 그리고 즉시 값을 했다.** 탐색 2016~2022(64코호트) / 확인 2023~2026(43코호트). 탐색에서 **5개 축이 통과**했는데 **holdout에서 전부 탈락**했다. 이 방어책이 없었으면 5건의 "발견"을 채택했을 것이다.
  | 축 | 탐색 초과 | 탐색 연도부호 | **holdout 초과** | **holdout 연도부호** |
  |---|---|---|---|---|
  | RET_6M LOW | +1.18%p | 6/6 | +0.75 | **2/4** (LOO −1.01) |
  | TURNOVER LOW | +1.08%p | 6/6 | **−0.57** | 1/4 |
  | HIGH_52W LOW | +1.04%p | 6/6 | +0.94 | 3/4 (LOO −0.64) |
  | VOLUME_TREND HIGH | +0.68%p | 5/6 | **−1.45** | 0/4 |
  | RET_3M LOW | +0.62%p | 5/6 | +0.42 | 2/4 (LOO −1.44) |
- ⚠️ **탐색↔holdout 격차 자체가 생존편향의 크기를 보여준다**: 탐색 구간 상위가 전부 **"많이 떨어진 것"·"작고 덜 거래되는 것"** — 즉 **폐지 확률이 가장 높은 구간**이다. 탐색(2016~2022)은 오래돼 폐지가 이미 일어난 기간이라 "떨어졌지만 살아남은" 표본만 남고, holdout(2023~2026)은 최근이라 그 편향이 덜 여물었다. TURNOVER LOW가 +1.08 → **−0.57**로 부호까지 뒤집힌 게 그 증거다.
- ⚠️ **판정 기준의 결함 하나**: `vsOpposite > 0`만 요구해 **HIGH_52W LOW(vsOpp +0.02)가 통과**했다 — 반대 방향도 +1.02라 **방향이 전혀 안 갈리는데** 통과한 것이다. 신호 없는 축은 양방향이 함께 좋거나 함께 나쁘다. 다음엔 **유의미한 마진**(예: ≥0.3%p)을 요구할 것.
- **저변동성 가설은 양 구간에서 기각** — `VOL_6M`은 LOW(−0.36 / −1.18)·HIGH(−1.06 / −2.33) **둘 다** 유니버스보다 나쁘다(극단이 양쪽 다 진다).
- ⚠️ holdout도 4년·43코호트로 얇아 "신호 없음"과 "표본 부족"을 완전히 가르진 못한다. 다만 **부호 반전 2건**은 표본 부족으로 설명하기 어렵다.
- **🔴 누적 결론 — 선정력은 세 개의 독립 도메인에서 전부 부재**: ① 인트라데이 신호(전략 12개, 진입-대조군 edge 전부 음수) ② 연간 재무(F-Score) ③ 가격·거래량 축 8개. **양 구간 모두에서 "유니버스 동일가중 단순보유"가 모든 선정 규칙을 이겼다.** 8/21 발굴 세션의 결론("유효한 축은 이미 게이트가 쓰는 전략×시장×국면뿐, 미탐색 pocket 없음")과 정합한다. → **다음 진전은 "무엇을 살까"가 아니라 "언제 노출될까"에 있다.**

### 로컬 인프라
- `docker-compose.yml` — PostgreSQL(`stockadvisor-postgres`, 5432) + Redis. **단, Redis는 기존에 떠 있던 공유 컨테이너 `redis-local`(6379)을 재사용** 중이므로, 캐시 키 정리 시 `FLUSHALL` 금지 — `kisQuote*`/`dartFinancials*` 패턴만 삭제할 것.
- Company 시드: `scripts/seed-companies.sql` (`docker exec -i stockadvisor-postgres psql -U stockadvisor -d stockadvisor < scripts/seed-companies.sql`). 6종목 적재됨(005930 등).
- 기동: `source set-env.sh && SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`. 헬스: `GET /actuator/health`.

### 검증 중 수정한 버그 2건
1. **검증 예외 → 500이던 것을 400으로**: `@Pattern` 위반 시 `ConstraintViolationException`이 미처리였음. `GlobalExceptionHandler`에 핸들러 추가.
2. **Redis 캐시 히트 시 `ClassCastException`**: `GenericJackson2JsonRedisSerializer`가 `@class` 미기록 → final record가 `LinkedHashMap`으로 복원됨. 캐시별 구체 타입 직렬화기(`Jackson2JsonRedisSerializer<>(mapper, type)`)로 교체. **캐시 미스만 테스트하면 안 잡힘 — 반드시 동일 키 2회 호출로 히트 경로까지 검증할 것.**
3. **배치 `UnexpectedRollbackException`**: `@Transactional` 루프 안에서 호출한 `recommend()` 실패가 공유 트랜잭션을 rollback-only로 만들어, catch 해도 커밋 시 전체가 터짐. 공시별 처리를 `DisclosureAlertProcessor`의 `REQUIRES_NEW`로 분리해 실패를 건별 격리. **외부 API를 루프에서 호출하는 배치는 반드시 건별 독립 트랜잭션으로.**

## 배포 (GCP Compute Engine)
- 단일 VM(e2-medium, 서울 `asia-northeast3`, Ubuntu 22.04, 30GB)에 docker-compose 풀스택. **반드시 인스턴스 1개**(스케줄러·인메모리 rateGate). 상세: `DEPLOY.md`.
- 아티팩트: `Dockerfile`(멀티스테이지 bootJar→JRE, TZ=Asia/Seoul), `docker-compose.prod.yml`(앱+PG+Redis, 시크릿 `.env`, 8080은 127.0.0.1만), `application-prod.yml`(ddl-auto update), `.env.example`(시크릿 템플릿).
- 검증됨: `docker build` + `docker compose -f docker-compose.prod.yml up` 으로 컨테이너 앱이 PG·Redis 연결·기동 확인.
- ⚠️ **지수 등락률 소스는 하나다(2026-08-20 오독 기록)**: `entry_market_change`(D의 잔차 계산), `MarketRegimeService.dayChangeOf`(국면 intraday 보정·반등일·고저 추적), `InverseIndexStrategy`(진입 판정) **전부 `KisApiClient.fetchIndexChangeRate("0001"/"1001")`**(60s 캐시)를 쓴다. 프록시 ETF(069500/229200)는 **MA3 국면 계산(`fetchDailyPrices`)에만** 쓰인다. 8/20 분석 중 "프록시 +4.1~6.6% vs 실지수 +1.7~2.3%로 두 소스가 어긋난다"고 판단했으나 **오독이었다 — KOSPI 값과 KOSDAQ 값을 맞비교한 것**(그날 KOSPI가 09:37 +4.07% → 11:04 +6.57% → 14:26 +5.78%로 상승, KOSDAQ은 +1.3~2.9% 구간). 지수 수치를 비교할 땐 **반드시 시장을 맞출 것**.
- ⚠️ **소스 tar에 `.env`가 딸려가 prod 시크릿을 덮어쓰는 함정**(2026-08-18 실측): VM엔 git이 없어 소스를 tar로 올려 덮어쓰는데, **tar는 `.gitignore`를 보지 않는다** → 로컬 `.env`(29줄)가 prod `.env`(46줄, 시크릿·튜닝 knob 전부)를 덮어썼다. 컨테이너 재생성 전에 발견해 백업본으로 복구했지만, 그대로 배포했으면 **KIS 키·계좌번호·Discord 웹훅·LIVE 화이트리스트가 통째로 유실**됐다. → tar 생성 시 **`--exclude=./.env --exclude=./set-env.sh`** 필수(`--exclude=./build --exclude=./.git --exclude=./.gradle --exclude=./logs`와 함께). 배포 스크립트 첫 줄에 `cp .env /tmp/env.bak.$(date +%s)`를 두면 사고가 나도 되돌릴 수 있다(이번에 이게 살렸다). 전개 직후 `wc -l .env`로 줄수 검증 권장.
- ⚠️ **재배포 시 stale 빌드 함정**: VM에 새 소스를 tar로 덮어쓰고 `compose up -d --build` 해도, 빌드스테이지 `COPY . .` 레이어가 **캐시 적중**해 옛 소스로 컴파일될 수 있음(`compose-exit=0`·컨테이너는 재생성되지만 이미지 created 타임스탬프가 안 바뀜 → 신호). **소스 변경분 배포 땐 `docker compose -f docker-compose.prod.yml build --no-cache` 후 `up -d`**로 결정적 재빌드. 배포 검증은 ① 이미지 created 갱신 확인 ② **jar 내 클래스 확인은 `grep -a`(거짓음성! 클래스가 zip-DEFLATE 압축됨) 말고 `docker cp` 후 python `zipfile.read()`로 디코드해 식별자 검색** ③ 앱 UP = `@ConfigurationProperties` 레코드 바인딩 성공(yml 키 누락 시 기동 실패).
- ⚠️ **ddl-auto update + `boolean NOT NULL` 신규 컬럼 함정**(실측): 기존 행 있는 테이블에 기본값 없는 NOT NULL 컬럼을 Hibernate가 `ALTER ADD`하면 Postgres가 거부 → **Hibernate는 경고만 남기고 넘어가 컬럼이 안 생김** → 그 엔티티 **전체 조회가 깨짐**("column does not exist", 앱은 UP인데 런타임 전 기능 마비). **반드시 `@ColumnDefault("false")` 지정**(→ `ADD COLUMN ... default false not null` 성공). 이미 터졌으면 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS x boolean NOT NULL DEFAULT false`로 즉시 복구(재배포 불요). nullable 컬럼(Long/Double/String)은 무관.
- ⚠️ **ddl-auto update + enum 신규값 → CHECK 제약 미이관 함정**(실측, 2026-07-01 실주문 테스트에서 발견): `@Enumerated(STRING)` 컬럼(예: `trade_order.status`)에 Hibernate가 만든 `..._status_check` CHECK 제약은 **테이블 최초 생성 시점의 enum 값만** 포함. 이후 enum에 값 추가(예: `OrderStatus.PENDING_APPROVAL`) 시 ddl-auto update가 **CHECK 제약을 재생성하지 않음** → 새 상태로 INSERT 시 `violates check constraint "trade_order_status_check"`로 **주문 저장 자체가 500**. 즉시 복구(무재배포): `ALTER TABLE trade_order DROP CONSTRAINT IF EXISTS trade_order_status_check; ADD CONSTRAINT trade_order_status_check CHECK (status IN ('NEW','PENDING_APPROVAL','DRY_RUN','SUBMITTED','PARTIALLY_FILLED','FILLED','CANCELLED','REJECTED','FAILED'));`(현 enum 전체). enum 값 추가 배포 시마다 재발 가능.
- ⚠️ **`docker-compose.prod.yml`은 `environment:`에 명시한 env만 컨테이너로 전달**(실측, 2026-07-01): `.env`에 값을 넣거나 바꿔도 compose의 `environment:` 블록에 해당 키가 없으면 **컨테이너에 반영 안 됨**(app은 yml 기본값 사용). 예: `TRADING_PERF_GATE_ENABLED`/`TRADING_ADAPTIVE_EXIT_ENABLED`/`SIGNAL_B_*`/`SIGNAL_VOLUME_MULTIPLIER`/`SIGNAL_MIN_OPINION_SCORE`가 처음엔 미전달이라 튜닝이 안 먹었음 → compose에 pass-through 라인 추가로 해결(현재 이 7개는 추가됨). **prod에서 새 `SIGNAL_*`/`TRADING_*` env로 튜닝하려면 반드시 `docker-compose.prod.yml`의 `environment:`에도 `KEY: ${KEY:-default}` 추가**할 것(현재 미전달 knob 다수: `SIGNAL_C_*`, `TRADING_RISK_*`, `TRADING_SIZING_*`, `COST_EXECUTION_*`, `MARKET_REGIME_*` 등 — 필요 시 추가). 검증: `docker exec sa-app printenv | grep KEY`.
- ⚠️ **`docker logs`는 재배포하면 사라진다 → 로그 파일 영속화 필수**(2026-08-14 실측·조치): json-file 로그는 `/var/lib/docker/containers/<id>/`에 있어 **`compose up -d` 재생성 시 컨테이너와 함께 삭제**된다(재시작은 보존, 재생성은 소실 — 이 차이가 함정). 8/14 배포 2회로 **그날 장중 로그가 전부 소실**돼 사후 검증이 막혔다: 리스크가드/게이트 **거부는 `trade_order`에 행을 남기지 않으므로**(CLAUDE.md의 "거부면 기록 없이 REJECTED") 로그가 유일한 단서인데, "D-KOSDAQ 오후 신호 2건이 게이트·노출 모두 여유인데 왜 주문이 안 나갔나"를 영영 확인 못 했다. → **조치**: compose에 `./logs:/app/logs` 호스트 볼륨 + `LOGGING_FILE_NAME=/app/logs/app.log`(Spring relaxed binding) → 재생성·재배포에도 보존. 롤링 50MB×30일·총 2GB 상한(`LOG_MAX_FILE_SIZE`/`LOG_MAX_HISTORY`/`LOG_TOTAL_SIZE_CAP`). docker 자체 로그도 `logging.options.max-size 50m/max-file 5`로 무제한 증가 차단(단 재생성 시 사라지므로 **파일이 정본**). ⚠️ 사후 분석은 `docker logs`가 아니라 **`/home/user/stock-advisor/logs/app.log*`**를 볼 것.
- ⚠️ **예약(지연) 배포를 SSH 세션 안에서 대기시키지 말 것**(2026-07-29 실측): "15:31까지 sleep 루프"를 품은 gcloud ssh가 4시간 대기 중 세션 드롭(return 255)으로 **배포가 조용히 실패**. 지연 배포는 ① 로컬에서 시간 대기(백그라운드) → ② 시간 도달 후 새 SSH로 배포 실행 순서로 — SSH는 짧게만 유지.
- ⚠️ **장중 `--no-cache` 빌드가 라이브 앱 KIS 호출을 굶김**(실측 2026-07-16): e2-medium(2vCPU)에서 빌드가 CPU를 점유하는 ~10분간 구 컨테이너의 매도 주문 5건(114800·251340 지수반등 청산)이 연속 FAILED(🔴 주문 오류 알림). 재시작 후 청산 조건 재충족 시 자동 회복(손실 0)했으나, **장중 배포 시 빌드는 `nice -n 19 docker compose ... build --no-cache`로 감속(권장)하거나 미청산 포지션 0을 확인 후 진행**. 스캔갭 타이밍은 스캔 정합만 지키고 CPU 경합은 못 막음.
- ⚠️ 관리형 Redis(Memorystore)는 불필요·고비용 → VM 컨테이너로 자체호스팅. 비용 대략 월 $30(올인원 VM).

## Commands

The Gradle wrapper is checked in — always use `./gradlew` (not a system `gradle`).

```bash
./gradlew bootRun        # run the application
./gradlew test           # run all tests
./gradlew build          # compile, run tests, and package
./gradlew clean          # remove build outputs

# 로컬 프로파일로 실행 (ddl-auto: update, SQL 로깅) — PostgreSQL·Redis 필요
SPRING_PROFILES_ACTIVE=local ./gradlew bootRun

# 단일 테스트
./gradlew test --tests com.stockadvisor.RecommendationDomainTest
```

## Stack

- **Java 17** (Gradle toolchain — 로컬 JDK와 무관하게 빌드가 고정).
- **Spring Boot 3.4.1** — web, validation, data-jpa, data-redis, cache, actuator, lombok.
- 외부 호출은 Spring `RestClient` 사용 (`RestClientConfig`의 공용 빌더 주입).
- **JUnit 5** (`useJUnitPlatform`).
- Base package: `com.stockadvisor`.
- 설정은 `application.yml` + `application-local.yml`. **모든 자격증명은 환경변수 주입** (코드/설정에 하드코딩 금지).

## Architecture

추천 요청 흐름: `RecommendationController` → `RecommendationService` → ① `KisApiClient`(시세 PER/PBR) + ② `DartFinancialService`(DART 재무) → **가중 합산 점수** → `RecommendationRepository` 저장 → 면책조항 포함 응답.

### 점수 산출 (RecommendationService — 예시 휴리스틱)
- **밸류에이션 점수**(KIS, PER/PBR 각 0~50): `band(x, good, max)` — good 이하 만점(50)·max 이상 바닥(`high-floor` 기본10)·사이 선형. PER good10/max40, PBR good1.0/max4.0, 적자(≤0)는 `loss-score`(15). env `stockadvisor.valuation.*`. ⚠️ **구버전 `40−PBR×20`(PBR≥2→0점, 성장주 BUY 불가) 버그 수정** — high-floor로 고평가도 바닥점 유지해 우량 성장주도 BUY 도달 가능.
  - **업종 상대평가**(`SectorValuationService`, `valuation.sector`, 기본 on): 절대기준보다 우선 — 업종 중앙값이 있으면 **(종목 PER/PBR ÷ 업종 중앙값) 비율**로 점수(`band(ratio, good-ratio 0.8, max-ratio 1.5)`) → 테크/바이오 등 업종 고PER 편차 흡수(업종 평균보다 싸면 가점). 업종 중앙값은 워치리스트 전 종목 시세 집계(`median` PER/PBR, 업종당 ≥`min-stocks` 5)로 산출 — **장전 평일 08:40 배치(`sector-valuation-cron`)** + 수동 `POST /api/v1/admin/refresh-sector-valuation`, 조회 `GET /api/v1/admin/sector-valuation`. ⚠️ 전 종목 순회라 무거움(~워치리스트 콜), 1일 1회. 갱신 전(재기동 직후)·업종 표본부족이면 **절대기준 fallback**. 단위테스트 `SectorValuationServiceTest`(median).
- **재무 점수**(DART, `scoreByFinancials`): 매출성장률(≤40점) + 영업이익률(≤30점) + 부채건전성(≤30점).
- **최종 = 0.5×밸류에이션 + 0.5×재무**. 단, `Company.corpCode`가 없거나 DART 데이터가 없으면 **밸류에이션만** 사용(graceful degradation), reason에 "재무데이터 미반영" 표기.
- `DartFinancialService`는 최근 연간보고서(11011)를 직전연도부터 최대 2개년 역순 시도, 연결(CFS) 우선·없으면 별도(OFS). 금액 문자열은 콤마 제거 후 long 파싱.
- ⚠️ 점수 가중치/공식은 모두 검증 안 된 예시값. 실데이터 백테스트 전까지 투자 판단 근거로 쓰면 안 됨.

- `config/` — `RedisCacheConfig`(캐시 TTL 1시간, 캐시명 상수 `DART_FINANCIALS`/`KIS_QUOTE`), `RestClientConfig`(타임아웃 3s/5s).
- `config/properties/` — `DartProperties`, `KisProperties` (`@ConfigurationProperties`, `StockAdvisorApplication`에서 `@EnableConfigurationProperties`로 등록).
- `dart/` — `DartApiClient`: 전자공시 단일회사 주요계정(`fnlttSinglAcnt`) 조회. `@Cacheable(DART_FINANCIALS)`.
- `market/` — `KisApiClient`: 한국투자증권 국내주식 현재가(`FHKST01010100`) 조회, `@Cacheable(KIS_QUOTE)`. `KisTokenManager`: OAuth 접근토큰 메모리 캐싱 + 만료 60초 전 자동 재발급.
- `domain/` — `Company`(종목코드 PK ↔ DART corp_code 매핑), `Recommendation`(추천 이력), `RecommendationType`(BUY/HOLD/SELL = 매수/중립/매도).
- `service/RecommendationService` — **PER/PBR 룰 기반 예시 휴리스틱** (실제 검증 안 됨, 백테스트 필요).
- `common/` — `Disclaimer`(투자권유 면책조항 상수), `ExternalApiException`, `GlobalExceptionHandler`(ProblemDetail 응답).

### 외부 API 연동 시 주의
- DART/KIS 응답은 HTTP 200이어도 본문 상태코드로 성공 판정 (`DART status="000"`, `KIS rt_cd="0"`). 실패 시 `ExternalApiException` throw.
- 외부 API 응답을 캐싱하는 신규 메서드는 `@Cacheable` 캐시명을 `RedisCacheConfig` 상수로 통일.

### 컴플라이언스 (필수)
- 모든 추천성 출력에 면책조항 포함. **API 응답**(`RecommendationResponse.from()`)은 전체 문구 `Disclaimer.INVESTMENT_DISCLAIMER`, **Discord 알림**(진입·후속)은 사용자 요청으로 한 줄 축약 `Disclaimer.SHORT`("⚠️ 투자 참고용이며 투자권유가 아닙니다.") 사용. 신규 추천 출력 경로 추가 시에도 둘 중 하나 반드시 포함.

## 다음 단계 (이어서 진행할 작업)

1. ~~DART·KIS API 키 설정 및 검증 완료~~ (모든 키는 `set-env.sh`에만 보관, `.gitignore` 등록됨).
   - DART: `list.json`·`fnlttSinglAcnt.json` 실호출 확인, DTO 필드 일치.
   - KIS: **실전투자(real account) 키**. OAuth 토큰 발급 + `inquire-price`(005930) 실호출 확인, `KisQuoteResponse.Output` 필드 일치.
   - ⚠️ KIS 토큰 발급은 **분당 1회 제한** — `KisTokenManager`가 24h 캐싱으로 이미 대응. 테스트 시 토큰 재발급을 연속 호출하지 말 것.
   - (필요시) `DB_*`, `REDIS_*` 추가. → 실행: `source set-env.sh && SPRING_PROFILES_ACTIVE=local ./gradlew bootRun`
2. ~~Company 마스터 적재 + 추천 API e2e 검증 완료~~ (`scripts/seed-companies.sql`로 6종목 적재, 추천 API 실호출·DB 영속화·캐시 확인). **남은 작업**: DART 기업코드(corpCode) ↔ KRX 종목코드 자동 동기화 배치 미구현(현재는 수동 시드, corp_code도 삼성전자만 채움).
3. ~~DART 재무데이터 점수 반영 완료~~ (`DartFinancialService` 추가, `RecommendationService`가 밸류에이션+재무 가중합산. e2e 검증: SK하이닉스는 강한 재무로 매도→중립 상승). 시드 6종목 corp_code 모두 적재(DART `corpCode.xml` 매핑에서 추출). **남은 작업**: corp_code 자동 동기화 배치(현재 수동 시드), `corpCode.xml`(전체기업 30MB zip) 주기적 적재.
4. ~~공시 기반 신호 알림 파이프라인(Phase 1) 완료~~ (위 "신호 알림 파이프라인" 참조. 강제 트리거로 Discord dry-run 메시지까지 e2e 확인).
5. ~~워치리스트 적재 배치(Phase 2) + reconcile + 정기 스케줄 완료~~ — 코스피1000+코스닥500 적재, stale 삭제(reconcile), 평일 16:00 정기 동기화 모두 완료·검증. **남은 작업**: 영문 종목코드 검증 패턴(`@Pattern`) 보정.
6. ~~Discord webhook 실연동 완료~~ — 실제 채널 발송 검증됨. **개선 여지**: 현재 plain content → embed 포맷, 알림 rate-limit/묶음 발송.
7. **통합 테스트**: 인프라 없는 단위테스트만 존재. e2e는 수동 curl. `@SpringBootTest` + Testcontainers 자동화 미작성.
8. **추천/신호 로직 고도화**: 점수 가중치·신호 임계값 모두 예시값. 실데이터 백테스트로 검증 필요. "상승추이"도 현재 당일 등락률 단순 판정.

# Project Context
- Domain: Korean stock market (KRX, KOSPI, KOSDAQ)
- Primary API: DART OpenAPI (opendart.fss.or.kr)
- Language: Korean financial terminology preferred in comments
- Compliance: 자본시장법 준수, 투자권유 면책조항 필수
